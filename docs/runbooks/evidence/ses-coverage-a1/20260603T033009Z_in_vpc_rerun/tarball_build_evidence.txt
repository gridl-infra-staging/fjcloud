tarball_build_log=tarball_build.log
tarball_build_log_size_bytes=0
s3_upload_log=s3_upload.log
host_checkout_log=host_checkout.log

Findings:
- `tarball_build.log` is zero bytes because the local tar creation command was
  quiet and returned no stdout/stderr to capture.
- `s3_upload.log` proves a single `scripts.tar.gz` object of 85.3 KiB was
  uploaded to `s3://fjcloud-releases-staging/a1-vpc-rerun/20260603T033009Z-70468/`.
- `host_checkout.log` proves the staging host downloaded that same S3 object,
  extracted it, applied executable bits to `scripts/*.sh`, `scripts/launch/*.sh`,
  and `.runtime/*.sh`, and printed `HOST_CHECKOUT_READY`.
- `s3_cleanup.log` proves the transport object was later deleted.

Open questions: none for tarball transport evidence.
