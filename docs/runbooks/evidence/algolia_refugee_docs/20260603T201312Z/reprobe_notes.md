# Direct Re-probe Notes

- The checklist's original external URL command failed because `rg -o` prefixes matches with filenames when scanning multiple files; `xargs` then tried to curl invalid hosts beginning with `docs/getting-started/...:`. The corrected `rg --no-filename ...` probe returned HTTP 200 for both discovered external URLs.
- A bare staging validator run failed fast on missing `SES_FROM_ADDRESS`, `SES_REGION`, and inbound roundtrip variables because repo-local `.secret/.env.secret` does not carry the SES roundtrip contract. The staging SSM hydrator succeeded and produced the required `SES_FROM_ADDRESS`; this shell preserved that value when the generated exports were sourced from a temp file. `SES_REGION` and inbound S3/domain defaults were exported from documented defaults before rerunning the exact validator command.
- The successful staging run proved SES outbound-to-inbound authentication, signup, email verification, index create, quickstart batch/search, migration list/add/search/get/update/delete/synonym/rule routes, and teardown/admin cleanup.
- s89 remediation refreshed this bundle after tightening duplicate marker rejection and migration get/delete assertions. `validate_customer_quickstart_test_output.txt` now includes negative coverage for duplicate quickstart/migration markers, stale `OBJECT_ID_PRIMARY` content, and no-op `OBJECT_ID_SECONDARY` delete responses. The delete case now refetches `OBJECT_ID_SECONDARY` after delete and requires a 404/not-found result.
- s91 remediation updated `head_sha.txt` to validated source commit `19ba825af06f99782a91142564c387851f01a24c`, which contains the duplicate-marker rejection, stronger migration get/delete assertions, and refreshed evidence outputs from the s89 remediation commit.
