# Flapjack Cloud (fjcloud) — Roadmap

**Last updated:** 2026-07-28 — reconciliation against `origin/main` at `dd1dbba8d` after `jul27_pm_0` closed (the only registered fjcloud orchestration whose `closeout_artifact_check.py --roster` exits 0) and `jul27_pm_10` W1-A landed. This revision supersedes the 2026-07-27 header below and folds it in.
**`main` is no longer red.** The two gates the `jul27_pm` closeout recorded as failing on merged `main` now PASS when re-run individually at `dd1dbba8d`: `launch-evidence-freshness-contract` (unstuck by `7b036158f`, which replaced the unreachable require-zero policy with a closed baseline registry — see the corrected freshness row) and `source-pollution`. Also re-verified PASS the same way: `check-sizes`, `roadmap-v2-shape`, `status-doc-consistency`, `web-lint`, `rust-lint`, `secret-scan`, `migration-test`, `local-schema-drift-contract`, `local-multinode-migration-contract`. This is per-gate evidence, **not** a merged-union run — the P0 union row below stays open until one `bash scripts/local-ci.sh --fast` exits 0 on merged `main` and its output is pasted into a closeout.
**Newly merged since 2026-07-27:** public support-routing discoverability (`jul27_pm_8`, worker `50185`) and the M1E shared-migration-wire fjcloud half (`c274eb533`). **`jul27_pm_8` landed with no lane branch, no worktree, and no merge commit** — `git branch -a --list '*jul27_pm_8*'` is empty and `git worktree list` has no entry for it, so worker `50185` built directly in the shared canonical clone on `main`. That is the **second** instance in two days of the same deviation (`jul27_pm_5` was the first) and it is not cosmetic: a worker running in the shared clone holds that clone dirty for its whole run, and `batman/queue.py::_prepare_source_status_for_merge:281-297` refuses every co-resident lane's merge while it is. The lane-isolation deviation and the merge-loss blocker are the **same** root cause seen from two ends; read them together in the merge-loss row below.
This revision supersedes the 2026-07-26 header below and folds it in: the local correctness wave, A3C's real-Algolia receipt, migration OpenAPI registration, and replica-copy honesty all remain merged and accurate.
**Newly merged:** real-pipeline oracle emitter (`3e0bb35ec`) plus its revenue/fleet/infrastructure consumers (`d61f30b63`, `f7beebb87`, `8593ccbf1`); multi-node migration refusal boundary (`e3388261e`); terminal migration outcome UI (`e4cdd26ad`); F10C erased-tombstone salvage (`24bb15212`); M0C architecture decision; timeline UI recovery; exact privacy-erasure acceptance (`11cdd4055`); and the console end-effect batch (`9fd7e2ab2`, `47d163a4f`, `504f3af0f`, `29aeeafe9`, `df44f91c9`).
**Read the two console rows in `## Planned` before citing that batch: its count gate is met, its residuals are not.** The `jul27_pm_0` release-confidence batch is now the active local owner for test-wiring, skip integrity, evidence freshness, and accessibility.
**`jul27_pm_0` Wave 1 is COMPLETE — all five instrument lanes are on `main`:** local schema-drift detector (`c61639235`), launch-evidence freshness truth (`901da1da6`), skip-integrity audit (committed straight onto `main` at `e6ca6d8fe` — no lane branch and no merge commit ever existed for it, the mirror-image deviation from `jul27_pm_4`'s over-isolation, and the same root cause: no stage owns the merge), structural accessibility gate (`7b984cd60`), and test-wiring reachability (`dd10a4231`, merged by hand after being stranded — see the merge-loss row in `## Planned`). **Wave 1 built instruments, not features.** Read its result as one sentence: the project could not previously tell whether its own tests executed, whether its skips were honest, whether its launch evidence was current, whether the local schema matched its migrations, or whether the console was usable with a screen reader — and now it can. **Every instrument came back red on first honest reading**, which is the finding, not a failure: 163 of 226 shell tests were reachable from nothing (now 207 reachable / 20 quarantined / 1 manual, `unaccounted=0`); 6 skip rows always skip locally and 3 of them are counted `@p0_coverage`, so that count overstates proven local coverage by 3; 5 of 6 launch-matrix sections rest on evidence 45–63 days old against the project's own 14-day bound; and the local migration ledger could mark migrations applied without running them. Waves 2 and 3 are deliberately unauthored — author them from the specs in `chats/icg/jul27_pm_0_release_confidence_orchestration.md`, not from a guessed work list.

## Current corrections to historical planned rows

The long-form rows below retain dated investigation history. The following
2026-07-26 corrections are authoritative wherever that history conflicts:

- **Engine exposure:** E1's transport switch and design contract are merged but
  inert/default-off. The remaining owner is a staging-fleet TLS proof followed
  by removal of public tcp/7700; it is not a new endpoint-switch implementation.
- **Production data plane:** a non-vacuous production signup → provision →
  ingest → exact-search → zero-residue-cleanup proof has run. The open work is
  recurring proof freshness and release-candidate ownership, not proving that
  the data plane has literally never served a request.
- **Public-release verdict:** the next historical verdict owner is
  `chats/icg/jul23_11am_8_rc_verdict_launch_anchor.md`, gated by a discharged
  §1 receipt. Do not revive the abandoned `jul16_10am_8` owner.
- **Closeout debt moved forward rather than clearing.** The five 2026-07
  closeouts exist (`chatting/jul20_fjcloud_console_closeout.md`,
  `jul20_9pm_console_prep_closeout.md`, `jul21_10pm_fjcloud_reliability_closeout.md`,
  `jul22_engine_exposure_tls_closeout.md`, `jul23_11am_launch_security_autoheal_closeout.md`)
  plus `jul25_pm_correctness_refugee_migration_polish_closeout.md`; do not
  re-author them. **Re-measured 2026-07-28:** `jul26_pm_11` and `jul27_pm_0`
  since closed (`chatting/jul26_pm_11_console_customer_quality_closeout.md`,
  `chatting/jul27_pm_release_confidence_closeout.md`); `jul26_12am_0` and
  `jul26_pm_7` still have merged lanes and no `chatting/*_closeout.md`. Same
  cause every time: the reconcile lane runs last, so a budget stop drops it.
  See the registry-debt row in `## Planned` for the full open/closed count.
- **`jul23_11am` successors:** its administrative closeout exists and E1
  shipped. The surviving work is the gated RC owner, TLS live flip/removal,
  replacement timeline UI recovery, and enabled autorepair proof. These are
  re-homed in
  `chats/icg/jul26_12am_0_local_migration_and_scale_closure_orchestration.md`;
  do not dispatch the stale monolithic descriptions below.
- **Algolia migration:** F10 catalog finalization and its consumer surface are
  merged. A3C's correctness receipt and the clean L1K SES recovery are merged;
  the stopped A3 and L1K workers must not be merged. F10C privacy scrub remains
  real but unmerged. Recover it only through
  `chats/icg/jul26_am_10_f10c_clean_salvage.md`, using F10C-only intermediate
  `25453f81e`; combined worker `55eac` is stopped and must not be lifecycle
  merged because it also imports superseded L1K ancestry.
- **Migration feature status:** backend and customer frontend are connected locally,
  migration operations are OpenAPI-registered, replica-copy behavior is honest, and
  real-Algolia create/replace/cancel/idempotency/resume-refusal correctness is
  certified. Staging/prod remain default-closed. **The local closure tail is now
  CLOSED** — terminal outcome UI, multi-node refusal proof, F10C erasure, and exact
  privacy-erasure acceptance all merged 2026-07-26/27. Next local chunk: the
  provider-neutral extraction (M1E in flapjack → M1F here), `jul26_pm_7` Wave 2.
- **Source migration expansion:** Meilisearch and Typesense are now pre-launch
  planned sources alongside Algolia. M0 contract/KAT and architecture/security
  lanes run before any product refactor; one shared job/erasure/wire/console
  lifecycle must remain canonical.
- **`FJCLOUD_ALGOLIA_MIGRATION_ENABLED` is no longer inert.** The 2026-07-16
  header below — "read by **no handler**", enforcement by hardcoded constant —
  was true then and is false now. F11 wired it: four handlers gate on
  `state.algolia_migration_enabled` (`routes/migration.rs:169`,
  `routes/migration/eligibility.rs:108`, `routes/migration/jobs.rs:168,482`),
  default still `false` (`config.rs:101-104`). Fail-closed by default and
  locally activatable, but via the flag, not a constant.
- **Merge-step stranding has eased; the five remaining unmerged fjcloud branches
  are written dispositions, not lost merges.** Re-verified 2026-07-27:
  `jul26_12am_2`, `jul25_am_3`, `jul25_am_2`, `jul24_pm_6`, and `jul13_12pm_3`
  are each superseded, evidence-only, or revert-hazardous and must be PRUNED,
  not merged. Per-branch proof is in the "Lane merges are being lost" row.

**Last updated:** 2026-07-25 — reconciliation against `origin/main` after the `jul24_pm` Wave-1 migration/ingest/reliability batch and the `jul25_am` recovery lanes. **This revision supersedes the 2026-07-24 header below wherever they conflict.** Merged and code-verified on `main`: **W1** Algolia migration surface activation (`146a69f33`), **W2** local real-pipeline harness (`7756f157b`), **W3** billing/pricing exact-cent correctness (`9b380f04f`), **A2** migration console UX (`a8f806224`), **E1** engine transport switch (`13015c14f`), **N1** closeout/deregister sweep (`7e78a4b8d`), and `jul25_am_1` node-secret lookup probe (`aa72f95fa`). **C1** aggregation-correctness KATs is in review (not yet merged).

**Correction to the 2026-07-24 engine-exposure narrative (item 2 below):** E1 DID ship the control-plane→engine transport switch — `engine_base_url()` / `engine_data_plane_tls_enabled()` in `services/provisioning/auto_provision.rs`, the design doc `docs/design/engine_data_plane_transport.md`, and the `FJCLOUD_ENGINE_DATA_PLANE_TLS_ENABLED` env var (registered in `docs/env-vars.md`) — **inert by default**. The prior claims that "the endpoint switch L10 never dispatched" and "`docs/design/engine_data_plane_transport.md` is absent" are **superseded and false as written**. The engine-exposure P0 remains OPEN because the switch defaults off (customers still get cleartext `http://…:7700` in effect), but the residual gap is now only the **parked staging-TLS-fleet proof that flips the switch**, not authoring the switch itself.

**Stranded-lane dispositions (supervisor recovery graph — do NOT hand-merge these branches):** the completed **VM-autorepair timeline UI** (`batman/jul23_11am_11`, +22, 6/6, conflict-free against `main` — its L3 backend already merged) is the one clean merge-pending candidate; execute it through `batman lifecycle`, not a raw git merge. **F10C** cloud privacy-scrub (`origin/batman/jul24_am_2`, +15) and **L1K** SES cause-preservation (`origin/batman/jul25_am_2`, +10) each carry real deliverables but never reached a green shared `local-ci.sh --fast`, and are routed into `chats/icg/jul25_am_3_fjcloud_closeout_gate_recovery.md` for merge-commit integration after a gate-repair pass. **A3** correctness proofs (`batman/jul24_pm_a3…`, +26, local-only) pins a detached Flapjack SHA (`b2defce…`, not public merge evidence) and is superseded by A3C `chats/icg/jul25_am_4_a3_migration_correctness_continuation.md` (gated on the Flapjack async-replace lane merging). The `jul24_pm_6`/`jul24_pm_7` staging-AWS diagnosis lanes are diagnosis-only and superseded by the merged `jul25_am_1` probe plus the SES root-cause fix carried in L1K. Prune-only: `batman/jul24_am_8` (empty) and `batman/jul23_pm_4` (annotation-only). Prior 2026-07-24 revision follows verbatim below.

**Last updated:** 2026-07-24 — reconciliation against `origin/main` merge state (not lane status tokens) after the `jul23_11am` launch/security/autoheal batch (L1–L12), the `jul23_pm` TLS/Caddy + F10-catalog continuations, and the `jul24_am` metering-receipt lanes. **Headline: the batch delivered real Wave-1 code but never terminally closed, and its two top objectives are UNMET on `main`.** (1) **§1 launch-unblock is NOT achieved** — the staging metering rollup was repaired at the code level (fail-honest freshness guard `scripts/probe_usage_rollup_freshness.sh` + local-CI gate, the `UsageDailyRollupSuccess` producer metric, and env-aware provisioning fixes, all merged) and proven fresh against real staging exactly once *by hand*, but the fixed-code staging redeploy FAILED, the live rehearsal never ran (frozen-source SHA tangle), the staleness alarm is unmerged, no `S1_BLOCKER=discharged` receipt exists, no §1 bundle is green, and the newest live-state still reads `ACTION_REQUIRED`. Read the §1 P0 row. (2) **Customers are still handed cleartext `http://…:7700` endpoints** — per-VM Caddy TLS shipped at the VM layer (`:443`, ACME `:80`, `CaddyRuntime`, networking rules) but the endpoint switch L10 never dispatched, so `services/provisioning.rs:288` and `services/provisioning/auto_provision.rs:284,296` still build `http://…:7700` and `docs/design/engine_data_plane_transport.md` is absent. Read the engine-exposure P0. (3) **Dead-VM autorepair L3 SHIPPED detection-only + disabled** (`services/vm_autorepair.rs`, `infra/migrations/064_vm_lifecycle_events.sql`, `GET /admin/vms/:id/lifecycle-events`, kill-switch `FJCLOUD_VM_AUTOREPAIR_ENABLED=false`, merged) — but its consuming admin **timeline UI (L11) is built 6/6 on `batman/jul23_11am_11` and NOT merged** (+22 ahead of `main`), so that feature is backend-only on the trunk; see the new "VM autorepair timeline UI built-but-unmerged" Planned row. (4) **F10 Algolia catalog-finalize MERGED** (`9c45d0de6`; `scripts/algolia_import_catalog_live_probe.sh` now on `main`), clearing the four-orchestration serialization point — but F11 activation still gates every customer-facing Algolia surface. (5) **The batch never closed:** L7/L8/L9 (§1 rerun, RC verdict, closeout+ROADMAP-reconcile) never dispatched, so this batch joins the closeout-debt row, the orchestration stays registered active, and no new `LAUNCH.md ## STATUS` verdict exists since 2026-07-22. Merge forensics: no recoverable unmerged product code on the stopped metering/TLS/algolia lanes (all merged or superseded); prune-only branches `batman/jul24_am_8` (empty abort) and `batman/jul23_pm_4` (annotation-only). **Prior revision (2026-07-23) follows verbatim.** verification sweep after the `jul21_10pm` reliability batch, the `jul16_10am` prod-truth lanes L6/L7, the `jul21_pm` Algolia F9 continuation, the `jul22_5pm` engine-exposure Wave 1, and the `jul20_9pm_4b` screen-spec continuation. **Six changes land with this revision, all reconciled against `origin/main` merge commits rather than lane status tokens.** (1) **The shared-VM teardown P1 SHIPPED** (merge `29a8649b6`) — the row below said "never dispatched"; that was stale by a day. (2) **The launch-matrix §1 P0's stated root causes are BOTH discharged and replaced by a third, different one**: `git: command not found` and the ALB 504 no longer appear; the 2026-07-22 rerun is red on `rollup_stale` — staging `usage_daily` has **0 rollups within 48h**, i.e. the staging aggregation job is not producing data. Read that row before touching the §1 runner. (3) **The live-state fleet/data-plane probe SHIPPED** (merge `84096fe8a`) and reports honestly; its row is narrowed, not closed, because the probe's first real run returns `ACTION_REQUIRED / environment_attribution_ambiguous`. (4) **The public-transparency N+1 + no-cache hardening SHIPPED** (merge `959d1648d6`), with one unrun post-merge staging verification stage. (5) **Algolia F9 merged** (`20b8d6bde`); **F10 is in flight and has been the single serialization point for four registered fjcloud orchestrations since 2026-07-22.** (6) **The three standalone ops screen specs SHIPPED** (`4838a70f3`), unblocking console R5. **Process caveat, unchanged and now compounding:** none of the `jul20_4pm`, `jul20_9pm`, `jul21_10pm`, or `jul22_5pm` batches has a closeout artifact; the `jul21_10pm` closeout continuation (`chats/icg/jul23_am_4_reliability_batch_closeout_continuation.md`) is authored and gated on F10. Prior revision context follows verbatim. **2026-07-21** — verification sweep after the `jul20_4pm` operator-visibility batch and the `jul20_9pm` console-prep batch. **Five changes land with this revision.** (1) **A new top P0 row: public engine port + unauthenticated engine dashboard on prod AND staging**, live-verified 2026-07-21 and unremediated; its remediation lane `chats/icg/jul20_4pm_9_engine_exposure_remediation.md` was authored but **never dispatched**. Read that row before touching any 7700 firewall rule — it and the prod-data-plane P0 below describe the *same* Terraform rule and give **opposite** instructions, and the row explains the root cause that dissolves the conflict. (2) **The prod-data-plane P0's "Fault 2" is no longer supported by current evidence** — see the correction inside that row. (3) **Eight merged, working features from the `jul20_4pm` batch were invisible in this ledger** because that batch never ran its required ROADMAP reconciliation; they are now folded into the Platform Ledger rows above with merge commits and their open gates named. (4) The **Algolia program row's engine gate has flipped green** and lane F9 is in flight — see that row. (5) The **mirror-leak P0 is downgraded** to its residual clause; issuing-layer deactivation is done and verified. **Process caveat:** the `jul20_4pm` batch is **not closed** — `chatting/jul20_fjcloud_console_closeout.md` does not exist, and lanes FJ-9/FJ-10 (`jul20_4pm_10`, `jul20_4pm_11`) are authored but never dispatched and still need disposition stamps. Prior revision context follows verbatim. **2026-07-20** — the **Algolia migration replacement program** row was reconciled against `origin/main` merge commits. The 2026-07-16 snapshot below had gone materially stale in a way that was actively causing wrong dispatch reasoning: it listed **seven already-merged lanes** (F5R4, F5P1, F5P2, F5V, F6R2, F8, and the F5 chain's completion) as "not started", reported a merge freeze as **HELD** that the operator **lifted on 2026-07-16**, and described integration-branch pre-push failures that are now all cleared on a branch that no longer exists. Read that row, not this header, for current program state. Prior revision context follows verbatim. **2026-07-16** (reconciled from `plan/jul14-algolia-amendments` + live read-only AWS probes; supersedes the 2026-07-14 revision, which carried two claims this pass proved false — see the P0 row and the Algolia row below). **Two corrections land with this revision.** (1) The 2026-07-14 text said Wave 3 of the jul13_12pm batch was "authored, NOT yet dispatched." It *was* dispatched: worker `0b5da` ran 2026-07-15, terminated `budget_exceeded` at 2/3 stages ($161.99, 7h11m), merged its partial work, and **never produced the RC verdict or the closeout artifact** — `chatting/jul13_iam_s1_rc_launch_closeout.json` does not exist on any branch, so the jul13_12pm batch is NOT closed. (2) The 2026-07-14 text said the unsafe migration routes are "removed fail-closed behind the typed `FJCLOUD_ALGOLIA_MIGRATION_ENABLED` guard defaulting false." The flag is **inert**: it is parsed (`infra/api/src/config.rs:100-104`), stored (`infra/api/src/state.rs:189`), assigned (`infra/api/src/main.rs:255`), and read by **no handler**. The actual fail-closed behaviour is a hardcoded constant at `infra/api/src/routes/migration.rs:75-83`, which returns `unavailable` unconditionally. Flipping the flag today changes nothing; F11 is the lane that will wire it (see the Algolia row). Historical note retained below —

The jul12_pm RC/LAUNCH anchor (`chats/icg/jul12_pm_7b_rc_verdict_launch_anchor.md`) completed 2026-07-13: the paid-beta RC at `docs/runbooks/evidence/invite-ready-rc/20260713T145624Z_jul12_pm_rc_verdict_launch_anchor/` returned `NOT-READY-real-defects` with exactly one real defect — `staging_billing_rehearsal=invoice_email_ses_not_ready` (§2) — and wrote the newest `LAUNCH.md ## STATUS` entry (blocker stub: `chats/icg/stubs/20260713_jul12_pm_rc_invoice_email_ses_not_ready.md`). The jul13_12pm batch then fixed that failure at its root cause: Wave 1 landed and applied the scoped staging CloudWatch Logs read policy with live on-role proof (`docs/runbooks/evidence/ses-iam-read/20260713T202728Z_staging_apply/`, all four API probes `ok`), and Wave 2 landed the canonical placeholder-free §1 six-row in-VPC runner plus the §1-manifest-aware RC classifier (`scripts/launch/run_ses_coverage_a1_in_vpc.sh`, `scripts/lib/ses_coverage_a1_integrity.py`, `scripts/launch/invoke_rc_with_env.sh --section1-manifest`). Wave 3 — `chats/icg/jul13_12pm_3_s1_rc_launch_verdict.md`, the frozen-SHA §1 rerun + fresh RC verdict + LAUNCH status anchor — **ran on 2026-07-15 and budget-stopped at 2/3 stages without issuing a verdict** (correction (1) above; the 2026-07-14 revision's "authored but NOT yet dispatched" is withdrawn). No RC newer than 2026-07-13 exists, and the §1 bundle Wave 3 did produce came back red on two new harness defects. Separately, the Algolia migration reliability program (`chats/icg/jul13_9pm_0_algolia_migration_reliability_orchestration.md`) began: Wave 0 lane F1 merged the fail-closed migration guard (see the program row below, including correction (2) on what actually enforces it); eleven fjcloud lanes have since merged onto the integration branch and 24 remain unstarted. Launch remains governed by `LAUNCH.md ## STATUS`; do not announce until a fresh RC reaches an allowed launch shape and writes its required status entry — and, per the P0 row below, no such RC may treat staging as proof of the prod data plane.
**Purpose:** `ROADMAP.md` owns the active product/work ledger and the current backlog.
Historical implementation detail lives in [`implemented/`](implemented/).
**Launch gate:** [LAUNCH.md](LAUNCH.md) owns the v1 launch sentence, blocker interpretation,
and current launch verdict. On launch-readiness questions, LAUNCH.md wins.

## Active

A real prod environment exists and its signup, pricing, billing, policy, and search data-plane lifecycle have been proven together once with exact search and zero-residue cleanup. The open launch work is proof freshness and a current RC verdict; staging evidence still may not substitute for the recurring prod search proof.

Active direction: make prod actually serve search and prove it; restore launch-matrix §1 to green; issue a fresh RC verdict from that proof. Then keep the customer release path honest and close only concrete gaps that block signup, email verification, search provisioning/ingest/query, usage, Stripe payment, or support email.

Current launch interpretation is intentionally compact here; use these owners for detail:

- [LAUNCH.md](LAUNCH.md) — launch sentence, blocker interpretation, and current verdict.
- [docs/launch_verification_matrix.md](docs/launch_verification_matrix.md) — launch-sentence
  verb-to-test traceability.
- [docs/runbooks/staging-evidence.md](docs/runbooks/staging-evidence.md) — staged evidence
  interpretation and artifact pointers.
- [docs/runbooks/paid_beta_rc_signoff.md](docs/runbooks/paid_beta_rc_signoff.md) — RC
  taxonomy and readiness-artifact contract.
- [docs/launch/beta_launch_remaining_work.md](docs/launch/beta_launch_remaining_work.md) —
  plain-language overview of what's left before paying beta (orientation only; LAUNCH.md +
  the launch matrix remain the authority).
- [docs/runbooks/live_environment_ops_approach.md](docs/runbooks/live_environment_ops_approach.md) —
  how to do staging/prod work without the repeated stall-and-strand headaches.
- [`implemented/`](implemented/) — closed waves, historical merge
  detail, and done-only prose moved out of this live ledger.

### Platform Ledger

| Area                                                                       | Status         | Current owner note                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------------------------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Core cloud API surface (auth, index lifecycle, billing, usage, deployment) | Built          | Customer-facing API endpoints and quota-aware index creation are implemented.                                                                                                                                                                                                                                                                                                                                                                                                     |
| Admin API and operations tooling                                           | Built          | Fleet/admin management, customer audit/snapshot drill-down, and broadcast tooling are implemented; status publication remains tracked below. **Extended by the `jul20_4pm` operator-visibility batch (merged 2026-07-20/21, reconciled into this ledger 2026-07-21):** admin index inventory + audit metadata + invoice Stripe drill-in (`routes/admin/indexes.rs:85`, `routes/admin/mod.rs:68`, merge `e1da3832b`); revenue/MRR billing-summary endpoint retiring the N+1 billing loader (`routes/admin/mod.rs:107`, hand-calculated-dollar test `billing_endpoints_test.rs:2446`, merge `ac70317ac`); fleet all-servers view owning the canonical `vm_health_rollup` classifier, which conservatively returns `Unknown` — never `Healthy` — for an empty deployment set (`services/vm_health_rollup.rs`, merge `672a316cc`); host-level per-VM telemetry collected from `/proc` into `vm_host_metrics` (`infra/migrations/061_vm_host_metrics.sql`, `infra/metering-agent/src/host_metrics.rs`, merge `b07394cd8`); and real host-metric columns on the fleet page asserted by exact normalized cell text (`admin-fleet.test.ts:318-320`). **Extended 2026-07-22 (`jul21_am_1`, merge `29a8649b6`):** one idempotent VM-teardown seam (`routes/admin/vms.rs::teardown_retired_vm_resources:368`, called from the decommission path at `:507`) that owns the Cloudflare A record and SSM node key alongside the inventory row, plus a **read-only** orphan detector at `GET /admin/vms/orphans` (`routes/admin/mod.rs:110`, `services/vm_orphan_reconcile.rs`, handler `routes/admin/vm_orphans.rs`). The detector is detect-only by design — it never deletes — and instance-only orphan enumeration is explicitly still deferred; see the teardown row under Planned. |
| Customer web portal core workflows                                         | Built | Onboarding, indexes, logs, billing, account settings, resend-verification, and support affordances are implemented. **Added 2026-07-20 (`jul20_4pm_6`, merge `072cab7a4`):** the per-index Infrastructure tab, owner of the `to_public_topology` privacy filter, which structurally projects `VmInventory` down to `{region, provider, utilization}` — hostname, `flapjack_url`, VM UUID and absolute capacity are dropped rather than merely hidden, and utilization is coarsened to a green/yellow/red bucket. Leak-proofed at two levels by sentinel tests (`services/public_topology.rs::to_public_topology_never_leaks_host_or_absolute_capacity`; `tests/integration/index_infrastructure_test.rs:328,374,462,482,513`). **Closed 2026-07-25:** Stage 3 completed the unmocked browser privacy proof before commit; implementation commit `03ef95443` finalized that tested worktree. The recorded Stage 3 passes were `cd web && npx playwright test tests/e2e-ui/full/public-infrastructure.spec.ts --reporter=line` (2/2 passed) and `cd web && npx playwright test tests/e2e-ui/full/index-detail-infrastructure.spec.ts --reporter=line` (2/2 passed), proving the customer UI/privacy path plus the public-page k-anonymity and closed-shape privacy checks against the real local app/database. **Extended 2026-07-27 by the `jul26_pm_11` console customer-quality batch (merges `9fd7e2ab2`, `47d163a4f`, `504f3af0f`, `29aeeafe9`, `df44f91c9`):** customer console actions now carry browser *end-effect* proofs rather than render-only assertions — replica create/delete persistence across refresh (new `web/tests/e2e-ui/full/console_replica_lifecycle.spec.ts`), index-detail delete redirect plus absence from the list (`indexes.spec.ts`), onboarding create-index failure→retry→recovered-credentials (`onboarding.spec.ts`), non-overlapping daily-usage chart labels at 768px and 1280px (new `console_daily_usage_chart.spec.ts`), and document upload/add-record read-after-write reuse. `docs/audits/ui_test_coverage_p0_ledger.md` was reconciled the same batch: of 23 examined rows, 6 were REAL, 13 STALE/retired (including the non-existent `createPreviewKey` surface), and 4 ALREADY-COVERED. **The batch's tag-count gate is met at 19 tags across 18 declarations, but three execution residuals survive — see the "Console browser-coverage residuals" row before citing this coverage as complete.** |
| Public beta landing and policy surface                                     | Built — one unrun verification | Public landing, pricing, terms, privacy, and DPA surfaces are implemented. **Added 2026-07-21 (`jul20_4pm_7`, merge `57ddcae80`):** the public `/infrastructure` transparency page, which reuses `to_public_topology` per VM and applies k-anonymity — `worst_region_utilization` returns `None` when a region has fewer than 2 VMs or fewer than 2 observed utilization buckets (`routes/public_infrastructure.rs:205-226`), so single-VM regions and partially observed regions cannot have an individual VM's load inferred. **Hardening open (concrete, not theoretical):** the route is registered on the **unauthenticated** public router beside `/health` with **no rate limiter** (`router/route_assembly.rs:107-111`; limiter middleware exists only for the auth/tenant/admin routers at `:32,72,125`), and the handler is **N+1 on the database** with no caching — one `tenant_repo.list_by_vm` per VM plus one `deployment_repo.find_by_id` per deployment (`routes/public_infrastructure.rs:66-92`). `PUBLIC_TOPOLOGY_MIN_REFRESH_INTERVAL_SECS = 60` exists but is advisory on the customer endpoint only and is neither enforced nor returned here. Unauthenticated + uncached + N+1 is a cost/DoS amplification vector; recommended fix is a limiter plus a ≥60s response cache and a batched tenants/deployments query. **CLOSED 2026-07-22 (`jul21_10pm_1`, merge `959d1648d6`):** the handler now serves from a TTL cache (`state.rs:275` `public_infrastructure_cache`, read at `routes/public_infrastructure.rs:69`, populated at `:81`, TTL contract tests at `state.rs:307-329`) and the N+1 is gone — one batched `tenant_repo.list_by_vms` (`:119`) plus one batched `deployment_repo.find_by_ids` (`:134`) replace the per-VM/per-deployment loops. **The one thing NOT done:** the lane finished 4/5 stages, so Stage 5 — the post-merge deployed-staging verification of the cached route against a real staging SHA — was never run (`chats/icg/_followups.md`, `jul21_10pm_1::stage_05`). The code and its unit/contract tests are green on `main`; the deployed surface is unverified. |
| Customer billing portal handoff                                            | Built          | Runtime publishable-key fetch, payment-method setup/default management, and backend/frontend tests are implemented.                                                                                                                                                                                                                                                                                                                                                               |
| Status page and outage communication tooling                               | Built          | Env-backed `/status` readback is live; `scripts/set_status.sh` owns publication of `SERVICE_STATUS`, `SERVICE_STATUS_UPDATED`, and `SERVICE_STATUS_MESSAGE`; `scripts/deploy_status.sh` owns deploy readback.                                                                                                                                                                                                                                                                     |
| Signup abuse and pricing trust guards                                      | Built          | Disposable-domain blocking, verify-before-Stripe customer creation, auth rate limits, and pricing drift checks are implemented.                                                                                                                                                                                                                                                                                                                                                   |
| Stripe lifecycle and webhook regression coverage                           | Built          | Invoice, payment-method, webhook signature, idempotency, and matrix coverage are implemented for the current metered v1 model.                                                                                                                                                                                                                                                                                                                                                    |
| Support inbox and synthetic canary automation                              | Built          | Current clean-env bundle: `docs/runbooks/evidence/ses-inbox-canary-clean-env/20260711T014655Z/SUMMARY.md`. Live staging proof is green on canonical `stuart-cli` for account `213880904778`: SES bounce/complaint RAN and exercised suppression/audit side effects through `scripts/probe_ses_bounce_complaint_e2e.sh::main`, inbound S3 round-trip is green, and the Stage 3 customer-loop canary invoke/readback is green with `errors_24h == 0` and `last_invocation` success. |
| Local dev mode and strict local signoff                                    | Built          | Local service replacements, commerce proof, cold-tier proof, HA proof, and browser preflight contracts are implemented.                                                                                                                                                                                                                                                                                                                                                           |
| Provisioning, placement, replicas, discovery, and Algolia migration        | **Partial — prod proven once; migration locally activatable, default closed** | Local/shared VM placement, replica routing, and index discovery are implemented and, **as of 2026-07-22, prod has served a real search** — the prod E2E bundle `docs/runbooks/evidence/prod-data-plane/20260723T000045Z_prod_e2e/` proves signup → provision → ingest → exact search → zero-residue cleanup, and that lifecycle now reruns on every non-`--staging-only` RC (see the P0 row below, which is narrowed but not closed). Historical engine-health gap is closed at the provisioning seam, not the replacement seam: `infra/api/src/services/provisioning/auto_provision.rs` and `infra/api/src/services/provisioning.rs` now ask the VM whether its engine answers before recording success, while replacement remains detect-only. **Partial mitigation shipped 2026-07-24 (`jul23_11am_3` L3, merged):** a detection-only dead-VM autorepair reconciler (`infra/api/src/services/vm_autorepair.rs`), a durable `vm_lifecycle_events` audit table (`infra/migrations/064_vm_lifecycle_events.sql`), an admin read endpoint (`GET /admin/vms/:id/lifecycle-events`), and a validation script (`scripts/validate_vm_autorepair_detection.sh`) with 5 detection-proof evidence dirs — all disabled by default (`FJCLOUD_VM_AUTOREPAIR_ENABLED=false`, `docs/env-vars.md:20-29`). It proves *detection* against a really-terminated instance; it does NOT yet replace, and it is scheduled by nothing. **Correction 2026-07-24:** provisioning DOES now have an engine-health gate — `c85fc4469` "Gate provisioning on engine health" wired `await_engine_health` → `mark_provisioning_failed` unconditionally into both paths (`services/provisioning.rs:393-409`, `provisioning/auto_provision.rs::shared_vm_health_deadline_exhausted`), so a VM whose engine does not answer is now rolled back and marked failed rather than recorded healthy — the specific hole that let 28 dead VMs certify green is closed at the provisioning seam (the reconciler/replacement half remains detect-only). The enabled end-to-end replacement proof (L12 `jul23_11am_12`) and the admin timeline UI that consumes these events (L11 `jul23_11am_11`, built 6/6 but unmerged) both never landed — see the dedicated Planned row. **F11 local activation wiring is now landed on this branch:** Algolia migration availability is computed from the config flag plus the declared route/engine capability contract, `POST /migration/algolia/list-indexes` now fails closed behind that same gate, and `/console/migrate` can render a minimal `migration-available` state when availability flips locally. **Unchanged served default:** staging and production still come up CLOSED because `FJCLOUD_ALGOLIA_MIGRATION_ENABLED` defaults `false`; this does not claim the full customer import flow is end-to-end ready. **The backend is still ahead of the customer surface here:** F9 shipped the whole cloud→engine dispatch/cancel/reconciliation service (`services/algolia_import/{admission,cancel,reconciliation,observation,status}.rs`, merge `20b8d6bde`) against a flapjack engine that mounts all three migration routes, while later Algolia waves still own the real customer migration workflow. **Updated 2026-07-25 (jul24_pm migration batch — backend/frontend/connected status):** the customer **frontend** now exists and is wired to the backend gate. W1 (`146a69f33`) replaced the hardcoded availability with a real `compute_availability()` (resume forced off) and fail-closed `POST /migration/algolia/list-indexes` behind it; A2 (`a8f806224`) shipped the `/console/migrate` flow end-to-end — `MigrationCreateFlow` mounted behind the `migration-available` gate, the `[jobId]` job-detail route with cancel wiring and the resume-hidden invariant, and a mocked Playwright spec. **Backend and frontend are therefore CONNECTED locally** (A2 Stage 1 re-verifies the W1 contract on drift). E1 (`13015c14f`) added the inert control-plane→engine TLS transport switch. **Remaining connected-but-not-certified gap:** end-to-end migration *correctness* against a real Algolia source (create / overwrite / idempotency / cancel / resume-refusal parity) is **A3, NOT merged** — it stopped on a detached engine pin and is superseded by A3C (`chats/icg/jul25_am_4_a3_migration_correctness_continuation.md`, gated on the flapjack async-replace lane). So the feature is *wired and locally activatable*, not *end-to-end certified*; staging/prod stay CLOSED by default (`FJCLOUD_ALGOLIA_MIGRATION_ENABLED=false`). |
| AI Search / RAG, recommendations, personalization, dictionaries            | Not current v1 | Keep outside the paid-beta launch path unless the strategic priority owner promotes it.                                                                                                                                                                                                                                                                                                                                                                                           |
| Engine geo-search and configurable ranking formulas                        | Upstream       | Engine roadmap items, not fjcloud launch blockers.                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Live credential validation and deploy-path visibility                      | Built          | Distinct prod/staging envs, split deploy jobs, webhook split, `/version`, sync manifest, and deploy provenance are implemented.                                                                                                                                                                                                                                                                                                                                                   |
| Prod operations evidence                                                   | Mixed          | Prod canary, RDS restore, alarm coverage, secret distinctness, and VM lifecycle proof exist; listed reruns below keep current-main evidence fresh. **Extended 2026-07-22 (`jul21_10pm_2`, merge `84096fe8a`):** `scripts/probe_fleet_dataplane.sh` plus a `fleet_dataplane` row in `scripts/probe_live_state.sh`, registered as a hermetic contract gate in `local-ci --fast`. It is fail-honest by construction — un-probed fleets and indeterminate identity render `ACTION_REQUIRED`, never `SKIP`. Its first real run (`docs/live-state/20260723T075504Z/`) does exactly that: `ACTION_REQUIRED`, `reason=environment_attribution_ambiguous`.                                                                                                                                                                                                                                                                                                                                |

## Planned

Priority-ordered. Top rows are active or immediately next. **All priorities are Flapjack-focused.**

### Source migration expansion

| ID | Work item | Current plan | Evidence / owner |
| --- | --- | --- | --- |
| MIG-SRC-CORE | Provider-neutral migration lifecycle and security | Pre-launch. Generalize the existing Algolia-shaped job/schema/wire/console surface only after M0 proves the target model. Keep one job/status/cancel/ACK/erasure/publication path, preserve Algolia route compatibility, separate source vendor from destination cloud provider, and settle self-hosted endpoint SSRF/TLS/redirect/DNS-rebinding policy first. | M0C: `chats/icg/jul26_am_11_source_migration_architecture_security.md` |
| MIG-MEILI | Meilisearch migration | Pre-launch, gated on MIG-SRC-CORE plus a pinned local exact-value source KAT covering records, primary key, settings, synonyms, tasks/quiescence, permissions, drift refusal, cleanup, and warnings. | Flapjack M0A: `chats/icg/jul26_am_12_meilisearch_migration_contract_kat.md` |
| MIG-TYPESENSE | Typesense migration | Pre-launch, gated on MIG-SRC-CORE plus a pinned local exact-value source KAT covering typed schema, IDs, exports, aliases, synonyms/curations, permissions, drift refusal, cleanup, and warnings. | Flapjack M0B: `chats/icg/jul26_am_13_typesense_migration_contract_kat.md` |
| A11Y-W2 | Signup-to-console browser accessibility closure | Wave 2 is CLOSED by capability. (a) **Route-surface expansion is CLOSED by jsdom audit**: `docs/audits/accessibility/20260728T143636Z_route_surface_expansion/SUMMARY.md` re-derived the manifest denominator as 40 `+page.svelte` routes, 40 scanned, 0 `unrenderable-in-jsdom`, and 0 unaccounted; `web/src/tests/a11y_route_manifest.json` remains the catalog and `web/src/tests/a11y.ts` remains the axe owner. (b) **Browser rule closure is CLOSED** under `chats/icg/jul28_6am_6_browser_accessibility_closure.md`: `docs/audits/accessibility/20260728T234739Z_browser_rule_closure/SUMMARY.md` records the real-browser catalog at 40 routes, `color-contrast`, `landmark-one-main`, and `region` enabled, inherited red baseline `routes_scanned=40 violations=216`, inherited green proof `routes_scanned=40 violations=0`, Stage 3 post-revert green proof `routes_scanned=40 violations=0`, and a deliberate `--color-flapjack-rose: #fff8ea` regression failing with `routes_scanned=40 violations=22`. Exit: `cd web && npx playwright test tests/e2e-ui/full/accessibility.spec.ts --reporter=line` returns `3 passed` with zero violations across that exact 40-route, three-rule browser surface. | Route audit: `docs/audits/accessibility/20260728T143636Z_route_surface_expansion/SUMMARY.md`; browser audit: `docs/audits/accessibility/20260728T234739Z_browser_rule_closure/SUMMARY.md`; seed audit: `docs/audits/accessibility/20260727T163449Z_signup_to_console_structural/SUMMARY.md` |

| Priority | Feature                                                   | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------- | --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **P0** | **No stage owns the merged-union gate, so `main` can sit red across a whole batch** | **Added 2026-07-27 at `jul27_pm_0` closeout, after running that gate for the first time and finding `main` already red.** `bash scripts/local-ci.sh --fast` on merged `main` returned `pass=30 fail=7`. Two of the seven — `launch-evidence-freshness-contract` and `source-pollution` — reproduce at `7b984cd60`, i.e. they were red on `main` BEFORE the closeout's own merge, having been introduced by `jul27_pm_6` and `jul27_pm_1` respectively. **Both lanes passed their own validation and both left `main` failing**, which is precisely the failure mode the orchestration's merged-union requirement exists to catch: "per-lane green does not imply union green … the batch is not closed until that union run passes on merged `main`." That requirement was written down and then never executed by any lane. **The mechanism is ownership, not tooling: per-lane validation runs in each lane's own worktree against a tree that does not yet contain its siblings, and no stage in any checklist owns the post-merge run.** Wave 1 is the demonstration — three lanes each edited `scripts/local-ci.sh`, the exact file whose union behavior nobody verified. This is P0 because it silently voids the project's only QA process: a red `main` that no one runs cannot fail for a real defect, which is the same class as the un-executed tests `jul27_pm_4` was built to find, one level up. Repair: give the union run a named owner — either a `[merge]`/`[union]` stage appended to every orchestration's last lane, or a standing post-merge hook that runs it and refuses batch close on non-zero — and record its verbatim output in the batch closeout. Exit: `bash scripts/local-ci.sh --fast` exits 0 on merged `main`, and each subsequent batch closeout in `chatting/` contains that command's verbatim output with a zero exit. |
| P1 | `local-ci --fast` is no longer fast — one gate now costs 21 minutes | **Added 2026-07-27 from the first successful run of `test-reachability-contract`.** `gate_test_reachability_contract` runs the probe AND loops `TEST_REACHABILITY_HERMETIC_TESTS`, executing all ~142 suites `jul27_pm_4` newly wired, sequentially, inside one gate. Measured: `bash scripts/local-ci.sh --gate test-reachability-contract` → `GATE_EXIT=0 ELAPSED_SECONDS=1260`, against a whole 37-gate `--fast` run that previously finished in roughly 14 minutes. **The cost was invisible until now because the gate had never once passed inside a CI run** — in the union run it exited at 31s on a registry error, before reaching the loop. Read the number honestly: the old 14 minutes was never the price of this suite, it was the price of the third of it that was wired, so this is a newly-revealed true cost, not a new expense. But it breaks the project's own fast-feedback rule ("run the smallest relevant test after every code change"), and a 35-minute pre-push gate will be skipped or backgrounded, which is how gates stop being run at all. **PARTIALLY REPAIRED 2026-07-27 — intra-gate concurrency landed and it is NOT enough; read the measured numbers before planning more.** The gate now runs the manifest at concurrency 8 instead of serially, keeping all 130 suites in `--fast` rather than demoting any to `--full`, because moving suites out of the routine lane would partially recreate the defect `jul27_pm_4` closed. Measured on merged `main`: `GATE_EXIT=0 ELAPSED_SECONDS=1037` and `130 hermetic suite(s) run at concurrency 8, 0 failed`, against the serial `ELAPSED_SECONDS=1260`. **That is 21min → 17min, a 1.2x improvement, not the 5x the worker count suggests.** Two reasons, both measured, neither fixable by raising the concurrency: **(a)** macOS ships bash 3.2, which has no `wait -n`, so the loop batches (launch N, wait for ALL N, repeat) and every batch costs its slowest member; **(b)** the cost is concentrated in a few very slow suites — sampling 47 of 130 serially, `algolia_import_catalog_live_probe_test.sh` took 195s and `apply_ses_log_read_policy_test.sh` 181s, i.e. two suites were 66% of that sample's 567s. **Concurrency also surfaced a classification defect worth its own attention:** three suites the reachability audit classified hermetic FAIL under concurrency and pass alone — `apply_ses_log_read_policy_test.sh`, `local_dev_down_test.sh`, `start_metering_test.sh`. They are hermetic in the audit's sense (no deployed environment, no live credentials) but not isolated from each other: they drive the shared local dev stack, its pidfiles, and shared fixture state. They now run in a serial tail registered in `scripts/tests/serial_only_tests.txt`, which is a scheduling list and not a skip list — every entry still runs on every invocation, the gate refuses an entry absent from the manifest, and the contract test asserts each entry is still manifest-resident. Remaining work, in the order that actually buys time: **(1)** make the slow suites fast — they are dominated by probe backoff sleeps, not real work, so shortening fixed sleeps is the single biggest lever; **(2)** give the three serial suites real isolation (unique compose project names and temp roots) so they rejoin the concurrent batch; **(3)** only then consider a bash-5 dependency for a refilling worker pool. Exit: `bash scripts/local-ci.sh --fast` completes within a stated wall-clock budget recorded in this row, no suite leaves the `--fast` lane, and `scripts/probe_test_reachability.sh` still reports `unaccounted=0`. |
| P1 | `jul27_pm_0` Wave 2's authored spec is stale — re-derive it before dispatching | **Added 2026-07-27 by measuring Wave 2's spec against the merged tree rather than trusting it.** `chats/icg/jul27_pm_0_release_confidence_orchestration.md` § Wave 2 item 1 tells the supervisor to fix five UX-polish items from `docs/audits/ux_polish_recommendations_2026_06_05.md`, on the reasoning that nothing has referenced that audit for seven weeks. **The inference is wrong: the audit is unreferenced because it was largely consumed, not because it was ignored.** Re-measured at `dd10a4231`, four of its five items are already shipped — password reveal exists as `web/src/lib/components/PasswordInput.svelte` and only two raw `type="password"` inputs remain repo-wide (both in `web/src/lib/components/migration/`), not the eleven across seven routes the spec names; the Google/GitHub buttons are live in `web/src/lib/components/OAuthButtons.svelte`, rendered from `signup/+page.svelte:125` and `login/+page.svelte:87`, and probe provider availability instead of dead-ending (`infra/api/src/routes/oauth.rs:117` still returns `oauth_not_implemented` when unconfigured, which is now the *handled* branch, not a broken button); account deletion is implemented at `web/src/routes/console/account/+page.server.ts:119` with password confirmation and explicit "does not cancel billing" copy, not a "email support to cancel" stub; and `dev@example.com` no longer appears in `web/src`. `web/src/lib/components/Tooltip.svelte` exists, so only the tooltip *placement* on Personalization / Recommendations / Profile Lookup is plausibly open. **The general failure is that the spec was authored from a dated audit without re-measuring, which is the same class of defect the batch was created to fix** — a stale instrument trusted over live state. **Items 2–5 were re-measured 2026-07-28 and two of the four are now retired.** **Item 4 (customer-documentation reachability) is CLOSED** — the 2026-07-27 spot-check that "nothing in `web/src` links to `docs.flapjack.foo`" was true when written and is false now: `jul27_pm_8` landed `READER_DOCS_URL` at `web/src/lib/format.ts:231` and renders it from `web/src/lib/components/SiteFooter.svelte:29`. **Item 5 (cross-browser expansion) rests on a FALSE premise and is struck** — it claims "Firefox and WebKit currently run only the `public` and `smoke` projects"; in fact `web/playwright.config.contract.ts` declares **eleven projects and every one is Chromium or a setup project**, and `web/src/tests/playwright-config-contract.test.ts:771-774` asserts `firefox:public`, `firefox:smoke`, `webkit:public` and `webkit:smoke` are all `undefined`. Cross-browser is not a partially-wired surface to extend; it is a deliberate 2026-05-02 removal locked by a contract test, and it stays governed by the existing P3 `Reintroduce Linux-Playwright Firefox/WebKit` row — reintroducing it means editing that contract test, which no lane may do incidentally. **Item 1 (UX-polish disposition) is also CLOSED, and the last surviving sub-item died on re-measurement too** — the 2026-07-27 revision left "only the tooltip *placement* on Personalization / Recommendations / Profile Lookup is plausibly open"; measured 2026-07-28, `web/src/routes/console/indexes/[name]/tabs/RecommendationsTab.svelte` imports `Tooltip` at `:6` and renders it from `:183` and `:205` over six derived tooltip strings, and `PersonalizationTab.svelte` imports it too. `docs/audits/ux_polish_recommendations_2026_06_05.md` is therefore **fully consumed** and should be retired with that measurement rather than turned into a lane; authoring one would have invented work that shipped weeks ago. Item 2 (browser accessibility) and item 3 (console coverage residuals) survive re-measurement and are the live Wave 2 content. Exit: each Wave 2 spec item is either re-verified live at dispatch time with a recorded command, or struck with the measurement that retired it; no Wave 2 lane is authored from an unre-measured 2026-06-05 row. **ITEM 3 RE-MEASURED 2026-07-28 — RETIRED. An earlier revision of this row claimed item 3 was real, that B21's proof was vacuous, and that `scripts/tests/p0_coverage_execution_receipt_test.sh` had a matcher hole. ALL THREE CLAIMS WERE WRONG AND ARE WITHDRAWN.** They came from fusing two different measurements: the older committed bundle `docs/runbooks/evidence/local-p0-coverage/20260728T122417Z/` (a first attempt run with `--reporter=line`, whose B21 log genuinely ends `1 skipped`), and a guard run that was actually reading a different, newer bundle. **Measured correctly:** `jul28_6am_1` produced a RETRY bundle `docs/runbooks/evidence/local-p0-coverage/20260728T151747Z/` with `--reporter=list`, whose B21 log carries `✓  2 [chromium] › tests/e2e-ui/full/auth-end-effects.spec.ts:66:2 › … @p0_coverage (1.9s)` and `2 passed`. **B21 executed and passed.** The guard is SOUND: it selects only the newest bundle (`max(bundles)`, `p0_coverage_execution_receipt_test.sh:300`), and running its own `raw_log_has_pass_line` matcher against both logs returns `False` for the skipped one and `True` for the passing one — exactly as designed. **And item 3's premise is retired because `jul28_6am_1` FIXED it:** it added `API_DEV_REQUIRE_LOCAL_EMAIL_DELIVERY=1` to `scripts/api-dev.sh`, which `unset`s `API_DEV_ALLOW_SKIP_EMAIL_VERIFICATION` and requires a loopback `MAILPIT_API_URL` so the verification mail stays in the local inbox, and `web/tests/fixtures/fixtures.ts` pulls the real token out of Mailpit. The email-verification success path IS provable locally, so the `SKIP_EMAIL_VERIFICATION` block in `web/playwright.config.contract.ts` is not the blocker item 3 named. **All five spec items are now dispositioned: 1, 3, 4 and 5 retired on measurement; 2 (browser accessibility) re-homed to `chats/icg/jul28_6am_6_browser_accessibility_closure.md`.** One small residual survives: the superseded `20260728T122417Z` bundle still records B21 as `outcome: executed_pass` against a log showing the test skipped. It is inert (the guard reads only the newest bundle) but it is a false evidence artifact; owner `chats/icg/jul28_1pm_1_p0_coverage_b2_b7_execution_continuation.md` corrects that receipt to match its log without deleting the bundle. **Standing lesson, which is why this withdrawal is recorded rather than erased: two measurements taken against different trees must never be narrated as one finding — the older bundle and the newer guard run disagreed because they were about different artifacts, not because anything was broken.** Exit for item 3: satisfied — no lane is owed for it. |
| P1 | Hermetic shell-test quarantine drawdown | **CLOSED 2026-07-28 at Stage 4 evidence base `e1510712`.** `scripts/tests/quarantined_tests.txt` remains the canonical roster and is now comment-only, preserving the `scripts/probe_test_reachability.sh::load_registry` contract instead of deleting the registry. Stage 1 re-derived the live repair denominator from that roster as three owned suites: `algolia_import_dispatch_live_probe_test.sh`, `catalog_lifecycle_service_window_live_probe_test.sh`, and `local_dev_up_test.sh`. Stage 2 repaired the Algolia dispatch fake-projection drift and catalog acceptance-oracle drift at their existing owner seams; Stage 3 repaired local-dev startup fixture/readiness drift and restored the suite to the manifest and serial tail. Stage 4 closure proof: `bash scripts/probe_test_reachability.sh` exits 0 and prints `corpus=230 reachable=229 allowlisted=1 quarantined=0 unaccounted=0`; `bash scripts/tests/probe_test_reachability_test.sh` prints `=== Results: 93 passed, 0 failed ===`; `scripts/lib/test_reachability_manifest.sh` contains each repaired suite exactly once. Exit: keep `bash scripts/probe_test_reachability.sh` exiting 0 with final summary `quarantined=0 unaccounted=0`; any future row in the roster must cite an existing owner `.md` path accepted by `reason_has_owner` and carry measured failure/runtime evidence. |
| P1 | `jul28_6am_2`'s dirty-alert preflight regressed `chaos_ha_failover_proof_test.sh`, and no gate runs it | **Added 2026-07-28 by the supervisor at the `jul28_6am_2` incomplete merge, from its own measurement rather than the lane's ledger.** `jul28_6am_2` (`d2636`) terminated `finished_incomplete` (`stop_reason=finished_incomplete_truncated_stages`, `completed_stages=[2,3,4]`) and was force-merged with `batman queue d2636 --merge-incomplete` at `e3cc6b1f5`, because its 17-suite drawdown and its `docs/audits/test-wiring/20260728T131813Z_quarantine_drawdown_i/SUMMARY.md` gate artifact are real and two downstream lanes gate on them. It left one **deferred `critical`** behind (`ha-dirty-alert-breaks-chaos-proof-suite`). **Re-measured independently, both ends:** at the merge base `ece3fbeeb`, `bash scripts/tests/chaos_ha_failover_proof_test.sh` exits 0 with `19 passed, 0 failed`; at the branch tip `762b655c5` it exits 1 with `16 passed, 3 failed`. The regression is this lane's, not pre-existing. **Cause:** Stage 4 replaced the cursor-based baseline-alert tolerance in `scripts/chaos/ha-failover-proof.sh` with a hard refusal (`ERROR: Dirty alert state for region ... clean up stale failover alerts before re-running`). **That instinct is correct and must not simply be reverted** — a failover proof run against alert state that already satisfies the proof proves nothing, which is the same anti-vacuity principle the reachability work exists to enforce. But three assertions in `scripts/tests/chaos_ha_failover_proof_test.sh` deliberately seed baseline alerts and now abort at the preflight instead of exercising the failover path: `should ignore baseline alerts and verify the lowest-lag replica`, `should report successful no-switchback verification`, and `PATH flapjack binary should ... allow mutating kill call to proceed`. **Why nothing caught it:** `chaos_ha_failover_proof_test.sh` is reached only through `scripts/tests/chaos_test.sh`, and neither file is in `TEST_REACHABILITY_HERMETIC_TESTS`, so `bash scripts/local-ci.sh --fast` never executes it — a suite that went red inside a batch whose entire subject was making suites execute. Decide which side is wrong before editing: either the fixtures must stop seeding a complete failover pair, or the preflight needs a narrow test-only seam that cannot be used to make a real proof vacuous. **Also carried from the same incomplete merge:** `staging_billing_rehearsal_test.sh` is green at 171s, 95% of the 180s fast budget (deferred `low`, `staging-rehearsal-fast-budget`); and Stage 1 never completed because its roster-only deletion was structurally impossible under its own charter — deleting the six rows made all six `unaccounted` and failed the reachability probe, while the stage forbade any execution-root edit, so Stages 2-4 did that work instead and the two Stage-4 `critical` ledger rows were fixed in `497b7a247` without being marked resolved. Exit: `bash scripts/tests/chaos_ha_failover_proof_test.sh` exits 0 with `19 passed, 0 failed`; the suite is registered in an execution root so `--fast` runs it and `scripts/probe_test_reachability.sh` still reports `unaccounted=0`; and the dirty-alert preflight is proved still fail-capable by a recorded red run against a seeded complete failover pair. |
| P1 | Registered orchestrations outlive their work — 2 of 8 fjcloud entries are open | **UPDATED 2026-07-28 after Stage 3 closeout publication.** The five closure candidates now have committed closeouts — `chatting/jul13_9pm_algolia_migration_reliability_closeout.md`, `chatting/jul16_prod_truth_launch_closeout.md`, `chatting/jul23_11am_launch_security_autoheal_closeout.md`, `chatting/jul24_pm_local_migration_ingest_reliability_closeout.md`, and `chatting/jul26_local_migration_scale_closeout.md` — and `closeout_artifact_check.py --roster` exits 0 for `jul13_9pm_0`, `jul16_10am_0`, `jul23_11am_0`, `jul24_pm_0`, and `jul26_12am_0`. The two still-open entries are `jul26_pm_7` (M-series tail continues; M1F is next after the merged M1E fixture) and `jul27_pm_10` (Wave 1 remains in flight). Stage 2 also surfaced two recovery candidates owned by the `Lane merges are being lost at batman lifecycle` row below: the browser-oracle stack/port-plan functions on `batman/jul26_12am_2_real_pipeline_browser_fixture`, and the 56-line `docs/runbooks/evidence/20260726_fjcloud_closeout_gate_recovery.md` on `batman/jul25_am_3_fjcloud_closeout_gate_recovery`. This P1 remains open until the supervisor mechanically removes the five exact registry lines published in `docs/audits/triage/20260728T105239Z_orchestration_state/DEREGISTRATION.md`. **UPDATED 2026-07-29 — `jul26_12am_0` only.** That deregistration already happened: an exact-path search of the `## Active orchestrations` list in `/Users/stuart/repos/gridl/mike_dev/pp/supervise.md` returns **0** entries for `jul26_12am_0` (the sole remaining mention is dated prose, which is not a registration), its closeout `chatting/jul26_local_migration_scale_closeout.md` is committed with a `Disposition:` line for all 23 rostered lanes, and `closeout_artifact_check.py --roster` exits 0. The other four registry lines named above belong to their own batches and were not reprobed by this row's update. **Original baseline retained for provenance.** **Added 2026-07-28 by running the closure checker over every fjcloud path in `pp/supervise.md ## Active orchestrations`.** `python3 matt_root/scripts/closeout_artifact_check.py <orch> --roster` exits **1** for `jul13_9pm_0`, `jul16_10am_0`, `jul23_11am_0`, `jul24_pm_0`, `jul26_12am_0`, `jul26_pm_7`, and `jul27_pm_10`, and **0** only for `jul27_pm_0`. Four of the seven (`jul13_9pm_0`, `jul16_10am_0`, `jul23_11am_0`, `jul24_pm_0`) are 4-15 days old and their surviving work is **already re-homed** by the `## Current corrections` bullets above, so they are not open work — they are open *registrations*. That is not free: `pp/supervise.md` requires the supervisor to dispatch, close, or write a freshly-probed park line for every registered orchestration on every tick, and forbids two consecutive identical parks, so each dead registration is a recurring per-tick tax paid against a batch that has nothing left to dispatch. `jul26_pm_7` legitimately stays open (its M-series tail continues, and M1F is its next dispatchable lane); `jul27_pm_10` legitimately stays open (Wave 1 in flight). Repair: write the missing `chatting/*_closeout.md` for each of the four dead registrations plus `jul26_12am_0`, each carrying per-lane dispositions, a `Ship disposition:` line, and an `unshipped inventory owner:` pointing at the `ROADMAP.md` row that already carries the surviving work; then remove those five lines from `## Active orchestrations`. Exit: `closeout_artifact_check.py --roster` exits 0 for all five, and `grep -c fjcloud_dev /Users/stuart/repos/gridl/mike_dev/pp/supervise.md` counts only orchestrations with an in-flight or dependency-ready lane. |
| P1       | Launch evidence freshness refresh                         | **Added 2026-07-27:** `docs/launch_verification_matrix.md` now measures launch-evidence age with `bash scripts/probe_launch_evidence_freshness.sh`. Current snapshot is a launch-readiness fact, not bookkeeping: §2-§6 still read `live`, but §2-§5 evidence is 3-4.5x past the `<14 days` launch bound and §6 is stale too; §1 is the only fresh section. Exit **(corrected 2026-07-28 — the original exit below was unreachable and is withdrawn)**: refresh accepted bundles until the command prints `stale=0 missing=0 missing_section=0 unparseable=0` and exits 0. The withdrawn clause also demanded `malformed_names=0 rejected_bundles=0`; `7b036158f` proved those two counts can never reach zero without deleting audit history — the 14 rejected bundles are preserved `*_NONGREEN` HA soak runs whose completion files are legitimately absent, and the 4 malformed names are 2026-05-02 directories written before the `<UTCTIMESTAMP>_<name>` convention existed. Erasing a failed soak to make a gate green is the exact dishonesty these gates exist to prevent, so the gate now compares against a closed baseline registry of those 18 known bundles and fails hard only on *new* integrity defects. Re-measured 2026-07-28: `sections=6 fresh=1 stale=5 missing=0 missing_section=0 unparseable=0 malformed_names=4 rejected_bundles=14`, probe exit 0, gate PASS. **Corpus repair (i) below is therefore CLOSED by policy, not by curation**. **Section 3/5 local re-verification update 2026-07-28:** `docs/runbooks/evidence/local-reverification/20260728T161546Z/` records Section 3 as `rows_total=13 rows_run_locally=8 rows_green=8 rows_red=0 rows_not_local=5` and Section 5 as `rows_total=8 rows_run_locally=5 rows_green=5 rows_red=0 rows_not_local=3`; this is additive local evidence only and does not reset the section freshness clocks because the bundle lives outside the probe-visible section directories. The genuine Section 3 non-local residuals are `scripts/canary/contracts/stripe_webhook_bad_signature_reject_contract.sh`, `scripts/canary/contracts/stripe_webhook_stale_timestamp_reject_contract.sh`, `scripts/canary/contracts/oauth_redirect_uri_contract.sh`, `scripts/canary/contracts/tenant_jwt_wrong_secret_reject_contract.sh`, and `scripts/canary/contracts/ec2_firewalld_contract.sh`; the genuine Section 5 deployed residual is `scripts/launch/multi_tenant_isolation_probe.sh`. The two Section 5 `infra/api/tests/integration/integration_metering_pipeline_test.rs` rows are a separate local-stack precondition/evidence issue, with `127.0.0.1:3099` and `127.0.0.1:7799` connection-refused proof in the Stage 2 manifest, so they must not broaden this row back into a generic “all of Sections 3 and 5 need live env” bucket. The older §2, §4, and §6 freshness refresh remains governed by their accepted evidence owners. **CORRECTION 2026-07-27 (closeout) — this row understated the state and the shadow-mode claim above is not what ships.** The gate is shadow-mode for *stale-but-well-formed* evidence only; it fails HARD on corpus-integrity defects, and the corpus has them right now, so **`launch-evidence-freshness-contract` WAS red on `main`, and was already red before the `jul27_pm_4` merge** — verified by running the gate in a baseline worktree pinned at `7b984cd60`. **REPAIRED 2026-07-27 at `7b036158f`; the gate now passes and the corpus was NOT touched.** The 18 directories are still on disk, unrenamed and undeleted, because they are audit history: deleting a failed soak to make a gate green is the dishonesty these gates exist to prevent. What changed is the gate's policy, which had conflated "the corpus contains directories I correctly skipped" with "a section's freshness cannot be determined". The former is a permanent property of a real evidence corpus; the latter is a defect. Only the latter now fails. The 18 are pinned in `scripts/tests/launch_evidence_legacy_bundles.txt` and compared exactly in both directions, so a NEW malformed bundle still fails on its first run and a curated-away bundle forces its registry line deleted in the same commit. `rejected_bundles=14 malformed_names=4`: the §5 HA bundles under `docs/runbooks/evidence/ha-*/2026052*_NONGREEN` are missing `soak_exit_code.txt` or carry empty `writes_attempted.count` / `visible_in_search_after.count` / `cross_tenant_leaks.count` / `noisy_neighbor_violations.count` / `fail_fast_during_restart_window.count`, and four §6 `fjcloud_post_deploy_evidence_20260502T*_<pid>` directories violate the bundle-name contract. **These are 2026-05 evidence-hygiene defects, not new breakage — but W1-D shipped a gate that is red on the tree it merged into, and no one noticed because the batch never ran its merged-union gate.** Two separable owners live here and should not be conflated: **(i) corpus repair** — curate or explicitly retire the 18 offending bundles so integrity counts reach zero, which is local, needs no live environment, and can start immediately; **(ii) freshness refresh** — rerun §2-§6 evidence, which needs live environments and is Wave 3. Do (i) first: until it lands, the gate cannot distinguish "evidence is old" from "evidence is malformed," which is the whole point of the instrument. Promote the gate to fatal only after (i) closes with zero false would-be kills and cites that evidence. |
| **P0**   | **Public engine port + unauthenticated engine dashboard on prod AND staging** | **UPDATE 2026-07-24 (`jul23_11am_2` L2 / `jul23_pm_4` L2R / `jul23_pm_5` L2C) — per-VM TLS SHIPPED at the VM layer, but the customer contract is UNCHANGED so cleartext credentials still ship.** Merged on `main`: Caddy is baked into the AMI and both boot paths, served on `:443` with ACME on `:80`, gated by an explicit `CaddyRuntime` provider contract (`infra/api/src/provisioner/cloud_init.rs`), with networking rules `flapjack_acme_http` (tcp/80) and `flapjack_customer_https` (tcp/443) at `ops/terraform/networking/main.tf:289,298`, plus a staging TLS live proof (`docs/runbooks/evidence/engine-tls/20260723T234406Z_l2_staging_proof`, raw payloads stripped from `main` per `0172896735`). **What did NOT happen — the endpoint switch L10 (`jul23_11am_10`) and its live proof L10V (`jul24_am_3`) never dispatched** (their gate needs L7's manifest + L8's verdict + a `LAUNCH.md` entry, none of which exist), so the product still hands customers plaintext: `infra/api/src/services/provisioning.rs:288` and `services/provisioning/auto_provision.rs:284,296` still build `http://{hostname}:7700`, the console still renders that endpoint with `Authorization: Bearer <key>` curl snippets, and `docs/design/engine_data_plane_transport.md` was never written. So the cleartext-credential-in-transit defect this batch was built to fix is **still live on `main`**; the fix exists on the VMs but the customer path does not use it, and the public `0.0.0.0/0` tcp/7700 rule is still in place. Historical narrowing retained: **Live evidence narrowed 2026-07-23** by `docs/runbooks/evidence/engine-admin-surface/20260723T091759Z/probe_transcript.txt`: Stage 2 disabled `/dashboard`, `/swagger-ui`, and `/api-docs/openapi.json` on the two reachable current engines, staging `i-00a3b28ba4c00433a` and prod `i-072d4333322ffd0eb`, where those routes now return 404 while `/health=200` and unauthenticated `/1/indexes=403`. Four staging VMs remain configured-but-unverified because they are still externally unreachable on public tcp/7700; this row therefore still owns the public engine exposure risk and must not be closed from the two-host proof alone. The dashboard/docs clause is also not durably closed for future or replaced VMs: Stage 2 had to replace the baked binary because the pre-replacement live binary lacked the `FLAPJACK_DISABLE_DASHBOARD` support string, so the canonical AMI/reprovision path still needs proof. Historical finding retained as active firewall context: both managed VM security groups admit `0.0.0.0/0` -> tcp/7700, prod `sg-0ab78cabd1b997099` (`fjcloud-prod-sg-flapjack-vm`) and staging `sg-047734af5235c69af`, via committed Terraform `ops/terraform/networking/main.tf:280-287` (`aws_vpc_security_group_ingress_rule.flapjack_public_data_plane`, description "public flapjack data plane"). The current `scripts/security/probe_engine_exposure.sh` EXPOSED/latent-EXPOSED classification still reflects that intentionally public security-group rule and should not be expected to pass until the later TLS/ingress lane removes the exposure. Traffic is plain HTTP, not TLS (`auto_provision.rs:201-202` builds `http://{hostname}:7700`). Root cause context retained: the API dials VMs by public IP (`infra/api/src/services/provisioning/auto_provision.rs:74`, `infra/api/src/services/provisioning.rs:343`), so packets egress via NAT and do not match the `flapjack_from_api` SG-reference rule (`networking/main.tf:271-277`); the remediation remains to derive the control-plane-to-VM address from the private IP/DNS so the SG-reference rule can carry API traffic. Owner: `chats/icg/jul20_4pm_9_engine_exposure_remediation.md` (FJ-1b) — authored but NEVER dispatched. Exit: public 0.0.0.0/0/::/0 tcp/7700 ingress removed from both managed VM security groups; the canonical upstream manifest/archive consumed by ops/packer/flapjack-ami.pkr.hcl is contract-tested to implement `FLAPJACK_DISABLE_DASHBOARD=1` and reprovision proof returns 404 for `/dashboard`, `/swagger-ui`, and `/api-docs/openapi.json`; Terraform plan-time contract fails on public 7700 ingress; a future `scripts/security/probe_engine_exposure.sh` run returns a non-EXPOSED verdict. **Remediation batch status 2026-07-23:** the owner is now `chats/icg/jul22_5pm_0_engine_exposure_tls_orchestration.md`, which supersedes the never-dispatched `jul20_4pm_9` and the doc-only `jul21_pm_1`. Its **Wave 1 merged** (`jul22_5pm_1`, merge `b42d2fe16`) — that is the dashboard/docs shutdown recorded above. Its **Wave 2 and Wave 3 are authored and NOT dispatched**: `jul22_5pm_2_engine_data_plane_tls_ingress` (per-VM Caddy TLS on loopback — an ALB was considered and rejected because it collapses client IPs, breaking `restrictSources`, per-IP limits and search analytics) and `jul22_5pm_3_engine_public_ingress_removal_and_reconcile` (delete the public 7700 rule once TLS replaces it). **The batch's stated premise is that the console currently tells customers to send `Authorization: Bearer <key>` to `http://vm-*.flapjack.foo:7700` with no TLS anywhere on the fleet** — the engine does enforce auth (unauthenticated `/1/indexes` = 403), so this is a cleartext-credential-in-transit defect, not an open-data defect, and the 2026-07-20 exposure probe never tested for encryption at all. Wave 2 is parked on a cross-repo detector in `mike_dev`, not on fjcloud work. |
| **P0**   | **Deactivate credentials exposed by the public-mirror evidence** | Stage 3 evidence `docs/runbooks/evidence/mirror-leak-scan/20260716T233251Z_credential_scan/` records `LIVE_CREDENTIALS_PRESENT` with `CI-unexaminable sites: 31`. The dispatch-ready owners are `docs/runbooks/evidence/secret-rotation/20260717T000002Z_mirror_leak_stage4/GAP_SPEC_rds_password_rotation.md` and `docs/runbooks/evidence/secret-rotation/20260717T000002Z_mirror_leak_stage4/GAP_SPEC_api_ssh_key_rotation.md`. **DOWNGRADED 2026-07-21 — issuing-layer deactivation is DONE and verified; only a residual remains.** `docs/runbooks/evidence/secret-rotation/20260719T182500Z_mirror_leak_rotation_verified/VERDICT.md` records **VERIFIED ROTATED**: SSM prod and staging `db_password` advanced to Version 2 (2026-07-17) and EC2 key pairs `fjcloud-api-prod` / `fjcloud-api-staging` were recreated 2026-07-17, discharging both Stage-4 GAP_SPECs at the issuing layer. **Residual (the only part still open):** the superseded SSH public key may persist in `authorized_keys` on instances launched before 2026-07-17T20:29Z, so EC2 authorization-path *absence* is not yet evidenced; that closure rides with the fleet rebuild. Original text retained as history: *"Issuing-system deactivation remains open under the authorized Terraform/fleet environment-mutation owner; the inventory remains explicitly live until dated RDS rejection and EC2/instance authorization-path absence evidence returns for staging and prod."* This row does not own L9 public-mirror recovery design. |
| **P0**   | **The production Flapjack data plane has never served a request** | **UPDATE 2026-07-22 (`jul16_10am_7`) — the non-vacuous prod proof and the recurring RC gate LANDED; this row is narrowed to the `origin/main` verification below, not closed.** A prod E2E bundle now exists at `docs/runbooks/evidence/prod-data-plane/20260723T000045Z_prod_e2e/` proving signup → provision → ingest → **exact** search (`run_b_transcript.txt`: `objectID=lifecycle-doc-1, nbHits=1`) → zero-residue cleanup (`db_post_cleanup_customer.sql.txt`/`db_post_cleanup_tenant.sql.txt` = 0) against real prod, and `scripts/validate_full_vm_lifecycle_prod.sh` gained a launch-gate-safe `data-plane` mode registered as the `prod_full_vm_lifecycle` paid-beta RC step (section 6) so a fresh prod lifecycle runs on every non-`--staging-only` RC (see `LAUNCH.md ## STATUS` 2026-07-22). **This does NOT resolve the `7700` firewall contradiction or downgrade the public-engine-exposure P0 at the top of this table** — both still govern the same `flapjack_public_data_plane` rule and the original SG-existence exit oracle is superseded by that row's removal instruction; the exits below no longer assert that rule. The billing/finalize leg remains parked behind the live-money `stripe_env` gate (`data-plane` mode never enters it). Original diagnosis retained verbatim: **Re-verified live 2026-07-16** (read-only EC2/SSM probes, account `213880904778`, `us-east-1`) — this row supersedes every prior "paid beta is live" framing for the *search data plane*. Signup/pricing/billing surfaces on prod are real; **search on prod is not**. Two independent faults, either alone fatal. **Fault 1 — the fleet runs an engine-less AMI.** All running `fj-vm-shared-*` instances (34 of the 35 running EC2 instances) carry `ami-078228dbe86117d85` (`flapjack-0.1.0-20260408`), which contains no `/usr/local/bin/flapjack`; `flapjack.service` has crash-looped since May (`status=203/EXEC`, `NRestarts=947777` on the sampled node, nothing listening on `:7700`). The SSM pointer `/fjcloud/prod/aws_ami_id` is **already correct** (`ami-01deed1a1e04b3276`, `flapjack-1.0.2-pl13-20260530`) and runs on zero instances — the VMs (Apr 24–May 19) simply predate the pointer update (May 29–30). No Packer work and no `flapjack_dev` work is implied. **Fault 2 — the API cannot reach the VMs. ⚠️ SUPERSEDED 2026-07-21 — the SG half of this diagnosis no longer matches live state; do not act on it as written.** Live read-only evidence captured 2026-07-21 (`docs/live-state/20260721T030116Z_engine_exposure_stage2/referenced_security_groups.json`) shows prod `sg-0ab78cabd1b997099` now admits `:7700` from **both** the API SG reference **and** `0.0.0.0/0` — and that public rule is no longer an undeclared hand patch on staging alone: it is committed Terraform applied to both environments (`ops/terraform/networking/main.tf:280-287`, `aws_vpc_security_group_ingress_rule.flapjack_public_data_plane`). So the connectivity blocker described below is very likely already gone, and the remaining gap is the **missing proof**, not the missing route. **Consequence that must be resolved before this row is actioned:** this row's third exit condition is an awk oracle asserting that a `7700` + `0.0.0.0/0` ingress rule **exists**, which mandates exactly the exposure the new engine-exposure P0 at the top of this table requires be **removed**. Both rows govern the same rule. The correct resolution is in that row: the API dials VMs by *public* IP (`auto_provision.rs:74`, `provisioning.rs:343` read `vm_instance.public_ip` and write the DNS A record from it) which is why NAT-egressed packets never matched the SG reference — switch those two call sites to the **private** address so the existing `flapjack_from_api` reference rule matches, then delete the public rule and rewrite this exit oracle to assert its **absence**. Original 2026-07-16 text, retained verbatim as history: *"The API dials VMs by public IP (`infra/api/src/services/provisioning/auto_provision.rs:109-120` builds `http://vm-shared-<id>.flapjack.foo:7700`; the A record is written from `public_ip` at `:54-73`), but prod SG `fjcloud-prod-sg-flapjack-vm` (`sg-0ab78cabd1b997099`) admits `:7700` only from the API SG — and the API egresses via NAT, so packets arrive from the NAT EIP and never match the SG reference. Staging (`sg-047734af5235c69af`) works only because of an undeclared hand-added rule (`0.0.0.0/0` on `:7700`, description "public flapjack data plane", added 2026-04-24, present in no `.tf` file)."* **Structural consequence — this is why 13 consecutive RCs missed it:** every automated launch proof runs against staging, so a hand-patched staging has certified a dead prod for three months. The dead VMs hold no customer data (the engine never started), so the rebuild is a plain re-provision, not a migration. Owner: `chats/icg/jul16_10am_0_prod_truth_launch_orchestration.md` (lanes 1–4), which supersedes the never-dispatched single-lane `chats/icg/jul14_2pm_1_prod_data_plane_fix.md` and reuses its stage bodies. **Why it sat unfixed:** that lane was authored 2026-07-14 from live receipts, was never registered in any orchestration, and sat 4th in the Algolia batch's environment-mutation queue behind a *dead* RC exam. Exit (all against `origin/main` after the `jul16_10am_7` merge; the SG-existence oracle is intentionally dropped — the engine-exposure P0 owns that rule's *removal*): `git show origin/main:scripts/launch/run_full_backend_validation.sh \| grep -q validate_full_vm_lifecycle_prod`; `git show origin/main:docs/launch_verification_matrix.md \| grep -q validate_full_vm_lifecycle_prod`; `git cat-file -e origin/main:docs/runbooks/evidence/prod-data-plane/20260723T000045Z_prod_e2e/SUMMARY.md`; `git show origin/main:docs/runbooks/evidence/prod-data-plane/20260723T000045Z_prod_e2e/run_b_transcript.txt \| grep -Fq 'objectID=lifecycle-doc-1, nbHits=1'`; `git show origin/main:docs/runbooks/evidence/prod-data-plane/20260723T000045Z_prod_e2e/db_post_cleanup_customer.sql.txt \| awk '$1=="customer_rows"{header=1;next} header && $1~/^[0-9]+$/{value=1;zero=($1==0);exit} END{exit !(header&&value&&zero)}'`; and `git show origin/main:docs/runbooks/evidence/prod-data-plane/20260723T000045Z_prod_e2e/db_post_cleanup_tenant.sql.txt \| awk '$1=="tenant_rows"{header=1;next} header && $1~/^[0-9]+$/{value=1;zero=($1==0);exit} END{exit !(header&&value&&zero)}'` (the zero-residue oracles read the psql count table's `customer_rows`/`tenant_rows` value row — do not grep for a synthetic `customer_rows = 0` string the artifact does not contain). L8 owns the next full RC verdict. |
| **P0**   | **Launch-matrix §1 regressed to 4/6 red on two harness defects** | **UPDATE 2026-07-24 (`jul23_11am_1` L1 / `jul24_am_1` L1C / `jul24_am_9` L1E) — the metering repair LANDED AT THE CODE LEVEL but §1 is NOT discharged; read this clause first.** Merged on `main`: the fail-honest rollup freshness guard (`scripts/probe_usage_rollup_freshness.sh` + `usage_rollup_freshness_staging/_prod` live-state rows + the `usage-rollup-freshness-contract` local-CI gate, merge `fa7fc6a26`), the `UsageDailyRollupSuccess` producer metric (`infra/aggregation-job/src/main.rs:227,292`, merge `10f00d6b8`), and durable provisioning repairs (`ops/user-data/bootstrap.sh`, `infra/api/src/provisioner/cloud_init.rs` persist the admin key + derive env-aware metering URLs — **new VMs only**). Staging produced a fresh `usage_daily` row exactly ONCE (2026-07-24T09:19:25Z, `fresh_rows=1`) but only after a **manual** convergence of 6 running nodes + a hand-run `fjcloud aggregation job` (`docs/runbooks/evidence/staging-billing-rehearsal/20260724T044921Z_l1c_completion/POSTDEPLOY_RUNTIME_REPAIR.md`); the systemd timer was never proven to self-produce, so it re-stales on its own. **Still OPEN for the launch gate — six unmet exits:** (a) the fixed-code staging redeploy FAILED (L1E merge `e63af75a5` carries `Batman-Merge-Status: running`, all 4 stages unrun; `rust-lint=failure` cargo-fmt diff in `cloud_init.rs:277` skipped `deploy-staging`); (b) the live billing rehearsal never ran (frozen-source SHA tangle: producer `10f00d6b8` is not an ancestor of the deployed `7b5584e85`); (c) **CORRECTED 2026-07-27 — the staleness alarm is now MERGED**, not plan-only: `aws_cloudwatch_metric_alarm.usage_daily_rollup_missing` exists at `ops/terraform/monitoring/main.tf:826` (commit `ccf9a3981f`) with four static assertions at `ops/terraform/tests_stage7_static.sh:321-324`; the residual is that it has never been `terraform apply`-ed to either environment, so the outage still cannot page; (d) no `S1_BLOCKER=discharged` terminal receipt exists anywhere; (e) no §1 bundle is green (`docs/runbooks/evidence/ses-coverage-a1/20260722T204849Z_jul16_10am_s1_rerun/all_green.txt=0`); (f) the newest committed live-state still reads `usage_rollup_freshness_staging=ACTION_REQUIRED` (`docs/live-state/20260723T170623Z/SUMMARY.md`). Cheapest close path: reconcile the SHA ordering so the deployed staging SHA contains `10f00d6b8`, redeploy through Debbie past the deploy gate, prove the aggregation **timer** self-produces a fresh row (not a manual invocation), merge + `terraform apply` the alarm, run the rehearsal to `rehearsal_completed` with no `rollup_stale`, then emit the `S1_BLOCKER=discharged` receipt and rerun §1 to `all_green=1`. Historical root-cause prose retained below. **ROOT CAUSE REPLACED 2026-07-22 — this row's title and both named defects are now historical; read this paragraph, not the ones after it.** `jul16_10am_6` (merge `7b5a37ec6`) froze `9d521e1c9`, synced staging, and reran the canonical six-row bundle: `docs/runbooks/evidence/ses-coverage-a1/20260722T204849Z_jul16_10am_s1_rerun/` is still `all_green.txt=0` at **4/6**, but **neither original defect appears**. Both `git: command not found` and the ALB 504 are gone — `ses_bounce` and `ses_complaint` are now `rc=0 pass=1`, and `deployable_currency` reports `deployable_drift=false` from inside the VM. The two red rows are `staging_dunning_delivery` and its dependent `dunning_email_inbox`, both `classification=rehearsal_failed / category=setup_infra`, and the nested detail names the actual blocker exactly once: `"classification":"rollup_stale"`, `"detail":"usage_daily freshness check failed ... usage_daily has no recent rollups (0 rows within 48h) — aggregation job may not be running"`. **This is not a harness defect and not email logic — it is a staging data-pipeline outage.** The rehearsal's `metering_evidence` step correctly refused to attempt live mutation on stale rollups, so `live_mutation_guard` and `live_mutation_attempt` were blocked downstream. Preflight passed all eleven config checks. **The next action is therefore to find out why the staging aggregation job has produced no `usage_daily` rows in 48h** (owner: `infra/aggregation-job`, deployed via its systemd timer) — not to touch `run_ses_coverage_a1_in_vpc.sh` again. Until that is fixed, §1 cannot go green and L8's RC cannot reach an allowed shape. Historical diagnosis retained below because the ALB/broadcast latency analysis is still the owner for the P1 async-broadcast row: Surfaced by the jul13_12pm Wave-3 §1 rerun, `docs/runbooks/evidence/ses-coverage-a1/20260715T203317Z_jul13_iam_in_vpc_rerun/` (`all_green.txt=0`). The IAM root cause the batch was built to fix **is fixed and live-proven**; §1 is now red on two *new*, unrelated, and concrete defects — neither in product email logic. **(a) `git: command not found` in the VM.** `scripts/launch/run_ses_coverage_a1_in_vpc.sh:187` packages the repo with `git archive` (a tarball with no `.git`) and extracts it to `/opt/ses-coverage-a1` on the instance; `staging_billing_rehearsal.sh` then sources `scripts/lib/deployable_currency.sh::probe_staging_deployable_currency`, which shells out to `scripts/deploy_status.sh:107-109` (`git -C "$REPO_ROOT" rev-parse origin/main`). The VM has neither git nor a checkout, so the currency preflight blocks and takes `staging_dunning_delivery` + `dunning_email_inbox` down with it (`classification=deployable_currency_unknown`). Deployable currency is a *dev-repo* fact; the runner already knows the frozen `--sha` on the dev host where git exists, so the fix is to compute it there and inject the verdict — not to install git in the VM. **(b) ALB 504 on admin broadcast.** `ses_bounce`/`ses_complaint` newly failed with `broadcast request failed with HTTP 504` at `elapsed_ms=60547`. `infra/api/src/routes/admin/broadcast.rs:112-193` sequentially awaits one SES send **plus** one DB write per recipient inside the request handler; `aws_lb.api` (`ops/terraform/dns/main.tf:265-277`) declares no `idle_timeout`, so the AWS default of 60s applies — an exact match for the observed 60.5s. Latency is O(recipients), so this degrades as the customer table grows and will recur at any real customer count. `broadcast_email`'s existing `dry_run` arm (`:122-129`) returns `recipient_count` without sending and is the cheap non-mutating way to confirm N first. Owner: `chats/icg/jul16_10am_0_prod_truth_launch_orchestration.md` lanes 5–6. Exit: a fresh §1 bundle under `docs/runbooks/evidence/ses-coverage-a1/` with `all_green.txt=1` at a frozen merged SHA. |
| P1       | Async admin-broadcast job with `202 Accepted` and status readback | Future backlog only; do not treat this as a second dispatcher or queue for the current synchronous request path. Stage 2 keeps `broadcast_email` synchronous and bounded at five in-flight recipients. Revisit a durable job when `scripts/customer_broadcast.sh --dry-run` reports roughly 125+ non-deleted recipients or the bounded-concurrency capacity KAT loses its 55-recipient/20 ms headroom under the 500 ms guardrail. That trigger is conservative: Stage 1 measured 55 recipients and the first bounce/complaint probe timed out at roughly 60.5 seconds, while five-way bounded fan-out should push the same latency shape well below the ALB default. The denominator alone did not prove the 504 cause; this row exists for future status/idempotency ownership if synchronous request headroom shrinks. |
| P1       | Public-release completion (active orchestration)          | Owner: `LAUNCH.md ## STATUS` for verdict, `docs/launch_verification_matrix.md` for launch-sentence evidence, and `chats/icg/jul16_10am_8_rc_verdict_and_launch_anchor.md` (L8 of the `jul16_10am_0` prod-truth batch) for the next RC/LAUNCH anchor. **Owner moved — do not dispatch the old one:** `chats/icg/jul13_12pm_3_s1_rc_launch_verdict.md` was dispatched 2026-07-15, budget-stopped at 2/3 with its verdict unrun, and was then formally **ABANDONED** by operator decision 2026-07-16 (`LAUNCH.md ## STATUS`). The "(authored, NOT yet dispatched)" description this row previously carried was wrong on both counts and contradicted the correction later in this same row. The jul12_pm anchor ran 2026-07-13: RC bundle `docs/runbooks/evidence/invite-ready-rc/20260713T145624Z_jul12_pm_rc_verdict_launch_anchor/` = `NOT-READY-real-defects`, sole real defect `staging_billing_rehearsal=invoice_email_ses_not_ready` (§2); §§1/3/4/5/6 green per its `verdict.json` `section_impact`. That defect's root cause (staging instance-role CloudWatch Logs read denial) is fixed and live-proven by the IAM row below, and the §1 runner + §1-aware RC classifier tooling is merged (jul13_12pm Wave 2). **CORRECTED 2026-07-16 — Wave 3 was dispatched and did NOT complete.** Worker `0b5da` ran `chats/icg/jul13_12pm_3_s1_rc_launch_verdict.md` on 2026-07-15, finished Stage 1 (frozen-SHA staging currency) and Stage 2 (the six-row §1 bundle), then terminated `budget_exceeded` at 2/3 stages ($161.99, 7h11m, `summary.json:status`) with Stage 3 — **the RC verdict and the LAUNCH anchor, i.e. the lane's entire reason to exist** — never run. Its partial work merged anyway (`f5abff380`). Consequences: no RC newer than the 2026-07-13 `jul12_pm` anchor exists; `chatting/jul13_iam_s1_rc_launch_closeout.json` does not exist, so `closeout_artifact_check.py` fails and the jul13_12pm batch is NOT closed; and the Algolia batch's merge freeze — whose release condition is a `jul13_12pm` verdict in `LAUNCH.md ## STATUS` — remains HELD on a lane that is dead. The §1 bundle it did produce came back **red** (see the §1 P0 row above). Authoring lesson, already an owned rule this lane violated: `pp/create_checklists.md:23` requires that "terminal verdict or closeout-anchor work that gates later dispatch gets its own small serial lane"; bundling the §1 run and the terminal verdict in one lane is precisely what let a budget stop strand the deliverable. Remaining launch work is re-carved accordingly in `chats/icg/jul16_10am_0_prod_truth_launch_orchestration.md`, which re-dispatches the RC path to a verdict (the first arm the freeze table itself authorizes) rather than abandoning the exam. Do not announce until that RC reaches an allowed launch shape. Exit unchanged: `chatting/jul13_iam_s1_rc_launch_closeout.json` exists on `origin/main` and `python3 scripts/launch/validate_launch_closeout.py --closeout=chatting/jul13_iam_s1_rc_launch_closeout.json --launch=LAUNCH.md --roadmap=ROADMAP.md --matrix=docs/launch_verification_matrix.md ...` passes per the Wave 3 gate in `chats/icg/jul13_12pm_0_iam_s1_rc_orchestration.md`.                                                                                                                                                                                                                 |
| P1       | In-VPC dunning evidence IAM read policy                   | Newly surfaced 2026-07-13 by `docs/runbooks/evidence/ses-coverage-a1/20260713T004742Z_jul12_pm_in_vpc_rerun/GAP_SPEC.md`. The scoped read policy now lives in `ops/iam/fjcloud-instance-role.tf` and was applied to staging against the actual `fjcloud-instance-role`; `docs/runbooks/evidence/ses-iam-read/20260713T202728Z_staging_apply/` records `terraform_apply`, all four CloudWatch Logs API probes `ok`, `stream_denominator=5`, and `state_reconciliation=not_needed`. Keep this row open until Wave 3 reruns the canonical §1 six-row in-VPC bundle and proves `all_green.txt=1`; do not treat the IAM apply proof alone as the dunning/inbox launch RC. Update 2026-07-14: the Wave 2 execution tooling for that rerun is merged — `scripts/launch/run_ses_coverage_a1_in_vpc.sh` (six-row runner, canonical `run_manifest.json`), `scripts/lib/ses_coverage_a1_integrity.py`, and the `--section1-manifest`-aware RC classifier in `scripts/launch/invoke_rc_with_env.sh` — so the only remaining work is dispatching `chats/icg/jul13_12pm_3_s1_rc_launch_verdict.md`. Exit: fresh §1 bundle under `docs/runbooks/evidence/ses-coverage-a1/` with `all_green.txt=1` at a frozen merged SHA.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| P1       | Algolia migration replacement program (active orchestration) | **UPDATE 2026-07-24 — F10 IS NOW MERGED, and F11 local surface activation is now landed on this branch.** `jul13_9pm_20_algolia_catalog_finalize` merged to `main` at `9c45d0de6` (with a follow-on `Record F10 publication gate repair` `a29a7d74a`), and its catalog live probe `scripts/algolia_import_catalog_live_probe.sh` now EXISTS on `main` — so the lane the prior snapshot called "genuinely unfinished, not merely unmerged" has landed. The catalog-caller denominator repair `jul23_pm_2` (F10R) also merged (3 of its 9 Stage-2 items deferred to F10P). **F11's local-only deliverable is now wired:** availability is no longer hardcoded `unavailable()`, the same gate now fail-closes `POST /migration/algolia/list-indexes`, and `/console/migrate` can render a minimal available-state indicator when the flag is enabled locally. **Unchanged served default:** staging and production still come up CLOSED because `FJCLOUD_ALGOLIA_MIGRATION_ENABLED` defaults `false`, and the privacy tail (F10P `jul23_pm_3`, F10C `jul24_am_2`) never dispatched (dispatch gate red: `catalog_lifecycle_acceptance_oracles.json` still reports `privacy_scrub_transport_unavailable`). The four orchestrations previously parked behind F10 (prod-truth L8 RC verdict, IAM/S1 closeout, the `jul20_4pm` console batch, the `jul21_10pm` reliability closeout) are therefore **unblocked and must now be dispatched** — that is the highest-leverage next move for the launch gate. Remaining not-started in the current local-wave chain: J1, I0, I1, S0, V1–V5, D0–D5. Historical prose (three snapshots deep) follows. **STATE AS OF 2026-07-23 — read this before the historical prose below, which is now three snapshots deep.** **F9 IS MERGED** (`jul21_pm_2_f9_algolia_runtime_continuation`, 6/6 stages, merge `20b8d6bde`) — not "in flight". It shipped the cloud→engine dispatch service (`infra/api/src/services/algolia_import/{admission,cancel,reconciliation,observation,status,error_classifier}.rs`), atomic/idempotent cancel intent, the cancel-vs-promotion race matrix, and a live dispatch probe (`scripts/algolia_import_dispatch_live_probe.sh`) run against a source-built cloud/engine union. **F10 (`jul13_9pm_20_algolia_catalog_finalize`) is in flight and is the batch's — and the repo's — single serialization point.** It has been running since 2026-07-22 (~15h, $140+, stage 5/6, 66 files / ~10.5k insertions on `batman/jul13_9pm_20_algolia_catalog_finalize`, not merged), and because it owns `ROADMAP.md` plus the Algolia service surface, **four other registered fjcloud orchestrations are parked behind it**: prod-truth L8 (the RC verdict), the IAM/S1 closeout, the `jul20_4pm` console batch, and the `jul21_10pm` reliability closeout. Its catalog live probe (`scripts/algolia_import_catalog_live_probe.sh`) does **not** exist on `main`, so the lane is genuinely unfinished, not merely unmerged. **F11 remains the sole activation owner and nothing customer-facing has changed:** availability is still hardcoded `unavailable()` and `/console/migrate` still renders only the unavailable panel (verified on `main` 2026-07-23). Remaining not-started: **16 lanes** — F11, J1, I0, I1, S0, V1–V5, D0–D5 (F9 merged, F10 in flight). Historical prose follows verbatim. Owner: `chats/icg/jul13_9pm_0_algolia_migration_reliability_orchestration.md` (18-wave DAG across fjcloud_dev + flapjack_dev; closeout artifact `chatting/jul13_9pm_algolia_migration_reliability_closeout.md`). Wave 0 lane F1 (`chats/icg/jul13_9pm_1_algolia_migration_safety_gate.md`) merged 2026-07-14: unsafe `POST /migration/algolia/{list-indexes,migrate}` routes removed fail-closed (`93a8165dc`), availability ops gate + SSM parameter owner `ops/scripts/set_algolia_migration_availability.sh` (`c0fc4569f`), reusable remote safety oracle `scripts/algolia_migration_safety_probe.sh` wired into `local-ci --fast` (`448ba448c`), and exact-identity prod publish contract in `scripts/launch/post_wave_a_sync_prod.sh` (`978640f6b`). **Guard correction 2026-07-16:** the fail-closed behaviour is a hardcoded constant (`infra/api/src/routes/migration.rs:75-83`), NOT the `FJCLOUD_ALGOLIA_MIGRATION_ENABLED` flag, which the availability handler does not read. (**Corrected 2026-07-18:** the flag IS now read by `infra/api/src/routes/migration/eligibility.rs:108` and `infra/api/src/routes/migration/jobs.rs:164,393` — the earlier "no handler reads" wording is false as of the 2026-07-18 handlers. **Corrected 2026-07-19:** current `origin/main` mounts the availability, list-indexes, destination-eligibility, job create/list/read, cancel, and resume routes in `infra/api/src/router/route_assembly.rs:405-434`; the flag therefore has served handler reach, but customer availability still has no activation effect because `infra/api/src/routes/migration.rs:120-159` hardcodes `available=false` and `cancel/resume/replace=false`, and `web/src/routes/console/migrate/+page.svelte` renders only the unavailable state.) This remains a live trap for the enablement ladder: Wave 14 (`chats/icg/jul13_9pm_21_algolia_staging_enable.md:10`) flips that flag via `ops/scripts/set_algolia_migration_availability.sh` and then "probe[s] served availability" — which would stay `false` unless F11 wires the availability/capability owner to the real route and engine support. F11 (`chats/icg/jul13_9pm_6_algolia_surface_activation.md`) remains the sole activation owner and its dispatch gate must assert that before any D-lane runs. **Route-surface correction:** `POST /migration/algolia/list-indexes` and the job/eligibility/cancel/resume surface are mounted on current `origin/main`; list-indexes still takes a customer's Algolia `app_id`/`api_key` and calls out with no availability check, while the shipped console does not expose a control that reaches it. The orchestration's own invariant table names this an admission entry point that a false guard must block, with F11 as implementing owner — so the gap is scheduled, not silent, but the route surface must not be promoted to customer-visible activation before F11. **Progress as of 2026-07-20** (reconciled against `origin/main` merge commits, not lane-file or status-token state; the prior 2026-07-16 snapshot below was stale and understated progress by seven lanes): merged — F1, F2, F3, F3R (as `jul15_1pm_f3_replacement_reservation_repair`), F4, F5 **and its full recovery chain** — F5R1, F5R2, F5R4 (`6f564df63`), F5P1 (`2c39bfaa0`), F5P2 (`d9bd6f667`), F5V (`9bee7f828`, "completes F5 recovery chain") — plus F6, F6R1, F6R2 (`3c35e077b`), F7 (`6d47631f8`, symlink/TOCTOU fail-closed hardening in `ops/packer/validate_flapjack_ami_input.sh`, 36/36 green), F12, and **F8** (`jul13_9pm_10`, shipped 2026-07-19 through the local-only `jul18_pm_0` orchestration — `d3f4a2395`, `1ee2c03bb`). **Open disposition — F5R3:** no merge commit exists on `origin/main` for F5R3, yet F5V claims the chain is complete. Confirm F5R3 was superseded (most likely by `jul17_pm_0_algolia_lifecycle_size_split`) and stamp that disposition, or re-home it; do not let batch close infer it. **Not started — 18 lanes**: S0, F9, F10, F11, J1, I0, I1, V1–V5, D0–D5. **Convergence risk, stated plainly:** F5 and F6 each budget-stopped and were split into strictly serial recovery chains — **those chains have now COMPLETED** (F5V merged 2026-07-19, discharging F5's original edge; F6R2 merged, discharging F6's). What remains serial is (a) the local consumer chain **F9→F10→F11→J1→V1→V2**, which needs no environment at all, and (b) Waves 11–18, ten consecutive single-lane deployment waves that do. At the observed $150–310/lane the remaining 18 lanes imply roughly $2.7k–5.6k. This program is the strategic differentiator for Algolia refugees (`docs/competitor-pricing-reference.md` — Algolia bills per search; Flapjack bills per MB stored with unlimited searches) but it is **not on the launch sentence** and must not outrank the P0 rows above. **Integration-branch gaps — ALL CLEARED, verified 2026-07-20** (retained as history; do not re-open): the branch `plan/jul14-algolia-amendments` was reconciled into `main` and **RETIRED** on 2026-07-18 — `git ls-remote --heads origin 'plan/jul14*'` returns 0 rows, so any lane file still naming it as a merge target is stale prose and must target `main`. Its two pre-push failures are gone: `check-sizes` passes (`infra/api/src/routes/indexes/lifecycle.rs` is **659** lines against the 850 limit, down from 896, closed by `jul17_pm_0_algolia_lifecycle_size_split`), and `source-pollution` passes (`chats/icg/_followups.md` carries **0** worktree-absolute `parallel_development/` paths). The one surviving item from this list is the F3R/F5R3 roster gap — see the F5R3 disposition note above and confirm the batch-close roster demands a stamp for both. **Next dispatch: F9** — and it is blocked on exactly one thing, which is *not* prod. **The merge freeze is LIFTED**, not HELD: operator decision 2026-07-16, recorded in `LAUNCH.md ## STATUS` ("jul13_12pm_3_s1_rc_launch_verdict — ABANDONED (operator decision); merge freeze LIFTED"). The prior "HELD" text here was stale and is withdrawn. F9's remaining gate is the cross-repo engine receipt: flapjack `origin/main` must mount BOTH `/1/migrations/algolia` (L3 `jul18_1pm_4`) and `/1/migrations/algolia/:job_id/cancel` (L5 `jul18_1pm_5`). **Measured 2026-07-20: base route PRESENT (L3 merged), cancel route ABSENT (L5 in flight).** So F9 is correctly blocked on the *engine*, and the moment L5 merges the gate goes green. **UPDATE 2026-07-21 — THE GATE IS GREEN AND F9 IS IN FLIGHT.** L5 (`jul18_1pm_5_migration_cancel`) merged to flapjack `origin/main` at `162af5dd8` on 2026-07-20 18:24 EDT — hours after the measurement above. Verified on flapjack `origin/main`: `engine/flapjack-http/src/router.rs:228` mounts `POST /1/migrations/algolia`, `:232` `GET /1/migrations/algolia/{job_id}`, and `:236-237` `POST /1/migrations/algolia/{job_id}/cancel` → `handlers::cancel_algolia_migration` (`migration/mod.rs:406`, enforcing `ensure_async_migration_owner` and mapping `MigrationCancelRequest::TooLate` → 409); all three are published in `engine/docs2/openapi.json`. F9 (`jul13_9pm_11`) was dispatched on that receipt and is **running now**. The not-started count below is therefore **17 lanes, not 18** — F9 is in flight, not unstarted. **Environment split that matters for parallelism:** F9/F10/F11/J1/V1/V2 are local code and local-stack verification and need no AWS environment — they must never be parked behind prod work. Only S0, I0, I1, D0–D5, and V3–V5 hold the environment claim, and those stay parked behind `jul16_10am_0`. The parked successor draft (`flapjack_dev/chats/icg/jul13_9pm_0_ingestion_control_plane_orchestration.md`) must NOT dispatch while this batch is active. Exit: V5 green and `test -f chatting/jul13_9pm_algolia_migration_reliability_closeout.md` plus `closeout_artifact_check.py` pass per the orchestration's batch-close block. |
| P1       | Execute staging billing rehearsal rerun                   | CLOSED 2026-07-12 by `docs/runbooks/evidence/staging-billing-rehearsal/20260712T010555Z_current_main/`, reconciled in `chatting/20260712T050500Z_jul11_pm_launch_unblock_closeout.md`. Staging was deploy-current for `dev_sha=ae06b99a96bc9d02f02e5fe4c930379f54270cd1` (`deployable_drift=false`, `doc_only_ahead=false`), `summary.json` is `passed` / `rehearsal_completed`, and `invoice_email.json` records CloudWatch Logs SES send-events evidence with `emails_with_messages=1` and `missing_invoice_ids=[]`. The reset-first owner resets exactly one already-allowlisted tenant, and repeat-pass reuse is constrained to that confirmed tenant before the live billing/email path runs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| P1       | Web-plane deploy automation                               | Landed for the branch-aware staging deploy and served-surface proof. `origin/main:.github/workflows/ci.yml` contains both `--branch=staging` and `--branch=main` Pages deploy anchors, and the closeout live probes showed `cloud.staging.flapjack.foo/pricing` plus `_app/version.json` current while prod also served the signup CTA. The prior branch-alias staleness is closed. Remaining follow-up only: split the prod `--branch=main` deploy into the prod mirror with its own scoped Cloudflare token; that two-token hardening is deferred and non-gating.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| P1       | AWS live-infra E2E remaining guardrails                   | Repo-owned prep and validation contracts are implemented. Budget action auto-enforcement remains a future option; alert-only coverage is live.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| P1       | Live RDS restore proof (2026-04-23 captured)              | Current-main snapshot restore succeeded and isolated target cleanup evidence was captured at `docs/runbooks/evidence/database-recovery/20260612T002802Z_current_main/`; reruns keep the same contract and evidence-bundle shape. **Added 2026-07-28 (`jul28_11am_3` Stage 3, measured not assumed): the `docs/launch_verification_matrix.md` §4 contract-coverage row cites four evidence logs that were never produced** — `docs/runbooks/evidence/database-recovery/20260525T231835Z_a4_coverage/{drill_static.log,drill_unit.log,evidence_static.log,evidence_unit.log}` are all absent, so that row is now marked `UNSUPPORTED` rather than reading as proof. Owner producers exist and are runnable: `ops/terraform/tests_rds_restore_drill_static.sh`, `ops/terraform/tests_rds_restore_drill_unit.sh`, `ops/terraform/tests_rds_restore_evidence_static.sh`, `ops/terraform/tests_rds_restore_evidence_unit.sh`. Falsifiable produce/replace/remove exit: either (a) produce all four receipts by running the owner scripts, tee stdout to the four named paths, and confirm `bash scripts/check_status_doc_consistency.sh` plus a dead-link scan report zero missing targets under that bundle; (b) replace the four citations with a real evidence bundle that proves the same coverage; or (c) remove the four citations from §4 and restate the row's coverage claim to what the surviving bundle files actually prove. Closing this row requires one of those states — leaving the citations present and the files absent is the failure condition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| P2       | `panics_total` monitoring alarm wiring                    | **Applied + live-confirmed (2026-07-11).** `docs/runbooks/evidence/panics-alarm/20260710T210400Z/` records staging and prod PASS, and a fresh live `aws cloudwatch describe-alarms --alarm-name-prefix fjcloud --region us-east-1 --query "MetricAlarms[?MetricName=='PanicsPerPeriod'].[AlarmName,StateValue]"` (creds explicitly sourced from `.secret/.env.secret`, ambient unset) returned `fjcloud-staging-api-panics-high OK` and `fjcloud-prod-api-panics-high OK` in namespace `fjcloud/api`. The app counter is `panics_total`; Terraform alarms watch `PanicsPerPeriod`. The closeout's earlier `InvalidClientTokenId` reread was a stale-ambient-cred artifact, now superseded by this successful live readback. This P2 row is effectively closed (both envs live).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| P2       | SES deliverability proof                                  | Narrowed 2026-07-13 by the terminal jul12_pm in-VPC bundle. `verify_email_clickthrough`, `password_reset_clickthrough`, `ses_bounce`, and `ses_complaint` are green in `docs/runbooks/evidence/ses-coverage-a1/20260713T004742Z_jul12_pm_in_vpc_rerun/`; clean-env support/canary proof remains green at `docs/runbooks/evidence/ses-inbox-canary-clean-env/20260711T014655Z/SUMMARY.md`. Launch-matrix §1 remains partial because `staging_dunning_delivery` and dependent `dunning_email_inbox` are blocked by the instance-role CloudWatch Logs read-policy gap above, not by email-send or clickthrough product logic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| P2       | Search API pagination metadata root fix                   | **CLOSED (Stage 3, 2026-07-20):** The fixed upstream engine remains source-backed at `4bce814e3f45857a51e7345366733f5aa5ff3dd7`, containing fix `8de44a30840c71afda5e09075d6e15b5cf9a43bc`, through the existing `FLAPJACK_DEV_DIR`/source-receipt launcher provenance. At frontend guard-removal commit `0a51f8a9a16b45cbd00651ef4a51c594015135a2`, the complete full-stack acceptance command passed 77/77, the exact live owner test `integration_flapjack_proxy_pagination` passed 1/1 with its three-document/two-page raw metadata and identity assertions, `cd web && npm test -- src/lib/components/InstantSearch.test.ts` passed 18/18, and `cd web && npm run check` completed with 0 errors and 0 warnings. The engine is now the single pagination-metadata owner; fjcloud retains only defensive parsing of explicit `totalPages`/`nbPages` and the one-page fallback. **Stage 1 REACHABLE record (2026-07-20):** fjcloud can exercise the fixed upstream engine through its existing live API-to-Flapjack integration owner, not through the mocked pass-through tests. Mock-seam evidence: `infra/api/tests/common/flapjack_proxy_test_support.rs:28` queues JSON responses with `MockFlapjackHttpClient::push_json_response`, and `infra/api/tests/integration/indexes_test.rs:6967,8188,8232,8276,8312,8351` assert returned bodies equal queued upstream fixtures, so those assertions do not build or run the engine. Live-seam evidence: `infra/api/tests/integration/integration_flapjack_proxy_test.rs` owns `flapjack_base`, `seed_shared_vm`, `create_index_via_api`, and `retry_with_delay`; `scripts/integration-test.sh` launches that integration owner through `scripts/integration-up.sh`. Source provenance resolved via the same Flapjack source helpers used by `scripts/integration-up.sh`: selected Flapjack source `HEAD=4bce814e3f45857a51e7345366733f5aa5ff3dd7`; `git merge-base --is-ancestor 8de44a308 HEAD` passed, proving the selected source contains required fix commit `8de44a30840c71afda5e09075d6e15b5cf9a43bc` (`merge(jul20_10am L2): search pagination truth — engine pagination fix (arm B)`). Launcher provenance proof: `scripts/integration-up.sh` reached `Flapjack provenance: source-build:<receipt>` with receipt `bdb3d9f18040f5d39a0849189832c8215f11629838068d976411310b3393c74b.receipt` under `.local/flapjack-source-receipts/`, then `scripts/integration-down.sh` completed. Operational note: the initial default-launcher failure exposed a stale API binary path, and the harness now uses the package's canonical `target/debug/fjcloud-api` output for both the default startup path and its shell-test coverage. This row tracks the fjcloud side only: keep the frontend guard until the API-layer regression test seeds >1 page, asserts raw `nbPages > 1` via the live integration seam against this fixed engine, and removes the guard's inference override. Exit: API-layer regression test green here against the fixed source-backed engine. |
| P2       | Shared login session for deployed-verify Playwright lanes | CLOSED 2026-07-13 by jul12_pm Lane 4 plus the Wave-2 live proof. The deployed browser bundle at `docs/runbooks/evidence/polished-beta-staging-verify/20260713T013035Z_jul12_pm_stage3_browser_verify/` records 7/7 final pass after rerun, no remaining real bugs, and `secret_hygiene.exit=0`; the old self-inflicted auth-429 false-red class did not recur. Keep the production rate limit unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| P2       | Repo GC completion — worktree and remote-branch manifest  | Narrowed 2026-07-15 by `docs/runbooks/evidence/repo-gc/20260715T043720Z/`: four clean, exact-tip-merged jul12 worktrees/branches (L4, stopped L7, L7b, and the L8 producer) were removed from the committed manifest; dirty L1/L3 worktrees were excluded. Post-cleanup `git branch --no-merged main` has nine rows: preserved `backup/pre_jul12_stage3_main_d5d673d33`, unclassified `batman/jul12_pm_8_batch_closeout_reconcile`, and seven active jul13/jul14/codex/plan refs (`batman/jul13_12pm_3_s1_rc_launch_verdict`, `batman/jul13_9pm_{2,3,4}_*`, `batman/jul14_pm_1_wave3_unshipped_launch_owners`, `codex/jul14-algolia-final-review`, `plan/jul14-algolia-amendments`). `git branch -r --no-merged origin/main` has one active row, `origin/plan/jul14-algolia-amendments`; no remote deletion is approved. Remaining jul12 cleanup is limited to preserving or resolving the two dirty merged worktrees and classifying the original unmerged L8 branch; exit remains zero rows from both exact branch commands after active batches terminate. |
| P1       | Shared-VM teardown leaks DNS records and node credentials | **SHIPPED 2026-07-22 (`jul21_am_1`, merge `29a8649b6`, 3/3 stages) — the leak is closed at the code level; two residuals remain.** One idempotent teardown seam (`routes/admin/vms.rs::teardown_retired_vm_resources:368`) now owns EC2 instance + Cloudflare A record + SSM node key + inventory row and is called from `decommission_vm` (`:507`), and a read-only detector at `GET /admin/vms/orphans` (`services/vm_orphan_reconcile.rs`) proved `orphans_found == 0` against the live fleet with every source `status: scanned` and no `enumeration_incomplete`. **Residual 1 — the reconciler is detect-only:** it reports orphans, it does not delete them, and nothing schedules it; a human still has to read the endpoint. **Residual 2 — instance-only orphan enumeration is explicitly out of scope** and was deferred to a follow-up lane that does not exist yet, so an EC2 instance with no inventory row is still invisible. The load-bearing false-green guard is the state-agnostic instance oracle unit test (stopped/pending/previous-AMI VMs must not classify as orphans), because today's 5-VM fleet does not exercise previous-AMI residue. Original text retained as history: *Authored, reviewed, never dispatched — the leaking code is unchanged, so this recurs on every VM retirement.* The admin `decommission_vm` path (`infra/api/src/routes/admin/vms.rs:434`) deletes only the `vm_inventory` DB row; it does not delete the VM's public Cloudflare A record or its SSM node key. The 2026-07-20/21 prod fleet rebuild consequently stranded **28 dangling public DNS records + 28 live `fj_live_` SSM credentials** (a subdomain-takeover and orphaned-credential surface). Those were swept by hand on 2026-07-21 and re-verified at 0 dangling with all 5 live VMs intact — but the hand sweep fixed the *instances*, not the *code*. Owner: `chats/icg/jul21_am_1_shared_vm_teardown_standardization.md` (parked/unvetted; single lane, independent of the console/engine waves, so it can dispatch any time). Its reviewed shape: promote the existing cleanup function into one idempotent teardown seam owning EC2 instance + Cloudflare record + SSM key + inventory row, add an orphan reconciler, and surface retire/clean in the admin console. Systemic write-up: `chats/suggestions/jul20_9pm_fjcloud_dev__vm_teardown_leaks_dns_and_node_credentials.md`. Exit: a single teardown owner all retirement paths call, plus a reconciler proving 0 orphaned DNS records and 0 orphaned SSM keys against the live fleet. |
| P2       | Live-state probe suite has no data-plane or fleet probe   | **SHIPPED AND HONEST 2026-07-22 (`jul21_10pm_2`, merge `84096fe8a`, 5/5 stages) — narrowed, not closed.** `scripts/probe_fleet_dataplane.sh` (read-only AWS only) plus a `fleet_dataplane` row wired into `scripts/probe_live_state.sh` through its existing `register_raw`/`add_row` owners, registered as a six-site hermetic contract gate in `local-ci --fast`. It satisfies the exit condition's hard part: `PROBE_ERROR` on absent/empty/unknown tokens and `ACTION_REQUIRED` on every un-probed fleet — it cannot render a silent pass. **Why this row stays open:** the probe's first real run (`docs/live-state/20260723T075504Z/`) returns `status: ACTION_REQUIRED, reason=environment_attribution_ambiguous` — it cannot yet attribute VMs to an environment (staging discovery returns an `eu-west-1` pointer with `outcome: missing` alongside the real `us-east-1` one), so it has never actually asserted a live search response. `flapjack_build_identity` still returns `SKIP_NO_CREDS` in both environments. So the guard is honest but not yet *informative*, and the search-response half of the exit condition is unproven. Exit (unchanged, now partially met): a fleet row that resolves environment attribution and asserts a real search response from live VMs. Original framing retained as history: *A guard that cannot fail.* `scripts/probe_live_state.sh` is the project live-state inventory, but its newest snapshot (`docs/live-state/20260722T232028Z/SUMMARY.md`, 17 probe rows) covers only Stripe, SNS, SSM, Cloudflare, RDS, privacy.com and API `/health` — there is **no VM/fleet probe and no search-data-plane probe at all**. The one engine-oriented row, `flapjack_build_identity`, returns `SKIP_NO_CREDS` with `reason=missing_expected_identity` in **both** staging and prod, i.e. it reports a non-failure when it has nothing to compare against. This is the same shape as the failure that let a dead prod search plane certify green for three months (the P0 above), and it is exactly what `CLAUDE.md`'s "a guard that cannot fail is not a guard" rule forbids — probes must not default to healthy on missing or indeterminate state. Exit: a fleet/data-plane probe row that queries live VMs for engine reachability and a real search response, that reports `ACTION_REQUIRED` (never `SKIP`) when identity or reachability is indeterminate. |
| P2       | `local-ci --fast` flake: `validate-bootstrap-env-local`   | **CLOSED 2026-07-23.** The sequential unchanged-clean-HEAD baseline at `abe7aacbfd83867db16ecfe65c8088b25bb0b087` passed `10/10` with 10 zero exits and aggregate `pass=310 fail=0 skip=0`; the re-parallelized unchanged-clean-HEAD proof at `e3a654b4924d839f19fbb988b8587d87d2af6781` also passed `10/10` with 10 zero exits and aggregate `pass=310 fail=0 skip=0`. `validate-bootstrap-env-local` now has one parallel schedule/dispatch path, `RUN_BOOTSTRAP_ENV_LOCAL_SEQUENTIAL` is removed, `web-test` and `rust-test` remain sequential, and bounded fan-out is retained. See `docs/live-state/2026_07_22_local_ci_bootstrap_proof.md` for the timed probe and per-run evidence. |
| P1       | Lane merges are being lost at `batman lifecycle` | **Added 2026-07-26 after four consecutive lanes finished green, pushed, and never merged. This is the batch's top throughput blocker: the work is already paid for and sitting on remote branches.** **CORRECTED 2026-07-26 after reading the lifecycle source rather than the failure symptom — there is ONE blocking mechanism here, not two.** An earlier revision of this row claimed a "self-merge deadlock" in which lifecycle refuses because the owning worker is live. **That is false.** Self-merge is supported by design: `matt_root/batman/lifecycle_engine.py::run_worker_completion` passes `allow_current_worker=True` to both `cmd_finish` and `cmd_queue`, and at `:534-543` it explicitly *defers* cleanup with `error="cleanup deferred: lifecycle caller is the current worker"` when `trusted_paths.is_current_process_worker(...)` is true. The `status: refused_live_worker` line in the `55eac` handoff is the **erase dry-run** reporting that deferral (`matt_root/batman/erase.py:740`), not the merge failing. **(1) The single real blocker is the dirty shared clone.** `matt_root/batman/queue.py::_prepare_source_status_for_merge:281-297` refuses the merge whenever the source repo has any uncommitted change and `allow_source_autosave=False` — which lifecycle always passes (`lifecycle_engine.py:525`), deliberately, so a lane cannot claim shared edits it does not own. That refusal is correct behavior, not a bug. The recurring damage is an **operational-discipline violation, not a code defect, and the rule already exists**: `mike_dev/pp/supervise.md:33` documents this exact failure with the same citations, adds that `queue.py:227-230` enumerates status with `--untracked-files=all` so *untracked* files count too, and states the decisive constraint — "Lane workers cannot clear it (they must not commit files they do not own), so the supervisor is the only actor who can." It already prescribes the fix: never write tick logs into a git repo, leave every touched clone clean, commit with an explicit pathspec (`git -C <clone> add -- <exact/path>`) so a co-resident worker's changes are never swept, and drive `git -C <clone> status --porcelain --untracked-files=all` to empty before ending a tick. So the repair is compliance, not a patch. (Do not "fix" this in `matt/epilogue.py`: its `_writeback_followups_and_priorities` targets the *worker's own worktree*, where epilogues do commit — it is not what dirties the shared clone.) Evidence: `~/.matt/projects/fjcloud_dev-3b47dc7b/jul25_am_3_fjcloud_closeout_gate_recovery.md-55eac3fe/session_handoffs/stage_05/s58_build_lifecycle-still-refused.md` (55eac, exit 1, source dirty at exactly that path); `jul25_am_4` (A3C) and `jul26_12am_4` (L1K) landed only because a supervisor ran `batman merge_worktree` by hand (`0905a6172`, `1cb6cbd0d`). So the merge step currently depends on out-of-band intervention on every lane that hits it. **(2) shared-clone dirtiness.** The same refusal names `source repo has uncommitted changes` for a file no lane owns — `chats/icg/_followups.md`, the supervisor's followups feed — which is dirty in the shared clone right now. Because branch/HEAD and the working tree are clone-global, one uncommitted supervisor file blocks the merge for *every* co-resident lane; the same string appears in ~20 lanes' handoffs going back to May. **(3) merge-as-prose.** A third variant needs no lifecycle bug at all: `jul23_11am_11_replacement_timeline_admin_ui` completed stages 1–6 (`state/project.json: completed_stages [1,2,3,4,5,6]`) and its last stage was "Validate, probe, commit, and push" — the merge lived only in prose under "Ownership and coordination" (`chats/icg/jul23_11am_11_replacement_timeline_admin_ui.md`), conditioned on holding for an L8 RC freeze that never dispatched. No stage owned the merge, so the lane terminated green and the branch orphaned. **Correction 2026-07-26 to the mechanism-(2) prescription:** an earlier revision of this row said to "give `_followups.md` a recurring committed home". That is a no-op — the file is already tracked in both repos (`chats/icg/_followups.md`, 436K in fjcloud, 224K in flapjack). The actual mechanism is that supervisor tooling *regenerates and appends* it mid-run (`Refresh supervisor followup feed`, `Refresh Flapjack followup feed`, `chore: refresh follow-up queue`), so the shared clone is dirty at unpredictable moments between refresh and commit — and any lane attempting lifecycle in that window is refused for a file it does not own. Standing repairs, in order: **(a)** make the feed refresh write-and-commit atomically, or have lifecycle's dirtiness check exempt that exact path, so the window cannot exist; **(b)** every checklist that ends in a merge must carry an explicit `[merge]` stage — never a prose note, and never a merge gated on a sibling lane that has not dispatched; **(c)** lifecycle needs an owner-PID-aware self-merge path (a lane's own PID must not count as a foreign live worker) or the merge must be dispatched as a separate short lane the worker cannot own. **STATUS 2026-07-27 — measurably better, and the residue is disposition, not loss.** All six `jul26_pm_11` console lanes plus `jul27_am_4` reached `main`. The fjcloud unmerged set is now five branches and each has a verified reason NOT to merge, so the standing recommendation is **prune, not merge**, for all five: `batman/jul26_12am_2_real_pipeline_browser_fixture` (37 ahead / 382 behind — superseded by its own Stage-1 cut `c627736f7`, already merged; `chats/icg/jul26_pm_7_local_prelaunch_completion_orchestration.md` marks it "not a restart candidate" because ~2,580 of its 8,578 lines are a second coordination mechanism duplicating the batch lease; preserve for future extraction only `local_real_pipeline_stack.sh::{lrp_start_booted_browser_oracle_stack,lrp_select_browser_oracle_stack}` with `playwright_port_plan.sh::lrp_resolve_browser_oracle_port_plan` plus their existing tests, without merging the stale branch); `batman/jul25_am_3_fjcloud_closeout_gate_recovery` (14 ahead / 297 behind — `git diff origin/main <branch>` is **-24,607 lines**, so merging it would revert the entire `jul26` wave; preserve only `docs/runbooks/evidence/20260726_fjcloud_closeout_gate_recovery.md`, without merging the stale branch); `batman/jul25_am_2_ses_dispatch_cause_preservation` (superseded — `map_send_email_error` with `DisplayErrorContext` is on `main` at `infra/api/src/services/email.rs:284-290` via the merged L1K recovery `1cb6cbd0d`; the residual delta is a test-style refactor only); `batman/jul24_pm_6_staging_aws_sdk_dispatch_recovery` (evidence/diagnosis only, superseded by the merged `jul25_am_1` probe); and `batman/jul13_12pm_3_s1_rc_launch_verdict` (operator-ABANDONED 2026-07-16; residual delta is DIRMAP churn). **RECURRENCE 2026-07-27 (late) — the "prune, not merge" status above is now PARTIALLY FALSIFIED; read this before acting on it.** The prune verdict remains correct for those five historical branches, but it described a snapshot, not a fixed rate. `batman/jul27_pm_4_test_wiring_reachability_gate` — the `jul27_pm_0` batch's **cross-cutting-invariant owner lane**, on which every sibling lane's test-wiring contract routes — finished 3/3 stages green at 18:58, pushed 22 commits, and was never merged, while its four Wave-1 siblings all reached `main` normally. It carried real deliverables no sibling had: `scripts/probe_test_reachability.sh`, `scripts/lib/test_reachability_manifest.sh`, the `test-reachability-contract` gate, `scripts/tests/manual_only_tests.txt`, `scripts/tests/quarantined_tests.txt`, and `docs/audits/test-wiring/20260727T193923Z_reachability/SUMMARY.md`. It was merged by hand into `main` at `dd10a4231` on 2026-07-27; the sibling `scripts/local-ci.sh` conflicts were resolved by keeping ALL gates, exactly as the orchestration prescribed. **This is the sixth instance and the first where the stranded lane was the batch's own invariant owner — so the loss rate is not decaying, and the batch's most load-bearing lane is not protected from it.** **The old exit condition had a hole that would have let this exact lane through:** it required the branch be `conflict-free by git merge-tree`, and `jul27_pm_4` was NOT conflict-free — it had six conflict hunks in `scripts/local-ci.sh`. Those conflicts were *designed in*: `chats/icg/jul27_pm_0_release_confidence_orchestration.md` declares them "EXPECTED and benign" because three Wave-1 lanes each add one `gate_*()` and one `schedule` line to the same file, and prescribes the resolution (keep all gates). A merge-loss detector that skips every conflicted branch is blind to precisely the parallel-batch shape this project runs. **Standing repair (d), added here:** the detector must classify `conflicted-but-prescribed` as a *merge candidate needing a resolver*, not as an exclusion — a lane whose orchestration names its expected conflicts and their resolution is mergeable work, not a blocked branch. **A SECOND, MORE DAMAGING MECHANISM was observed live during that hand-merge and is NOT the dirty-clone refusal this row already documents — it is the inverse.** At 2026-07-27 19:10:34 an out-of-band `matt`/`batman` process ran `git reset --hard origin/main` against the shared primary clone while a staged, uncommitted merge was in the index, silently destroying the merge, its conflict resolution, and an unrelated uncommitted `ROADMAP.md` edit. `git reflog` records it as `reset: moving to HEAD`. **The mechanism is `matt_root/batman/git.py::safe_primary_sync`:** before hard-syncing it probes only `probe.is_ahead` — whether `origin/main..main` contains local *commits* — and refuses on that alone (`git.py:570-578`). **A staged-but-uncommitted merge is not "ahead" by that definition**, so the guard passes and `reset --hard origin/main` (`git.py:580-586`) runs. Uncommitted work has no protection whatsoever: no `git status` check, no `MERGE_HEAD` check, no index check. So the merge pipeline both *fails to merge* lanes (dirty-clone refusal, above) and *destroys* hand-merges performed to recover from that refusal — the recovery path is unsafe for the same reason the primary path is. **A THIRD variant hit this session's own repair work at 21:05-21:20, after repair (e) had already shipped, which is how we learned (e) is necessary but not sufficient.** A supervisor tick ran a BLANKET `git stash` over the whole fjcloud clone — not `reset --hard` — to clear the way for a lane dispatch, sweeping up in-flight edits it did not own (`scripts/local-ci.sh`, `scripts/tests/probe_test_reachability_test.sh`, and an untracked `scripts/tests/serial_only_tests.txt`), then committed four lane-authoring commits on top. **Credit where due: this variant PRESERVED the work** — the stash was labelled `shared-clone residue 20260727: local-ci reachability gate work (rescued so jul27_pm_9 could dispatch)`, and `git stash pop` restored all three files intact, contract test still 77/77. That is strictly better than the `reset --hard` variant, and the honest label is what made recovery a 30-second operation. **But it is still a sweep of another actor's uncommitted work**, and the repo rule it violates already exists: `CLAUDE.md` § Shared-Clone Git Safety says to stash with an explicit pathspec (`git stash push -u -- <your paths>`) precisely so a co-resident worker's changes are never swept. A worker mid-edit would have had its working tree change underneath it with no signal. **Standing repair (g), CORRECTED 2026-07-27 after looking for the code instead of assuming it — the first draft of this repair pointed at a defect that does not exist.** It said "supervisor tick code that clears a clone must stash with an explicit pathspec." **There is no such code.** The only `stash` in all of `matt_root` and `pp` is `git stash create` in `batman/git.py::_probe_worktree_numstat`, which is read-only — it builds an object and touches neither the worktree nor the stash stack — and `batman/queue.py::_refuse_source_mutation_without_autosave` only *prints* advice, already pathspec-scoped, already carrying a code comment forbidding a bare `git add -A`. The sweep was a **hand-run command by a supervisor agent overriding correct guidance**, which puts it in the same class as mechanism (2) above: compliance, not a patch. Writing a scoping guard for a non-existent call site would have created a `ROADMAP.md` row owning a phantom defect — the precise failure this batch was built to stop, committed by this batch's own closeout. The enforceable lever is the guidance itself, at the last point anyone reads before clearing a clone. **Done 2026-07-27** at `mike_dev 57b2e8d81`: the refusal now states that a bare `git stash` sweeps every co-resident's uncommitted work, with four regression tests for an arm that previously had none, and a verified live specimen (deleting the warning reddens the test). **Residual, and it is the real one:** this only helps an actor who reads the refusal. The durable fix is removing the pressure that makes sweeping attractive — repair (a), the dirty-clone window — not policing the sweep. **Standing repair (e), added here:** `safe_primary_sync` must refuse when the target clone has a dirty worktree, a non-empty staged index, or `.git/MERGE_HEAD` present, and it must say which of those it found; the ancestry probe alone is not a sufficient precondition for a destructive reset. **Operational rule until (e) lands:** any hand-merge into a shared clone must be committed immediately after conflict resolution — never validated while uncommitted — because `local-ci.sh --fast` takes minutes and the reset can land inside that window. That is exactly how this instance was lost, and the merge was redone and committed before revalidating. **SEVENTH INSTANCE, found while running this batch's own closing proof, and it is a worse shape than the other six: a never-pushed LOCAL branch holding half of a cross-repo contract.** `batman/jul27_pm_2_m1e_shared_migration_wire_lifecycle` is the fjcloud half of a lane whose flapjack half merged at `flapjack_dev faf4f89da` on 2026-07-27. The fjcloud half existed only as a local branch — never pushed, never merged — so `infra/api/tests/fixtures/algolia_migration_engine_contract.json` and its consumer test were pinned to a different engine contract than the one flapjack shipped, **while both repos' suites read green**. Merged at `c274eb533` after validating rather than assuming: conflict-free, and the contract suite is hermetic (`include_str!`, no engine binary), so it was run on the merged tree — `cargo test -p api --test platform algolia_migration_engine_contract` → `ok. 10 passed; 0 failed`. **This exposes a blind spot in this row's own exit condition, which greps `git branch -a --no-merged origin/main` — a never-pushed local branch does not appear there at all.** It surfaced only because `closeout_artifact_check.py --roster` happens to enumerate local branches. **Standing repair (f), added here:** the unmerged-lane detector must enumerate LOCAL branches as well as remote ones, and cross-repo lanes sharing a stem across two repos must be checked as a pair — a lane whose sibling repo merged its half while this repo did not is a split contract, not a pending merge, and should be reported at higher severity than an ordinary stranded branch. **Second finding from the same merge — line-number pointers are not durable owners.** `scripts/tests/quarantined_tests.txt` cited its owner as `ROADMAP.md:141`, and `scripts/probe_test_reachability.sh::reason_has_owner` validates that citation by reading that exact line. The merge shifted the row, so all 20 quarantine entries silently failed owner validation and fell through to `unaccounted`, taking the probe from exit 0 to exit 1 — a gate reporting a defect that did not exist. The pointers were repaired to the merged line, but **a gate keyed to a line number in a file other lanes edit will re-break on the next ROADMAP change**; it should key on a stable row title or an anchor id instead. **EIGHTH INSTANCE, 2026-07-28, and it closes the loop between this row and the "no lane isolation" deviation — they are one defect seen from two ends.** `jul27_pm_8_public_support_routing_discoverability` (worker `50185`) shows `merged` in `batman status`, but `git branch -a --list '*jul27_pm_8*'` is **empty**, `git worktree list` has **no** entry for it, and `git log --first-parent` shows its ~15 stage commits sitting directly on `main` with no merge commit. The worker built in the shared canonical clone. `jul27_pm_5` did the same thing on 2026-07-27 and the `jul27_pm` closeout called it a "mirror-image deviation" from `jul27_pm_4`'s over-isolation — but it is not merely a mirror image, it is the **cause** of the other end: a worker building in the shared clone holds that clone dirty for the entire run, and `queue.py::_prepare_source_status_for_merge:281-297` refuses every co-resident lane's merge while it is. Mechanism (2) above blames "the supervisor's followups feed" for the dirty clone; that is one source, and an unisolated lane worker is a far larger one, dirty for hours instead of seconds. **Standing repair (h), added here:** the detector must also catch the zero-branch case — a lane whose `batman status` says `merged` while no branch and no worktree ever existed for its stem is not a clean merge, it is an unisolated run, and it must be reported at the same severity as a stranded branch. `git branch --no-merged` can never see it, because there is nothing to see. Exit: `git branch -a --no-merged origin/main` contains no `batman/*` branch that is simultaneously stage-complete and unmerged for more than 24h, **whether or not `git merge-tree` reports conflicts** — a conflicted branch whose orchestration prescribes the resolution counts as unmerged work, not as an exclusion — **and** every lane the status view reports as `merged` has either a merge commit or a recorded `Disposition:` explaining why none exists. |
| P1       | Console browser-coverage residuals (batch close in flight) | **Added 2026-07-27 and CORRECTED the same day — read the correction first.** An earlier revision of this row claimed the batch's gate "closed red at 18/19" and was "arithmetically unreachable." **That claim was WRONG and is withdrawn.** The target at `docs/audits/ui_test_coverage_p0_ledger.md:77` is 19 semantically valid **tags**, not 19 test declarations: `grep -roh '@p0_coverage' web/tests/ \| wc -l` returns **19** across 18 declarations, because `web/tests/e2e-ui/full/console_replica_lifecycle.spec.ts:36` legitimately closes rows 16 and 17 in a single declaration. `chats/icg/jul27_pm_1_console_customer_quality_closeout.md` carry-forward 3 states this explicitly and forbids splitting that declaration to inflate the count — **do not split it.** The count gate is MET; semantic validity is being assessed by worker `cc118` running that closeout lane. **Three real residuals survive and need named owners at batch close:** (1) row 8 (`web/tests/e2e-ui/full/billing_portal_payment_method_update.spec.ts:210`) self-skips whenever `stripeCustomerId` starts with `cus_local_`, so it executes only under `API_DEV_ALLOW_LIVE_STRIPE=1` and does not run under the default local Stripe mock — a conditional-execution gap, not a proof; (2) "zero skips" is **not locally satisfiable and must never be written as a gate**: `web/tests/e2e-ui/full/auth-end-effects.spec.ts:66` skips on every local run because `web/playwright.config.contract.ts:625-633` unconditionally forces `ENVIRONMENT=local` + `SKIP_EMAIL_VERIFICATION=1` + `API_DEV_ALLOW_SKIP_EMAIL_VERIFICATION=1` onto the locally spawned stack, and `web/tests/e2e-ui/full/console.spec.ts:526` self-skips for want of a payment-method-free shared-plan customer; (3) `auth-end-effects.spec.ts:17,107` fail whenever Mailpit is not running — a stack-ownership gap that the closeout lane still works around with a manual "start Mailpit first" instruction rather than an owned automated step. **Stage 3 screen-spec reconciliation note, 2026-07-28:** the focused Vitest claim proofs pass, but the focused Playwright proof for analytics/events/overview remains incomplete because the corrected local-stack run timed out on blank-page analytics cases before reaching the full selection; do not cite those Playwright files as refreshed Stage 3 behavior proof until this row's browser-stack owner reruns the same focused selection green. Exit: `chatting/jul26_pm_11_console_customer_quality_closeout.md` exists, `closeout_artifact_check.py --roster` exits 0, each of the three residuals carries a named owner row here, and the focused analytics/events/overview Playwright selection has a recorded green rerun before those files are cited as refreshed Stage 3 behavior proof. |
| P1       | Routine-local skip integrity audit follow-up | **Added 2026-07-27 from `docs/audits/test-integrity/20260727T160044Z_skip_classification/SUMMARY.md`.** Six catalog rows always skip under the routine local baseline: B1/B2/B7/B10/B21/B36; the non-empty P0 subset is B2 `web/tests/e2e-ui/full/billing_portal_payment_method_update.spec.ts:222`, B7 `web/tests/e2e-ui/full/upgrade_to_shared_unmocked.spec.ts:22`, and B21 `web/tests/e2e-ui/full/auth-end-effects.spec.ts:91`, so counted `@p0_coverage` overstates proven routine-local coverage by 3 rows. Exit: ``python3 -c 'import pathlib,sys; text=pathlib.Path("docs/audits/test-integrity/20260727T160044Z_skip_classification/skip_classification.md").read_text(); rows=[(p[1].strip(),p[2].strip(" `")) for p in (l.split("\x7c") for l in text.splitlines()) if len(p)>5 and p[1].strip() in {"B1","B2","B7","B10","B21","B36"} and p[5].strip().strip("`")=="always-skips-locally"]; live=lambda a: "test.skip(" in "\n".join(pathlib.Path(a.rsplit(":",1)[0]).read_text().splitlines()[max(0,int(a.rsplit(":",1)[1])-9):int(a.rsplit(":",1)[1])+8]); open=[f"{r} {a}" for r,a in rows if live(a)]; print("always-skips-locally=0" if not open else "\n".join(open)); sys.exit(0 if not open else 1)'`` prints exactly `always-skips-locally=0`; until then it prints remaining row IDs with `file:line`. Static closure only proves anchors/signatures are gone; any claim that a row now executes locally must cite a separate recorded validation command. |
| P1       | Local migration ledger can record a migration as applied without running it | **Added 2026-07-27 from `chats/icg/jul27_am_4_console_payment_method_end_effect_completion.md`, which found `_schema_migrations` claiming `051_customer_subscription_cycle_anchor.sql` applied while `public.customers` lacked the column.** A hand-audit reporting coverage of "all 30 `ADD COLUMN` statements" found exactly three drifted columns — `billing_mode` (`014_billing_mode.sql`), `quota_warnings_sent` (`050_quota_warnings_sent_jsonb.sql`), and `subscription_cycle_anchor_at` (`051`), all on `customers`. **That audit was incomplete:** re-measured 2026-07-27, `infra/migrations/` holds **51** `ADD COLUMN` occurrences across 24 files, and the pass checked no `CREATE TABLE`, `CREATE INDEX`, or constraint DDL at all — so treat those three columns as a sample, not an inventory. They were re-applied idempotently to the local database, which fixed the symptom and not the cause. **Mechanism (verified 2026-07-27):** `scripts/lib/migrate.sh:59-76` treats any database with user tables but an empty `_schema_migrations` as a "pre-tracking volume" and seeds **every** migration filename as applied **without executing any of them**, swallowing all errors with `>/dev/null 2>&1 \|\| true` at `:73`. The script's own comment at `:54` concedes the consequence: "Adding a new migration to a legacy volume requires --clean." There is no checksum or post-apply verification, so a filename recorded as applied is never re-checked. **Scope is local only — production and staging are NOT affected:** `ops/scripts/migrate.sh:43` and CI both use `sqlx migrate run`, which keeps its own `_sqlx_migrations` table with checksum validation. The exposure is `scripts/local-dev-up.sh:191`, `scripts/local-dev-migrate.sh:46,63`, `scripts/integration-up.sh:116`, and `scripts/playwright_local_stack.sh:274` — i.e. the local dev database and every local integration and browser-test stack. That matters here specifically because the automated suite is the only QA process: a drifted local schema makes tests fail for the wrong reason or pass while proving nothing. **Ruled out:** post-application edits to those migration files (`git log` shows exactly one commit each, never modified). A second instance of the same class sits alongside it: `scripts/tests/migrate_test.sh` exists but is invoked by **no** gate or runner, so the migration helper's only test can never fail. Owner: `chats/icg/jul27_pm_3_local_schema_drift_detector.md`. Exit: `bash scripts/tests/probe_local_schema_drift_test.sh` passes hermetically, the `local-schema-drift-contract` gate is registered in `scripts/local-ci.sh` and runs `migrate_test.sh` too, `bash scripts/probe_local_schema_drift.sh` exits 0 against a live local database, and a deliberately dropped column makes that probe exit non-zero. |
| P1       | Five merged fjcloud batches have no closeout artifact     | **UPDATE 2026-07-29 — of the three `jul26` batches named below, only `jul26_pm_7` still owes a closeout.** `jul26_12am_0` now has `chatting/jul26_local_migration_scale_closeout.md`, carrying a `Disposition:` line for each of its 23 rostered lanes, exactly one `Ship disposition:` line (`not-shipping`, with an unshipped-inventory owner), and `closeout_artifact_check.py --roster` exit 0 — so it no longer lacks either a closeout or lane dispositions. `jul26_pm_11` is routed by its own existing `chatting/jul26_pm_11_console_customer_quality_closeout.md` and is not this row's debt. `jul26_pm_7` remains open. **Original 2026-07-27 text retained for provenance:** the five artifacts named below NOW EXIST and must not be re-authored; the debt moved to the `jul26` batches (`jul26_12am_0`, `jul26_pm_11`, `jul26_pm_7`), which at that time had merged lanes and no `chatting/*_closeout.md`. `jul25_pm` closed cleanly at `chatting/jul25_pm_correctness_refugee_migration_polish_closeout.md`, which is the proof the pattern is fixable, not inherent. **UPDATE 2026-07-24 — the `jul23_11am` launch/security/autoheal batch is now a fifth uncloseout batch, and it is the worst instance yet.** Its L9 (`jul23_11am_9_batch_closeout_and_roadmap_reconcile`) never dispatched, so the batch has no closeout, no per-lane `Disposition:` stamps, a still-stale ROADMAP (this very revision is a hand-reconcile standing in for L9), no new `LAUNCH.md ## STATUS` entry, and the orchestration is still registered active at `mike_dev/pp/supervise.md:12`. The concrete cost, again measurable in this file: the §1 P0, engine-exposure P0, and Algolia rows were all materially wrong until this manual sweep, and the L11 admin timeline UI silently sat unmerged. **Not bookkeeping — this is why stale claims keep reaching dispatch reasoning.** `jul20_4pm` (console/operator-visibility), `jul20_9pm` (console prep), `jul21_10pm` (reliability) and `jul22_5pm` (engine exposure) all have merged, working lanes on `origin/main` and **no** `chatting/*_closeout.md`, so `closeout_artifact_check.py --roster` returns non-zero for every one of them and no lane roster carries a `Disposition:` stamp. The concrete cost is measurable in this very file: the teardown P1 read "never dispatched" for a day after it shipped, the §1 P0 named two defects that no longer occur, and the console P3 said three screen specs "do not exist" while they were on `main`. The reconciliation step is what converts merged code into a true ledger, and it is the step that gets dropped when a batch runs out of budget. **The one authored owner is `chats/icg/jul23_am_4_reliability_batch_closeout_continuation.md`** (covers `jul21_10pm` only), and it is gated on F10 because both touch `ROADMAP.md`. The other three batches have no closeout owner at all. Exit: a closeout artifact per batch with a roster-complete disposition line for every lane, plus `closeout_artifact_check.py --roster` exit 0. |
| P2       | Staging aggregation job is not producing `usage_daily` rollups | **UPDATE 2026-07-24 — repaired at the code level, proven fresh once by hand, not yet durable; the §1 P0 row above carries the full receipt.** The running-fleet root cause was found and hand-fixed: staging metering agents were pointed at PROD `api.flapjack.foo`, Flapjack `/metrics` returned 403, and the synthetic seeder was selecting prod-VPC nodes; manual convergence of 6 nodes plus a hand-run aggregation job yielded one fresh row (2026-07-24T09:19:25Z). The durable code fixes (env-aware metering-URL derivation + admin-key persistence in `ops/user-data/bootstrap.sh` / `cloud_init.rs`) are merged but apply only to **newly provisioned** VMs; the systemd timer was never proven to self-produce, the staleness alarm is unmerged, and the latest live-state still reads `ACTION_REQUIRED`. Split out 2026-07-23 from the §1 P0 above so the pipeline defect has its own owner rather than living inside a launch-harness row. Evidence: `docs/runbooks/evidence/ses-coverage-a1/20260722T204849Z_jul16_10am_s1_rerun/staging_dunning_delivery.log` — `classification=rollup_stale`, `usage_daily has no recent rollups (0 rows within 48h)`. Everything upstream of it is green: the rehearsal preflight passes all eleven configuration checks and staging deploy currency is `deployable_drift=false` at `9d521e1c9`. Candidate owners in order of cheapness to check: the `infra/aggregation-job` systemd timer state on the staging API host, then whether metering rows are arriving at all (a dead `metering-agent` would starve the aggregator rather than break it), then the job's own error path. **Do not diagnose this from the §1 runner** — the runner is correctly reporting someone else's outage. Note the shape: this is the same class of failure as the dead prod data plane — a background job silently stopped producing and nothing alerted, which is also what the fleet/data-plane probe row above exists to prevent. Exit: `usage_daily` rows within 48h on staging, a rerun `staging_billing_rehearsal` reaching `rehearsal_completed`, and a probe or alarm that fails loudly on rollup staleness. |
| P1       | `jul23_11am` batch — undispatched launch/security successors + one built-but-unmerged UI lane | **Added 2026-07-24 to give the batch's stranded tail an explicit owner (its L9 reconcile never ran).** Six lanes were authored and **never dispatched**, and their dispatch is the shortest path to a launch verdict: **L7** (`jul23_11am_7`, §1 bundle rerun — gated on the non-existent `S1_BLOCKER=discharged` receipt), **L8** (`jul23_11am_8`, RC verdict + `LAUNCH.md` anchor — the announce-gate owner, still un-run so no verdict exists since 2026-07-22), **L9** (`jul23_11am_9`, batch closeout + ROADMAP reconcile), **L10** (`jul23_11am_10`, engine TLS endpoint switch — see engine-exposure P0), **L10V** (`jul24_am_3`, endpoint-switch live proof), **L12** (`jul23_11am_12`, enabled autorepair replacement proof). Separately, **L11** (`jul23_11am_11`, replacement-timeline admin UI) is **built 6/6 on `batman/jul23_11am_11` (+22 ahead of `main`) and NOT merged** — real frontend (`962e7d4d4 Render admin VM lifecycle timeline`, browser tests) that renders L3's `vm_lifecycle_events`; `main`'s `web/src/routes/admin/fleet/[id]/+page.svelte` has no `lifecycle` reference. Disposition needed: rebase L11 onto current `main` and merge (the backend it consumes is already on `main`), or explicitly abandon. **RESOLVED 2026-07-26 — MERGED to `main` via recovery lane `jul26_am_12_replacement_timeline_ui_recovery` (worker `b0aea`, 3/3, 57/57), whose branch carried the stranded UI at merge `245ed9dc7`; landed here as `3d6b8a5f0` with 26 files, +1753/-368, and `local-ci --fast` green. The diagnosis below is retained because the *mechanism* that stranded it is still open. Prior status, now historical — "still unmerged, still conflict-free, three days stale; needs no rebase":** `git merge-tree --write-tree origin/main batman/jul23_11am_11_replacement_timeline_admin_ui` returns a tree with zero conflicts against current `main`, and `git cherry -v origin/main <branch>` shows all 21 commits still unmerged, including `962e7d4d4` (render), `108a3f68b` (load), `fec204cc7` (browser coverage). Its 2026-07-24 root cause is now diagnosed and is *not* a rebase problem: the lane had no `[merge]` stage — see the new "Lane merges are being lost at `batman lifecycle`" row. **Its re-homing made this worse, not better:** `chats/icg/jul26_12am_0_local_migration_and_scale_closure_orchestration.md` re-homes it as L18 in Wave 3, gated on L11-real-pipeline (unauthored) which is itself gated on L2 (`jul26_12am_2`, an 11h/$116 runaway), because both would touch `fleet.spec.ts`/fixtures. A conflict-free, 6/6-complete, +1450-line merged-backend consumer is therefore queued behind two lanes that do not exist yet. Recommended disposition: merge it now as its own short lane ahead of Wave 2B and make the later fixture lanes consume the merged result, rather than the reverse. Exit: L8 lands a fresh RC verdict in an allowed shape with a matching `LAUNCH.md ## STATUS` entry; L11 is merged or stamped abandoned; the orchestration is deregistered from `mike_dev/pp/supervise.md`. |
| P3       | Reintroduce Linux-Playwright Firefox/WebKit               | CI is Chromium-only after the 2026-05-02 cutover; reintroduce if customer signal shows Firefox/non-Safari-WebKit gaps.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| P3       | Console unification program (post-launch)                 | One Svelte console for the managed cloud and the standalone OSS binary; retire the React `engine/dashboard` after a parity gate. Program SSOT: `docs/design/console_unification_revised_plan.md` (v7, 2026-07-18) — all four operator decisions are settled there, including the 2026-07-18 source-home decision: the shared console will live in **flapjack_dev** and fjcloud will consume a pinned flapjack revision (ADR: flapjack_dev `engine/docs2/3_IMPLEMENTATION/decisions/active/0006_console_source_home.md`). Not on the launch path. Start gates per the plan's sequencing section: Algolia fjcloud web-touching waves complete before P1/P2; E7 merged on flapjack `origin/main` before P4. Immediate residue (**RE-VERIFIED 2026-07-22 — the prior "4 missing analytics paths" wording is STALE**): all 9 analytics `#[utoipa::path]` handlers ARE already registered in `infra/api/src/openapi.rs:102-110` (9 defined == 9 registered). The genuinely-unregistered handlers are **8 Algolia migration-job + shared-upgrade routes** (`{create,get,list,cancel,resume}_algolia_import_job`, `check_algolia_destination_eligibility`, `get_upgrade_status`, `upgrade_to_shared`), which are entangled with the **active Algolia migration program** (P1, F9 in flight) and its deliberately-gated availability + deferred-resume-is-a-defect rule — so their OpenAPI registration is routed to that program's owner (`chats/icg/jul13_9pm_0_algolia_migration_reliability_orchestration.md`), NOT a standalone console-prep task. Regenerate with `UPDATE_OPENAPI_ARTIFACT=1` only when that owner registers them. **Console-prep batch status (`jul20_9pm`, merged 2026-07-20) — 3 of 4 design deliverables shipped:** `docs/design/console_transport_mapping_catalog.md` (476 lines, per-surface transport derivation web→proxy→engine), `docs/design/console_core_extraction_map.md` (228 lines, neutral-vs-billing-coupled `$lib` split for P3), and `docs/design/console_bff_proxy_contract.md` (293 lines, `/console-api/*` treated as a credential-injecting SSRF boundary). **The fourth did NOT ship:** lane B3 (`jul20_9pm_4_ops_surface_screen_specs`) merged its own checklist file and nothing else — 0/2 stages, 1/27 items, $8.45 — so `docs/screen_specs/{system,cluster,snapshots}.md` **do not exist**. Its own closeout stamp reads "deferred — completeness FAIL (deliverable ABSENT) — merged per operator no-cherry-pick directive — NOT shipped; requires a continuation lane." **RESOLVED 2026-07-22 — the continuation ran.** `chats/icg/jul20_9pm_4b_ops_surface_screen_specs.md` (2/2 stages, `needs_review`) landed all three specs at `4838a70f3`: `docs/screen_specs/system.md` (102 lines), `cluster.md` (95), `snapshots.md` (105), all present on `origin/main` and indexed in `docs/screen_specs/coverage.md`. **R5 (System/Cluster/Snapshots parity build) is no longer doc-blocked.** Its remaining gate is the program's own sequencing rule — Algolia fjcloud web-touching waves complete before P1/P2 — which is currently held by F10. |

- **DictionariesTab size gate returned to the default owner.** Owner: `scripts/check-sizes.sh`; the captured roadmap-v2 title `DictionariesTab size override remains.` is now historical, not the current state. The 2026-07-07 EditorDialog split removed both EditorDialog `PER_FILE_OVERRIDES` caps, and the later size-gate cleanup also removed the stale `DictionariesTab.svelte|800` override. `web/src/lib/components/DictionariesTab.svelte` is now governed by the normal `.svelte` fallback limit; the remaining temporary Svelte override is the index-detail shell extraction cap in `PER_FILE_OVERRIDES`.
- **jul07_3pm release-batch parked lanes are narrowed.** Owner: `chats/icg/jul07_3pm_0_dev_velocity_refugee_delight_orchestration.md`; parked lanes, updated 2026-07-13: Playwright `workers > 1` remains deferred behind evidence of benefit over the now-green `workers: 1` baseline, lane 15 (staging billing rehearsal rerun) is closed by `docs/runbooks/evidence/staging-billing-rehearsal/20260712T010555Z_current_main/`, and lane 17 (`e2e-deployed` failure paging) landed through the deployable-currency SSOT below. Lane 12 (settings sub-tabs part 2: Language & Text, Facets & Filters, Display) is already merged into the jul09 release-batch state; do not treat it as a built-but-unmerged terminus lane.
- **SES bounce/complaint live probe rerun remains open.** (Captured roadmap-v2 title — historical, kept for shape invariants; the probe is GREEN as of 2026-07-11.) Owner: `scripts/probe_ses_bounce_complaint_e2e.sh`; current clean-env bundle: `docs/runbooks/evidence/ses-inbox-canary-clean-env/20260711T014655Z/SUMMARY.md`. The fresh rerun hydrated the staging DB URL through SSM, RAN bounce and complaint against live staging, and exercised `poll_sns_side_effects`, second-send suppression, audit rows, and cleanup with exit `0` for both owners.
- **Account-retention automation implemented.** Owners: `docs/runbooks/account_data_policy.md` and the `infra/retention-job` crate (deployed via `ops/systemd/fjcloud-retention-job.timer`); it runs the automated retention-duration cleanup loop for `list_deleted_before_cutoff` candidates and delegates erasure to `POST /admin/customers/:id/hard-erase`.
- **AWS live-infra E2E current-main wrapper evidence passed.** Owner: `scripts/launch/live_e2e_evidence.sh`; evidence: `docs/runbooks/evidence/live-e2e-guardrails/20260612T033222Z_current_main/fjcloud_live_e2e_evidence_20260612T033222Z_79205/summary.json` (`overall_verdict=pass`, runtime-smoke and billing rehearsal checks pass, no external blockers). Credentialed destructive billing/webhook capture remains a future option via the wrapper's existing live-mutation path. Operational detail owned by `docs/runbooks/aws_live_e2e_guardrails.md`.
- **Staging billing rehearsal current-main rerun remains open.** (Captured roadmap-v2 title — historical; RESOLVED 2026-07-12 by the current-main live rerun.) Owner: `scripts/staging_billing_rehearsal.sh`. Canonical fresh proof is `docs/runbooks/evidence/staging-billing-rehearsal/20260712T010555Z_current_main/`: `deploy_status_before.json` records staging current for `dev_sha=ae06b99a96bc9d02f02e5fe4c930379f54270cd1` with `deployable_drift=false`, `summary.json` is `passed` / `rehearsal_completed`, and `invoice_email.json` proves invoice-email dispatch on the reset-first current-month path with `emails_with_messages=1` and `missing_invoice_ids=[]`. The deployable-currency SSOT fast-follow remains landed at `docs/runbooks/evidence/deployable-currency/20260710T213440Z/`, and the reset-first rehearsal preflight half is now closed by this live rerun.
- **Unmerged lane disposition (re-verified 2026-07-10 — every branch below re-checked with `git merge-base --is-ancestor` and `git cherry -v main`).** ABANDON, all confirmed unmerged and carrying no salvageable product code: `batman/jul08_am_1_rc_shape_drift_investigate_and_rerun` (stopped 0/5 stages; its one product diff re-adds an `onclick` handler to `PaymentMethodSetupForm.svelte` that main **deliberately rejected** — main carries an explicit "Do NOT reintroduce onclick / capture-phase click+pointerdown listeners here" comment, so merging would regress main; its `ops/terraform/dns/main.tf` change is byte-identical to main's and the branch is otherwise _behind_ main), `batman/jul08_pm_1_rc_rerun_produce_clean_bundle` (4 unique commits, 214 files, essentially all `DIRMAP.md` noise), `batman/jul09_am_6_billing_rehearsal_real_run` (evidence/DIRMAP/checklist only), and the `polished-beta-staging-verify` rerun4/rerun5 branches. **`batman/jul07_3pm_14_playwright_parallel_workers` is now ABANDON, not SALVAGE (correction).** Its fixture/spec-ownership refactor **already reached main independently** via `5256ad368`, `159053c14`, `a74928abe` (`web/tests/fixtures/fixtures.ts`, 4660 lines, full disposable-fixture machinery). Note the salvage lane `jul09_pm_4` did **not** carry that refactor — it landed only evidence, DIRMAP, config, and `scripts/lib/stripe_request.sh`; the refactor was re-authored on main under a different spec-naming scheme (main: `account-delete.spec.ts` / `auth-end-effects.spec.ts`; branch 14: `public-` prefixed). Branch 14's sole remaining unique delta is the `workers: 4` flip, which failed its own Stage-5 3-green-runs gate. Nothing left to salvage. **Full-sweep extension (2026-07-11):** every remaining unmerged branch beyond the five above — 25 branches plus the stray `stage3-pages-parity-publish` worktree — was classified via `git cherry -v main` and three-dot product diffs: 13 carry content that already landed on main (main's re-authored version is newer where it matters), 12 are evidence/checklist-only, **zero orphaned product code — nothing to merge**. The only unique un-landed content anywhere is planning prose on `prep/post-5d631-queued-edits` (a deploy-latency profiling idea and a shared-login verify-harness idea; its third row, the search-preview pagination undercount, already landed via `bfe5e7223`/`3cca4b672`/`c9eedd2dc`). Every listed branch/worktree is safe to delete under the same ABANDON basis. **Remote extension (2026-07-12):** the 16 unmerged `origin/*` branches and the 3 local `backup/stage1_*` refs were audited the same way — zero unlanded product code; details and the deletion-manifest follow-up live in the "Repo GC completion" Planned row above. The pruned `prep/post-5d631-queued-edits` tip's two open-work rows are now recovered as Planned rows above (note: the pagination undercount commits cited earlier in this bullet are the frontend workaround; the backend root fix is the recovered open row).
- **Playwright parallelism deferred behind a green serial baseline.** `web/playwright.config.ts` remains `workers: 1`. Latest owner evidence is `docs/runbooks/evidence/playwright-serial-green/20260710T220025Z/{SUMMARY.md,triage.md}`: two consecutive full serial runs passed with `301 passed`, `19 skipped`, `0 failed`, plus `lint:e2e` and `local-ci --fast` green. Future `workers > 1` work is deferred until evidence shows a real benefit over this now-green serial baseline.
- **Playwright CI local Stripe-mode regression remains open.** Owner: `.github/workflows/ci.yml`; `STRIPE_LOCAL_MODE: "1"` is already owned by the Playwright job environment, so future work should preserve that CI contract instead of adding a parallel local-mode path.
- **June 3 customer-release rerun follow-ups remain open (narrowed).** Metrics product work is closed: `docs/runbooks/evidence/metrics-tab-reconcile/20260711T005607Z/finding.md` classifies the old miss as stale-staging drift and local proof is green/non-vacuous. The former jul11 metrics probe residual is now closed by `docs/runbooks/evidence/metrics-probe-ns-timestamp/20260712T190558Z_devalue_fix/classification.md`, which captures a green authenticated staging rerun after the probe learned SvelteKit's devalue-indexed `__data.json` payload shape (`exit_code == 0`, `metrics_tab_data_ok == true`). The historical `verify_email` S3 inbox item stays tracked by `web/tests/e2e-ui/smoke/customer_release_surfaces.spec.ts`; clean-env support/canary proof is green at `docs/runbooks/evidence/ses-inbox-canary-clean-env/20260711T014655Z/SUMMARY.md`.
- **`e2e-deployed` deploy-currency gate remains open.** Owners: `.github/workflows/ci.yml::e2e-deployed`, `scripts/launch/wait_for_pages_parity.sh`, `scripts/lib/deployable_currency.sh`, and `scripts/deploy_status.sh --json`. The served-marker parity guard and branch-aware staging deploy are landed, and the deployable-currency SSOT fast-follow is landed; keep this row open only for future deployed-lane evidence that consumes those owners rather than raw commit-behind drift.
- **Public launch is NOT announce-ready.** Current verdict is owned by `LAUNCH.md ## STATUS`; the top entry is `2026-07-13 jul12_pm RC verdict launch anchor — NOT announce-ready` (`NOT-READY-real-defects`, sole real defect `staging_billing_rehearsal=invoice_email_ses_not_ready`). That defect's IAM root cause is fixed and live-proven (see the In-VPC dunning IAM row above), **Owner corrected 2026-07-23:** `chats/icg/jul13_12pm_3_s1_rc_launch_verdict.md` was ABANDONED by operator decision 2026-07-16 and must not be dispatched; the RC-verdict owner is `chats/icg/jul16_10am_8_rc_verdict_and_launch_anchor.md` (L8), which has never been dispatched and is currently parked behind Algolia F10. No roadmap row may clear the announce gate until L8 lands a fresh RC verdict in an allowed launch shape and a matching `LAUNCH.md ## STATUS` entry. The newest `LAUNCH.md ## STATUS` entry is the 2026-07-22 `jul16_10am_7` data-plane SHIP/PARK record, which explicitly asserts no launch-shape verdict.

## Archive

Historical implementation details and done-only roadmap prose live in [`implemented/`](implemented/). Do not duplicate that audit trail here.
