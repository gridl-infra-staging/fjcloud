# Flapjack Cloud (fjcloud) — Roadmap

Reconciliation headers for 2026-07-30 (both passes), 2026-07-31 and 2026-08-01 were retired inline 2026-08-05, and both 2026-08-05 headers were retired 2026-08-07, to hold this ledger under its 200-line shape cap; their full text is preserved verbatim in [the reshape archive](implemented/2026-06-05_roadmap_v2_reshape_archive.md#reconciliation-headers-retired-2026-08-05) and [its 2026-08-07 successor](implemented/2026-06-05_roadmap_v2_reshape_archive.md#reconciliation-headers-retired-2026-08-07). The 2026-08-06 header was retired the same way at 23:5xZ on 2026-08-07 into [the second-pass successor](implemented/2026-06-05_roadmap_v2_reshape_archive.md#reconciliation-headers-retired-2026-08-07-second-pass). The 2026-08-02, 2026-08-03 and both 2026-08-07 headers below remain live.
**RECONCILED 2026-08-07 ~23:55Z against `origin/main` `777a8a2a9`; this paragraph supersedes older reconciliation headers only where it names the same fact.** Six lanes merged since header SHA `675a1e9d5` and `bash scripts/probe_ssot_currency.sh` read `STALE lane_merges_since=6 threshold=5`, so this pass was owed. Read from the merge diffs. **(1) `aug07_5pm_2` (`777a8a2a9`, 50 files, +5,818) made the fleet-dataplane pointer requirement measured rather than catalogue-driven** (`scripts/probe_fleet_dataplane.sh` +69, new `scripts/tests/probe_fleet_dataplane_test.sh` +140): a region is exempt from same-region subnet/AMI oracles only when its EC2 read succeeded AND it holds zero managed-engine instances, so `eu-west-1` stops blocking the engine-TLS diagnosis while a populated or unreadable region still reds. **It landed only after the supervisor rewrote its branch in place** — commits `f2ac2eacd` and `c92a11962` carried prohibited `Co-Authored-By: Claude Opus 4.8` trailers, `batman land` is warn-only on that finding, and the merge was aborted mid-flight and re-run at rewritten tip `9daf0e43e` with an identical tree. **(2) `aug07_4pm_8` (`d3483b364`, +90)** repaired the local-source teardown and secret-absence gate. **(3) `aug06_10pm_5` (`b32220e47`, +618)** made the synthetic-traffic probe report visibility failure honestly. **(4) `aug07_4pm_6` (`fea62b7c6`, 31 files, +487)** recorded a target-bound staging-sync acceptance receipt. **(5) `aug07_6pm_6` (`980e66f9a`) shipped NO product code** — one 78-line checklist file, so staging-CI absence-grace determinism is NOT done and its row stays open. **(6) `aug07_5am_0` (`da20dbb5f`, 106 files, +8,220/-1,884)** repaired the source-parity fast gate and landed the provider-parity fixture. **Live deploy state measured this pass:** staging `/version` serves `dev_sha 8e4c4e52e`, prod still serves `dev_sha 99262771479d` built 2026-07-30 — unchanged for eight days, which is why the source-picker batch's SP-2 remains in its FR-8 cross-batch wait rather than blocked on engineering. **APPENDED 2026-08-08 ~00:40Z at `2e7db3b02`, for what landed after the `777a8a2a9` pass; row-level detail is deliberately kept out of the table because `aug07_6pm_1` is reflowing those rows right now.** **(a) The engine-TLS verdict this batch existed to produce is NEGATIVE.** `docs/runbooks/engine_tls_fleet_state.md` reads `staging_fleet_tls_only_capable=no`: the Stage-2 gate replayed `ACTION_REQUIRED reason=search_evidence_indeterminate`, so Stage 3 never provisioned the disposable VM (`Resources created: 0`) and no engine was measured serving TLS. Item (1) records the probe change; this records the outcome, and the outcome is that the engine-exposure P0 did not move. **(b) The P0 staging-mirror row below is STALE — its staging half reopened, and one test is holding the whole deploy path.** `scripts/probe_mirror_ci_currency.sh` reads staging `run_id=31199924808 conclusion=failure` at mirror HEAD `06ba4b38e` and prod `reason=ci_run_missing`. Sole red: `postgres_reconciliation_starts_absence_grace_only_after_authenticated_404` (`api/src/services/algolia_import/reconciliation_postgres_absence_tests.rs:82`), 960 pass / 1 fail, which skipped `deploy-staging`, `deploy-prod` and `e2e-deployed`. `scripts/launch/post_wave_a_sync_prod.sh` refuses to promote without a green staging mirror HEAD, **so that one test is why prod still serves `dev_sha 992627714`.** The fix is `739f318ff` on `aug07_7pm_6`; it merges clean. **(c) `chats/icg/_followups.md` had an uncommitted delete-instead-of-close at 15:39Z; RESTORED 2026-08-08, now clean at 227 `## Open` rows.** An earlier revision of this clause said `## Harvested` was deleted. It was not — only `## Open` shrank, and `queue_followups.py:941-947` re-derives such rows regardless. **(d) The `aug07_6pm` CI-substrate batch was rostered at `428544967`** — four lanes against the single-lock `local-ci --fast` (1736s wall, 1542s in five non-concurrent gates).
**RECONCILED 2026-08-07 ~13:30Z against `origin/main` `675a1e9d5`; this paragraph supersedes older reconciliation headers only where it names the same fact.** Six lanes merged in the twelve hours to 2026-08-07T08:17Z and **not one of them reached this file until this pass** — the sixth consecutive batch to fail that way; see the closeout-debt row. Read from the merge diffs, not from `implemented/`, whose 2026-08-07 receipts are auto-generated stubs reading `Applied from roadmap refresh proposal` and carrying no outcome. **(1) `aug06_10pm_1` (`5f86f452f`) closed the public plaintext data plane in Terraform:** `flapjack_public_data_plane` is absent from `ops/terraform/networking/main.tf`, `tests_stage1_static.sh:35` pins its count at `0`, and `validate_all.sh` rejects four mutation fixtures (exact, wrong name, wrong protocol, wrong range). **This is code only. The live security-group rule still exists and must NOT be applied while no engine serves 443** — removing tcp/7700 first deletes the only customer data plane. **(2) `aug07_3am_1` (`8e4c4e52e`) made engine identity survive a scheme change** (`infra/metering-agent/src/tenant_map.rs`, +`scripts/tests/flapjack_url_producer_parity_test.sh`), so enabling TLS can no longer silently filter every tenant out of `usage_records`. **(3) `aug06_10am_4` (`18bff8fa4`) retired the ~21,000-query `GET /admin/tenants`** (+2,418/-263 over 24 files; `routes/admin/tenant_quotas.rs` extracted). **(4) `aug06_4pm_6` (`f03cd0839`)** hardened the synthetic-traffic seeder harness (+542). **(5) `aug06_10am_6` (`6ac2428f2`) shipped NO product code** — its Stage-2 decision gate fired and it merged 160 lines of checklist only, so admin-customers pagination is NOT done and the row it was authored against stays open. **(6) `aug07_3am_2` (`b6735b7da`) shipped no code by design and STOPPED at Stage 1** on `environment_attribution_ambiguous` — see the engine-exposure row for why that blocker is fifteen days old.

## Current corrections to historical planned rows

The long-form rows below retain dated investigation history. The following
2026-07-26 corrections are authoritative wherever that history conflicts:

- **Engine exposure:** E1's transport switch and design contract are merged but
  inert/default-off. The remaining owner is a staging-fleet TLS proof followed
  by removal of public tcp/7700; it is not a new endpoint-switch implementation.
- **Production data plane:** a non-vacuous production signup → provision →
  ingest → exact-search → zero-residue-cleanup proof has run. The open work is
  recurring proof freshness and release-candidate ownership, not proving that
  the data plane has literally never served a request.
- **Public-release verdict:** the next historical verdict owner is
  `chats/icg/jul23_11am_8_rc_verdict_launch_anchor.md`, gated by a discharged
  §1 receipt. Do not revive the abandoned `jul16_10am_8` owner.
- **Closeout debt moved forward rather than clearing.** The five 2026-07
  closeouts exist (`chatting/jul20_fjcloud_console_closeout.md`,
  `jul20_9pm_console_prep_closeout.md`, `jul21_10pm_fjcloud_reliability_closeout.md`,
  `jul22_engine_exposure_tls_closeout.md`, `jul23_11am_launch_security_autoheal_closeout.md`)
  plus `jul25_pm_correctness_refugee_migration_polish_closeout.md`; do not
  re-author them. **Re-measured 2026-07-28:** `jul26_pm_11` and `jul27_pm_0`
  since closed (`chatting/jul26_pm_11_console_customer_quality_closeout.md`,
  `chatting/jul27_pm_release_confidence_closeout.md`); `jul26_12am_0` and
  `jul26_pm_7` still have merged lanes and no `chatting/*_closeout.md`. Same
  cause every time: the reconcile lane runs last, so a budget stop drops it.
  See the registry-debt row in `## Planned` for the full open/closed count.
- **`jul23_11am` successors:** its administrative closeout exists and E1
  shipped. The surviving work is the gated RC owner, TLS live flip/removal,
  replacement timeline UI recovery, and enabled autorepair proof. These are
  re-homed in
  `chats/icg/jul26_12am_0_local_migration_and_scale_closure_orchestration.md`;
  do not dispatch the stale monolithic descriptions below.
- **Algolia migration:** F10 catalog finalization and its consumer surface are
  merged. A3C's correctness receipt and the clean L1K SES recovery are merged;
  the stopped A3 and L1K workers must not be merged. F10C privacy scrub remains
  real but unmerged. Recover it only through
  `chats/icg/jul26_am_10_f10c_clean_salvage.md`, using F10C-only intermediate
  `25453f81e`; combined worker `55eac` is stopped and must not be lifecycle
  merged because it also imports superseded L1K ancestry.
- **Migration feature status:** backend and customer frontend are connected locally,
  migration operations are OpenAPI-registered, replica-copy behavior is honest, and
  real-Algolia create/replace/cancel/idempotency/resume-refusal correctness is
  certified. Staging/prod remain default-closed. **The local closure tail is now
  CLOSED** — terminal outcome UI, multi-node refusal proof, F10C erasure, and exact
  privacy-erasure acceptance all merged 2026-07-26/27. Next local chunk: the
  provider-neutral extraction (M1E in flapjack → M1F here), `jul26_pm_7` Wave 2.
- **Source migration expansion:** Meilisearch and Typesense are now pre-launch
  planned sources alongside Algolia. M0 contract/KAT and architecture/security
  lanes run before any product refactor; one shared job/erasure/wire/console
  lifecycle must remain canonical.
- **`FJCLOUD_ALGOLIA_MIGRATION_ENABLED` is no longer inert.** The 2026-07-16
  header below — "read by **no handler**", enforcement by hardcoded constant —
  was true then and is false now. F11 wired it: four handlers gate on
  `state.algolia_migration_enabled` (`routes/migration.rs:169`,
  `routes/migration/eligibility.rs:108`, `routes/migration/jobs.rs:168,482`),
  default still `false` (`config.rs:101-104`). Fail-closed by default and
  locally activatable, but via the flag, not a constant.

**RECONCILED 2026-08-02 against `origin/main` `67bacdf68`; this paragraph supersedes every older reconciliation header wherever they conflict.** Applied here: the three unconsumed handovers `chatting/aug02_5am_roadmap_corrections_fc1.md`, `chatting/aug02_5am_roadmap_corrections_fc2.md` and `chatting/aug02_5am_roadmap_corrections_fc4.md` (named in full, not brace-collapsed, so `scripts/check_handover_consumption.sh` can verify the citation) — their declared writer FC-6 (`chats/icg/aug02_5am_6_batch_close_and_roadmap_reconcile.md`) was authored but never dispatched — plus the `aug02_9am_2` console-preview salvage that merged at `67bacdf68` after those handovers were written. **If FC-6 is dispatched later it must re-derive against `origin/main`, not replay its authored text**, because the facts it was written to carry are already applied below. Fjcloud lanes merged 2026-08-01/02 and reflected in the rows: `aug01_11pm_1` (`8dc6486dc`), `aug01_11pm_2` (`7ecd0597a`), `aug01_3pm_1` (`4fd7f13b5`), `aug01_4pm_1` (`40992de37`), `aug01_5pm_1` (`bcb873efc`), `aug01_7pm_1` (`0c38ae20f`), `aug02_3am_1` (`e93f78b67`), `aug02_4am_1` (`a2c927c04`), `aug02_5am_1` (`c175694f9`), `aug02_5am_2` (`7ea080304`), `aug02_5am_4` (`d48f9fb75`), `aug02_12am_1` (`814a96605`), `aug02_9am_1` (`b64e6d82c`), `aug02_9am_2` (`67bacdf68`).

**RECONCILED 2026-08-03 against `origin/main` `aba6f4454`; this paragraph supersedes every older reconciliation header wherever they conflict.** It applies by hand the six `chatting/aug02_11am_roadmap_handover_fs{1,2,3,4,6,7}.md` handovers that the `aug02_11am` batch produced correctly and on time, because their declared consumer `aug02_11am_9` stopped at stage 2 of 3 — the same failure as `aug02_5am`'s unconsumed `fc{1,2,4}` handovers recorded in the header above, four days apart. Merged and now reflected in the rows: `aug02_11am_1` (`ac6483e82`, enforced CSP + security headers), `aug02_11am_2` (`84655a059`, password policy), `aug02_11am_3` (`5b9b5157b`, preview capability ownership), `aug02_11am_4` (object-store config seam), `aug02_11am_6` (preview media-type boundary), `aug02_11am_7` (`a18629fb7`, cutover verification API), plus `aug03_1am_1` (`d10ff`), `aug03_2am_1` (`9764d`) and `aug03_3am_1` (`16ab8`), which were test-harness repairs only. **Measured on this tree, not asserted:** `cd web && npx vitest run` → 251 files / 3457 tests / 0 failures / 119s. **Three findings this reconciliation surfaced that no row previously owned, each now a `## Planned` row, and two were fixed the same day:** cutover verification covers 1 of 3 providers and has never run against a real source (open); the enforced CSP refused the browser accessibility runner's inline scanner injection (**fix landed** — same-origin loader plus a hermetic, mutation-checked browser contract; the catalog re-run is the remaining exit); and `FLAPJACK_PORT` and `LOCAL_MEILISEARCH_PORT` shared the default `7700` so the three-provider stack could not start at all (**fix landed** — Meilisearch moved to `7710` and a pre-flight guard now refuses any two equal host ports, naming both variables). A correction this header carries deliberately: the first version of the accessibility row said the blocked scan read as a clean pass. It did not — `accessibility.spec.ts` asserts its own scanned-route denominator and fails loudly. The coverage loss was real; the silence was not. **Corrections applied the same day, after re-measurement:** the OAuth console half does ship (`OAuthButtons.svelte` at `login/+page.svelte:87`, `signup/+page.svelte:132`, plus a browser round-trip), the 628 age-swept follow-ups were classified and their closure was substantively correct, and a dispatch-backlog figure of 30 was withdrawn for 17 on a disposition-aware method. **Two lanes remain unmerged and both merge clean:** `batman/aug02_11am_8_cutover_verification_console` (the customer half of cutover verification) and `batman/aug02_11am_9_batch_close_and_reconcile` (whose stage-2 measurements are folded in here rather than re-derived). Batch inventory and method: [implemented/2026-08-03_aug02_11am_batch_reconciliation.md](implemented/2026-08-03_aug02_11am_batch_reconciliation.md).

**RECONCILED 2026-08-03 against `origin/main` `d1f48247a`; this paragraph supersedes every older reconciliation header wherever they conflict.** It applies by hand the five `chatting/aug03_11am_handover_fj1.md`, `chatting/aug03_11am_handover_fj2.md`, `chatting/aug03_11am_handover_fj3.md`, `chatting/aug03_11am_handover_fj4.md` and `chatting/aug03_11am_handover_fj5.md` handovers, because their declared consumer **FJ-6 (`chats/icg/aug03_11am_6_batch_close_and_reconcile.md`) was never dispatched** — `git branch -a --list '*aug03_11am_6*'` returns zero refs, and `grep -c aug03_11am ROADMAP.md` returned `0` before this edit even though the batch closeout and all five handovers were on `main`. That is the **third consecutive batch** whose reconcile lane failed to reach this file (`aug02_5am`'s FC-6 and `aug02_11am`'s FS-9 preceded it), and the mechanism is unchanged and now measured three times: the reconcile lane gates on every sibling merging, so it is structurally last and inherits whatever budget stop or newer batch arrives first. All five Wave 1 lanes merged, and every deliverable below was re-verified against the tree rather than trusted from the status board or the closeout: FJ-1 (`186a5af89`) at `infra/api/src/auth/api_key.rs:101-102,144`; FJ-2 (`fcdda08d8`) at `infra/api/src/router/route_assembly.rs:272,336`; FJ-3 (`f110f6ce5`) at `web/tests/e2e-ui/full/accessibility.spec.ts:98`; FJ-4 (`f3e926d3b`) at `route_assembly.rs:445-446`; FJ-5 (`b3abdd0cf`) at `infra/api/src/services/algolia_source.rs:315,406`. **The enforced-CSP accessibility row is CLOSED and retired here.** `node -p` over `web/src/tests/a11y_route_manifest.json` yields `scannedRoutes.length` = `40`; `accessibility.spec.ts:98` now derives `EXPECTED_CATALOG_DENOMINATOR` from that manifest instead of hardcoding `40`, so the denominator cannot silently shrink; the acceptance run measured `3 passed`, `routes_scanned=40 violations=0 catalog_status=COMPLETE`. That row's line was reused for the browser-suite row below, which `aug03_11am_3` surfaced in the same no-fail-fast run. **FC-5 (`aug02_5am_5_neutral_source_discovery_consumer`) is remeasured on `origin/main` at `3fd4098e7`** during this reconciliation, which is what unblocks the three-provider browser proof; see that row. **A live blocker this reconciliation found, recorded because the class will recur:** `origin/main` was carrying a `ROADMAP.md` of **205 lines against the hard 200-line cap** at `scripts/check_roadmap_v2_shape.sh:152`, so `roadmap-v2-shape` — and therefore `local-ci --fast`, every lane's pre-push gate — was RED on `origin/main` for roughly four hours; the repair commit `647503ed6` sat unpushed in the canonical clone the whole time and reached `origin` only as a passenger on the FC-5 merge. `roadmap-v2-shape` is absent from the six-gate `.batman.toml` merge set, so no merge would have caught it. Two closed P2 rows were archived to fund this edit's line budget: [aug03_11am reconciliation](implemented/2026_08_04_aug03_11am_batch_reconciliation.md).

Historical reconciliation revisions through 2026-07-24 are preserved in [the Stage 2 row-size archive](implemented/2026-06-05_roadmap_v2_reshape_archive.md#reconciliation-header-history); the 2026-07-25 reconciliation header, the engine-exposure narrative correction, the stranded-lane disposition graph and the `jul12_pm` RC anchor are preserved in [the 2026-08-02 header archive](implemented/2026-08-02_roadmap_header_and_refugee_depth_archive.md). The current interpretation is the reconciliation at the top of this file.

**Purpose:** `ROADMAP.md` owns the active product/work ledger and the current backlog.
Historical implementation detail lives in [`implemented/`](implemented/).
**Launch gate:** [LAUNCH.md](LAUNCH.md) owns the v1 launch sentence, blocker interpretation,
and current launch verdict. On launch-readiness questions, LAUNCH.md wins.

## Active

A real prod environment exists and its signup, pricing, billing, policy, and search data-plane lifecycle have been proven together once with exact search and zero-residue cleanup. The open launch work is proof freshness and a current RC verdict; staging evidence still may not substitute for the recurring prod search proof.

Active direction: make prod actually serve search and prove it; restore launch-matrix §1 to green; issue a fresh RC verdict from that proof. Then keep the customer release path honest and close only concrete gaps that block signup, email verification, search provisioning/ingest/query, usage, Stripe payment, or support email.

Current launch interpretation is intentionally compact here; use these owners for detail:

- [LAUNCH.md](LAUNCH.md) — launch sentence, blocker interpretation, and current verdict.
- [docs/launch_verification_matrix.md](docs/launch_verification_matrix.md) — launch-sentence
  verb-to-test traceability.
- [docs/runbooks/staging-evidence.md](docs/runbooks/staging-evidence.md) — staged evidence
  interpretation and artifact pointers.
- [docs/runbooks/paid_beta_rc_signoff.md](docs/runbooks/paid_beta_rc_signoff.md) — RC
  taxonomy and readiness-artifact contract.
- [docs/launch/beta_launch_remaining_work.md](docs/launch/beta_launch_remaining_work.md) —
  plain-language overview of what's left before paying beta (orientation only; LAUNCH.md +
  the launch matrix remain the authority).
- [docs/runbooks/live_environment_ops_approach.md](docs/runbooks/live_environment_ops_approach.md) —
  how to do staging/prod work without the repeated stall-and-strand headaches.
- [`implemented/`](implemented/) — closed waves, historical merge
  detail, and done-only prose moved out of this live ledger.

### Platform Ledger

| Area                                                                       | Status         | Current owner note                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------------------------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Core cloud API surface (auth, index lifecycle, billing, usage, deployment) | Built          | Customer-facing API endpoints and quota-aware index creation are implemented.                                                                                                                                                                                                                                                                                                                                                                                                     |
| Admin API and operations tooling                                           | Built          | Fleet/admin management, customer audit/snapshot drill-down, and broadcast tooling are implemented; status publication remains tracked below. **Extended by the `jul20_4pm` operator-visibility batch (merged 2026-07-20/21, reconciled into this ledger 2026-07-21):** admin index inventory + audit metadata + invoice Stripe drill-in (`routes/admin/indexes.rs:85`, `routes/admin/mod.rs:68`, merge `e1da3832b`); revenue/MRR billing-summary endpoint retiring the N+1 billing loader (`routes/admin/mod.rs:107`, hand-calculated-dollar test `billing_endpoints_test.rs:2446`, merge `ac70317ac`); fleet all-servers view owning the canonical `vm_health_rollup` classifier, which conservatively returns `Unknown` — never `Healthy` — for an empty deployment set (`services/vm_health_rollup.rs`, merge `672a316cc`); host-level per-VM telemetry collected from `/proc` into `vm_host_metrics` (`infra/migrations/061_vm_host_metrics.sql`, `infra/metering-agent/src/host_metrics.rs`, merge `b07394cd8`); and real host-metric columns on the fleet page asserted by exact normalized cell text (`admin-fleet.test.ts:318-320`). **Extended 2026-07-22 (`jul21_am_1`, merge `29a8649b6`):** one idempotent VM-teardown seam (`routes/admin/vms.rs::teardown_retired_vm_resources:368`, called from the decommission path at `:507`) that owns the Cloudflare A record and SSM node key alongside the inventory row, plus a **read-only** orphan detector at `GET /admin/vms/orphans` (`routes/admin/mod.rs:110`, `services/vm_orphan_reconcile.rs`, handler `routes/admin/vm_orphans.rs`). The detector is detect-only by design — it never deletes — and instance-only orphan enumeration is explicitly still deferred; see the teardown row under Planned. **Extended 2026-08-01 (`jul30_10pm_8`, merge `4500055da`) — admin sessions are now durable and API-owned, backend and frontend connected.** `infra/api` owns credential validation, session validation, revocation, operator identity and timeout enforcement: `auth/admin_session.rs` (24h absolute lifetime, 1h inactivity timeout, `PgAdminSessionRepo`), `routes/admin/sessions.rs` mounted at `POST|DELETE /admin/sessions` and `GET|DELETE /admin/sessions/current` (`routes/admin/mod.rs:30-35`), table `infra/migrations/071_admin_sessions.sql`. The web console consumes it rather than minting its own: `web/src/lib/server/admin-session.ts` holds only the cookie name, a lifetime clamped to the API's own ceiling, per-IP login throttling, and fail-closed wrappers, and a lost session redirects to `/admin/login` (`08feecfb7`). Contract coverage: `infra/api/tests/integration/durable_admin_session_contract.rs` (603 lines). **Extended 2026-08-07 (`aug06_10am_4`, merge `18bff8fa4`, +2,418/-263 over 24 files):** `GET /admin/tenants` no longer issues roughly 21,000 sequential queries per request; per-tenant quota handling moved to its own owner (`routes/admin/tenant_quotas.rs`) and `services/billing_health.rs` grew the batched readers, with exact-value coverage in `tests/integration/tenants_test.rs` and `tenant_repo_test.rs`. **Not shipped, and the row it was authored against stays open:** `aug06_10am_6` was dispatched for admin-customers pagination, its Stage-2 decision gate fired, and merge `6ac2428f2` carries 160 lines of checklist and no product code — `web/src/routes/admin/customers/+page.svelte` still renders unpaginated |
| Customer web portal core workflows                                         | Built | Onboarding, indexes, logs, billing, account settings, resend-verification, and support affordances are implemented. **Added 2026-07-20 (`jul20_4pm_6`, merge `072cab7a4`):** the per-index Infrastructure tab, owner of the `to_public_topology` privacy filter, which structurally projects `VmInventory` down to `{region, provider, utilization}` — hostname, `flapjack_url`, VM UUID and absolute capacity are dropped rather than merely hidden, and utilization is coarsened to a green/yellow/red bucket. Leak-proofed at two levels by sentinel tests (`services/public_topology.rs::to_public_topology_never_leaks_host_or_absolute_capacity`; `tests/integration/index_infrastructure_test.rs:328,374,462,482,513`). **Closed 2026-07-25:** Stage 3 completed the unmocked browser privacy proof before commit; implementation commit `03ef95443` finalized that tested worktree. The recorded Stage 3 passes were `cd web && npx playwright test tests/e2e-ui/full/public-infrastructure.spec.ts --reporter=line` (2/2 passed) and `cd web && npx playwright test tests/e2e-ui/full/index-detail-infrastructure.spec.ts --reporter=line` (2/2 passed), proving the customer UI/privacy path plus the public-page k-anonymity and closed-shape privacy checks against the real local app/database. **Extended 2026-07-27 by the `jul26_pm_11` console customer-quality batch (merges `9fd7e2ab2`, `47d163a4f`, `504f3af0f`, `29aeeafe9`, `df44f91c9`):** customer console actions now carry browser *end-effect* proofs rather than render-only assertions — replica create/delete persistence across refresh (new `web/tests/e2e-ui/full/console_replica_lifecycle.spec.ts`), index-detail delete redirect plus absence from the list (`indexes.spec.ts`), onboarding create-index failure→retry→recovered-credentials (`onboarding.spec.ts`), non-overlapping daily-usage chart labels at 768px and 1280px (new `console_daily_usage_chart.spec.ts`), and document upload/add-record read-after-write reuse. `docs/audits/ui_test_coverage_p0_ledger.md` was reconciled the same batch: of 23 examined rows, 6 were REAL, 13 STALE/retired (including the non-existent `createPreviewKey` surface), and 4 ALREADY-COVERED. **The batch's tag-count gate is met at 19 tags across 18 declarations, but three execution residuals survive — see the "Console browser-coverage residuals" row before citing this coverage as complete.** **Extended 2026-08-02 (`aug02_11am_1`, merge `ac6483e82`) — the customer web surface now sends security headers and an ENFORCED CSP, not report-only.** `web/src/lib/server/security_headers.ts::applyDocumentSecurityHeaders` is the single owner of `Strict-Transport-Security`, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy` and `X-Robots-Tag`, applied at `web/src/hooks.server.ts:104`; the policy itself is installed through `kit.csp.directives` from `web/svelte.config.js::createDocumentCspDirectives` with `kit.csp.mode = 'auto'` and no surviving report-only policy. Before this the whole HTML surface sent exactly one header (`X-Robots-Tag`). **Measured on `main` 2026-08-03: `cd web && npx vitest run` → 251 files, 3457 tests, 0 failures, 119s.** **Not full-suite green — `vitest` is the component lane; the browser lane is 15 red (see its row under `## Planned`).** The CSP/accessibility block is CLOSED (`aug03_11am_3`, `f110f6ce5`): same-origin loader `web/tests/fixtures/axe_injection.ts`, acceptance `routes_scanned=40 violations=0 catalog_status=COMPLETE`. **Extended 2026-08-03 (`aug03_11am_2`, `fcdda08d8`):** the Synonyms `Clear All` and Suggestions `Rebuild` controls posted to routes that were never registered; both are now mounted (`route_assembly.rs:272,336`) and proven unmocked at `index-detail.spec.ts:535,1119` with negative controls, and the class is closed — `api_client_route_contract.sh` now covers `client.ts`, `call_sites=106 route_violations=0 exemptions=1`. |
| Public beta landing and policy surface                                     | Built — one unrun verification | Public landing, pricing, terms, privacy, and DPA surfaces are implemented. **Added 2026-07-21 (`jul20_4pm_7`, merge `57ddcae80`):** the public `/infrastructure` transparency page, which reuses `to_public_topology` per VM and applies k-anonymity — `worst_region_utilization` returns `None` when a region has fewer than 2 VMs or fewer than 2 observed utilization buckets (`routes/public_infrastructure.rs:205-226`), so single-VM regions and partially observed regions cannot have an individual VM's load inferred. **Hardening open (concrete, not theoretical):** the route is registered on the **unauthenticated** public router beside `/health` with **no rate limiter** (`router/route_assembly.rs:107-111`; limiter middleware exists only for the auth/tenant/admin routers at `:32,72,125`), and the handler is **N+1 on the database** with no caching — one `tenant_repo.list_by_vm` per VM plus one `deployment_repo.find_by_id` per deployment (`routes/public_infrastructure.rs:66-92`). `PUBLIC_TOPOLOGY_MIN_REFRESH_INTERVAL_SECS = 60` exists but is advisory on the customer endpoint only and is neither enforced nor returned here. Unauthenticated + uncached + N+1 is a cost/DoS amplification vector; recommended fix is a limiter plus a ≥60s response cache and a batched tenants/deployments query. **CLOSED 2026-07-22 (`jul21_10pm_1`, merge `959d1648d6`):** the handler now serves from a TTL cache (`state.rs:275` `public_infrastructure_cache`, read at `routes/public_infrastructure.rs:69`, populated at `:81`, TTL contract tests at `state.rs:307-329`) and the N+1 is gone — one batched `tenant_repo.list_by_vms` (`:119`) plus one batched `deployment_repo.find_by_ids` (`:134`) replace the per-VM/per-deployment loops. **The one thing NOT done:** the lane finished 4/5 stages, so Stage 5 — the post-merge deployed-staging verification of the cached route against a real staging SHA — was never run (`chats/icg/_followups.md`, `jul21_10pm_1::stage_05`). The code and its unit/contract tests are green on `main`; the deployed surface is unverified. **PUBUX-004 CLOSED 2026-07-30 (`jul30_2am_2`, merged):** the `/pricing` public-page audit gap is closed and browser-verified at both audit viewports (`1280x720`, `390x844`). The `/infrastructure` deployed-staging verification named above remains the outstanding item in this row. |
| Customer billing portal handoff                                            | Built          | Runtime publishable-key fetch, payment-method setup/default management, and backend/frontend tests are implemented.                                                                                                                                                                                                                                                                                                                                                               |
| Status page and outage communication tooling                               | Built          | Env-backed `/status` readback is live; `scripts/set_status.sh` owns publication of `SERVICE_STATUS`, `SERVICE_STATUS_UPDATED`, and `SERVICE_STATUS_MESSAGE`; `scripts/deploy_status.sh` owns deploy readback.                                                                                                                                                                                                                                                                     |
| Signup abuse and pricing trust guards                                      | Built          | Disposable-domain blocking, verify-before-Stripe customer creation, auth rate limits, and pricing drift checks are implemented. Pricing-trust SSOT at HEAD, **remeasured on `831996142` 2026-08-06 after `aug05_12pm_2` (`c0593`) and `aug05_12pm_4` (`ef725dce1`) merged, superseding the 2 of 5 figure this row previously carried**: reading `metadata()` in each `infra/pricing-calculator/src/providers/*.rs`, **4 of 6 now carry a `last_verified` date** — `Algolia` and `MeilisearchResourceBased` at `2026-07-06`, plus `TypesenseCloud` and our own `Griddle` newly stamped `2026-08-05` — and **2 remain `None`**: `ElasticCloud` (`elastic_cloud.rs:12`; $99/120 GB/2-zone baseline found, scaled tiers not found) and `AwsOpenSearch` (`aws_opensearch.rs:12`; gp3 plus one r6g.xlarge example found, full instance/transfer table not found, and the dedicated-master example mismatches modeled `m6g.large.search`). **The consequence is a permanently red nightly alarm and it was a deliberate, correct choice, so do not "fix" it by exempting a provider.** `Nightly Stripe Clock` has concluded `failure` every night since 2026-07-31 (last success 2026-07-30; newest `31078239221`, 2026-08-06T06:43Z), and the failing job is `pricing-freshness`, not the Stripe test-clock lane, which passes. The single failing test is `pricing_freshness_wall_clock_tripwire` (`infra/pricing-calculator/src/lib.rs:362`), which calls `ensure_pricing_freshness(90)` against real wall-clock time; it fails **only** on the two unverified providers, exactly as `aug05_12pm_2`'s own invariant required — "a provider that cannot be verified stays `None` with a written reason rather than being stamped or exempted." **It will stay red until those two providers are verified against their published sources**, which is real open work, not a gate to silence. Evidence: `docs/runbooks/evidence/pricing-verification/20260729T170845Z/SUMMARY.md`. No published customer-facing pricing number changed: `git diff --exit-code c34a40b1ce..HEAD -- infra/pricing-calculator/src/providers/{typesense_cloud.rs,elastic_cloud.rs,aws_opensearch.rs}` is clean and the bundle's `Changed Constants` says none. `stale_providers_from_metadata` and the freshness gate fail on `last_verified = None` with `never-verified`; each unverified provider stays unverified until its declared public source supports every modeled constant/assumption in a new evidence bundle and its owning `metadata()` receives the actual read date; preserve the AWS dedicated-master mismatch until a source supports a general replacement. **Customer-facing paid-minimum render CLOSED 2026-07-30 (`jul30_2am_2`); its VALUE CHANGED 2026-08-05 and this row asserted the old one until 2026-08-06.** `sharedPlanMinimumMonthlyLabel` is still the single route render owner, but the minimum is **$15**, not `$5`: `68b6d95e4` moved `SHARED_MINIMUM_MONTHLY_CENTS` to `1_500` (`infra/pricing-calculator/src/providers/griddle.rs:46`, pinned by an exact-value assertion at `:291`). **Stage 3 SSOT correction 2026-07-31:** `toCustomerVerificationLabel` still maps `verification_label === 'unverified'` to `Modelled pricing`, and the table header remains `Pricing verified`; this wording gap stays open until no provider lacking `last_verified` renders as verified or methodology choice. **Password policy hardened 2026-08-02 (`aug02_11am_2`, merge `84655a059`) — closed, detail compressed 2026-08-06 for row budget.** `infra/api/src/validation.rs` raised `MIN_PASSWORD_LEN` 8 -> 15 and counts Unicode code points (`:44-49`) rather than bytes, closing the three-emoji hole, while `MAX_PASSWORD_LEN = 128` stays byte-enforced at `:36` so a long input cannot force an unbounded walk; a vendored blocklist (`validation/weak_passwords.txt`, `:59,76,91`) rejects common secrets, and `weak_password_count()` at `:69` asserts the corpus size so a partially parsed corpus cannot silently disable the check. |
| Stripe lifecycle and webhook regression coverage                           | Built          | Invoice, payment-method, webhook signature, idempotency, and matrix coverage are implemented for the current metered v1 model.                                                                                                                                                                                                                                                                                                                                                    |
| Support inbox and synthetic canary automation                              | Built          | Current clean-env bundle: `docs/runbooks/evidence/ses-inbox-canary-clean-env/20260711T014655Z/SUMMARY.md`. Live staging proof is green on canonical `stuart-cli` for account `213880904778`: SES bounce/complaint RAN and exercised suppression/audit side effects through `scripts/probe_ses_bounce_complaint_e2e.sh::main`, inbound S3 round-trip is green, and the Stage 3 customer-loop canary invoke/readback is green with `errors_24h == 0` and `last_invocation` success. |
| Local dev mode and strict local signoff                                    | **Built — single-tenant on this host** | Local service replacements, commerce proof, cold-tier proof, HA proof, and browser preflight contracts are implemented. **Extended 2026-08-02 (`aug02_11am_4`, lane record `implemented/2026-08-02_remove_the_process_global_env_race_in_the_cold_tier_tests_by_giving_the_config_one_pure_seam.md`):** the cold-tier config no longer reads process-global env from inside the tests, which removed the order-dependent race between concurrently running cold-storage cases; the surviving half is the `cold_storage_endpoint_reads_env` flake row under `## Planned`. **NOT built, and it is the throughput ceiling on every browser proof: the local stack binds fixed host ports, so exactly one lane per host can bring it up.** See the "Local browser-proof stack is single-tenant" row under `## Planned` for the measured 2026-08-03 instance and the `FLAPJACK_PORT`/`LOCAL_MEILISEARCH_PORT` shared-default collision.                                                                                                                                                                                                                                                                                                                                                           |
| Provisioning, placement, replicas, discovery, and source migration        | **Partial — prod proven once; three-provider migration locally activatable, default closed; browser proof outstanding** | Current state: the production signup → provision → ingest → exact-search → zero-residue-cleanup proof is implemented and has passed once; provisioning rejects engines that fail health checks, while replacement remains detection-only. Owners: `scripts/validate_full_vm_lifecycle_prod.sh` for recurring production proof and `chats/icg/jul30_2am_0_three_provider_migration_and_gate_throughput_orchestration.md` for the provider-neutral migration chain. **MEASURED 2026-08-02 — this replaces the 2026-07-30/07-31/08-01 update blocks, which are archived in [History](implemented/2026-06-05_roadmap_v2_reshape_archive.md#platform-ledger-migration-history).** The customer-inspectable half is landed end to end and locally connected — three-identity closed union (`algolia`, `meilisearch`, `typesense`) with `has_adapter()` true for all three (the HTTP 400 `source_provider_unsupported` refusal is gone), untruncated per-item warnings, all three guides under `docs/getting-started/`, the backend preview owner at `infra/api/src/routes/migration/preview.rs` (`b2752ddf5`), per-provider `deny_unknown_fields` create DTOs normalized into one `CreateImportJobAdmissionRequest` (`7ea080304`), and the console preview step at `67bacdf68`. **Two residuals, both named and neither closed by the above:** (a) merge `b57a6e59d` landed `finished_incomplete` with its GREEN verification stage unrun, so adapter admission is not a completed proof; (b) the unmocked three-provider browser journey has never run and is blocked cross-repository — see the browser-proof row under `## Planned`. Exit unchanged: a fresh non-`--staging-only` RC repeats the exact-search/zero-residue lifecycle, and the migration chain reaches its declared served/browser acceptance before served activation. **EXTENDED 2026-08-03:** preview capability (`capabilities.rs:55`), preview media-type (`preview.rs:171`), cutover verify (`verify.rs`) and FC-5 source discovery (`3fd4098e7`) are on `origin/main`; `verify.rs:167-174` still refuses non-Algolia providers, so a vendor round-trip is owed. **Extended 2026-08-05 (FR-3 `f9a2e`, merge `9f0e8edfe9`):** the Caddy-bearing engine AMI is baked and one staging VM is proven end to end serving an authenticated customer search over public HTTPS with zero-residue teardown (staging AMI `ami-07bff4ef03ac9ad00`); fleet-wide rebuild and public-port removal remain FR-5. **Beta scope fact, stated so it is not rediscovered a fourth time:** `FJCLOUD_ALGOLIA_MIGRATION_ENABLED` defaults to `false` (`config.rs:103-107`) and staging/prod stay default-closed, so the 2026-08-08 paid beta ships with every source-migration surface OFF; the picker defect below is not customer-reachable until that flag flips, and flipping it is the gate on turning the differentiator on. **Extended 2026-08-06 (`aug05_1pm_1`, merge `60dcd3159`) — the console half of the untagged-union defect is fixed and locally connected.** `web/src/lib/api/client_normalizers.ts` is the single client-side owner that detects the hosted response arm structurally (by the presence of an `indexes` array, since no provider appears on the wire) and normalizes both arms into the one shape `MigrationCreateFlow.svelte` already reads, with per-arm known-answer tests asserting exact index names and document counts and a mutation check proving them red when the normalizer hunk alone is reverted. **The unmocked three-provider browser journey is still the one open half of this row**, it is still blocked, and its blocker is now precisely located rather than described as cross-repository in general: `6b492`'s parity proof certifies against a Flapjack commit that is not on flapjack `main`, so it must be re-run after the `1.0.11` pin bump. See the pin-drift row under `## Planned` for the ordered unblock chain. |
| AI Search / RAG, recommendations, personalization                          | Not current v1 | Keep outside the paid-beta launch path unless the strategic priority owner promotes it. **Corrected 2026-08-06:** dictionaries was dropped from this row — it was built past the scope line (full CRUD lifecycle with unmocked browser coverage at `web/tests/e2e-ui/full/index-detail.spec.ts:921`) and ships as a supported surface. Recommendations and personalization stay non-v1 for a concrete reason, not just scope preference: both consume customer behavioral events, and fjcloud has no customer event-ingestion endpoint, so neither can produce useful output for any customer. Visibility state for all three is owned by `docs/reference/ui_surface_support.md`. |
| Engine geo-search and configurable ranking formulas                        | Upstream       | Engine roadmap items, not fjcloud launch blockers.                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Live credential validation and deploy-path visibility                      | Built          | Distinct prod/staging envs, split deploy jobs, webhook split, `/version`, sync manifest, and deploy provenance are implemented.                                                                                                                                                                                                                                                                                                                                                   |
| Prod operations evidence                                                   | Mixed          | Prod canary, RDS restore, alarm coverage, secret distinctness, and VM lifecycle proof exist; listed reruns below keep current-main evidence fresh. **Extended 2026-07-22 (`jul21_10pm_2`, merge `84096fe8a`):** `scripts/probe_fleet_dataplane.sh` plus a `fleet_dataplane` row in `scripts/probe_live_state.sh`, registered as a hermetic contract gate in `local-ci --fast`. It is fail-honest by construction — un-probed fleets and indeterminate identity render `ACTION_REQUIRED`, never `SKIP`. Its first real run (`docs/live-state/20260723T075504Z/`) does exactly that: `ACTION_REQUIRED`, `reason=environment_attribution_ambiguous`.                                                                                                                                                                                                                                                                                                                                |

## Planned

Priority-ordered. Top rows are active or immediately next. **All priorities are Flapjack-focused.**

### Source migration expansion, and cross-repo process findings

| ID | Work item | Current plan | Evidence / owner |
| --- | --- | --- | --- |
| coordination-prose-unverified-cross-surface-claims-aug01 | Make cross-surface coordination claims re-derivable instead of trusting stale prose. | Extend the existing screen-spec freshness probe to scan `chats/icg`, require `Evidence:` for cross-surface claims, and preserve an own-surface negative control. | Source: `chats/suggestions/aug01_pm_fjcloud_dev__coordination_prose_asserts_facts_about_surfaces_the_writer_does_not_own.md`. Exit: `bash scripts/tests/probe_screen_spec_claim_freshness_test.sh` passes its exact three-way classification. |
| process-findings-owned-in-mike-dev | Five process findings fjcloud surfaced whose implementation is entirely outside this repository. | **Collapsed from five separate rows 2026-08-04, and the collapse is the point.** Every exit below runs against `mike_dev`, not this tree, so as five fjcloud-owned rows they asserted that fjcloud owns work fjcloud cannot do — and they consumed the whole 200-line budget, which is what made `roadmap-v2-shape` red and `origin/main` refuse every lane's push for four hours on 2026-08-03 (see `647503ed6`). They stay listed so fjcloud can still notice they were never implemented. Plan prose is not restated: each `Source:` file owns it, and `chats/suggestions/README.md` calls that directory a scratchpad agents do not read, so the falsifiable exits are inlined here instead of cited. (1) **cross-repo-producer-has-no-waiting-consumer-signal-aug02** — expose waiting consumers in the producer repo through reciprocal `# feeds:`/`# waits-on:` registry annotations. Exit: `python3 -m pytest matt_root/tests/test_supervise_doctrine_structure.py -x -q -k registry_edges` passes. (2) **revised-checklist-cannot-reach-a-stopped-worker-aug02** — refuse restarts when a materially revised source checklist differs from the persisted plan, via a plan-time content digest at the source-to-runtime translation seam. Exit: `python3 -m pytest matt_root/tests/test_stale_plan_guard.py -x -q` passes the mismatch, unchanged, and annotation-only cases. (3) **browser-stack-admission-is-a-gate-with-no-queue-aug03** — turn contended browser-stack admission into schedulable work. Exit: `grep -q 'contended host resource' pp/create_checklists.md; echo "contract=$?"` prints `contract=0`. **Its premise is now half-false and the row must be re-derived before it is implemented:** browser stacks are not scarce under the Playwright-managed stack, only under manual `local-dev-up.sh` — see the manual-stack row under `## Planned`. (4) **lane-completion-ignores-its-declared-external-proof-aug03** — a fail-closed, owner-bearing closeout disposition for a shipped batch whose declared external proof never ran. Exit: `grep -q 'shipping-with-unproven-external-surface' matt_root/scripts/closeout_artifact_check.py; echo "vocab=$?"` prints `vocab=0`; measured `0` matches 2026-08-04, so this is open. (5) **handover-owner-assignment-unchecked-against-target-scope-aug03** — validate that handover-owned work fits the named target's scope through `owner_scope_cite` validation at the intake boundary, covering matching, mismatching and no-owed-work cases. Exit: `grep -q 'owner_scope_cite' matt_root/matt/intake_input_validation.py; echo "validator=$?"` prints `validator=0`. | Implementing owner: **`mike_dev`** — no exit above is runnable from this repository. Sources, all under `chats/suggestions/`: `aug02_am_fjcloud_dev__producer_repo_has_no_signal_that_a_second_repo_is_blocked.md`, `aug02_am_fjcloud_dev__revised_checklist_cannot_reach_a_stopped_worker.md`, `aug03_am_fjcloud_dev__browser_stack_admission_is_a_gate_with_no_queue.md`, `aug03_am_fjcloud_dev__lane_completes_and_ships_while_its_handover_says_the_proof_never_ran.md`, `aug03_am_fjcloud_dev__owed_work_assigned_to_a_lane_whose_scope_excludes_it.md`. |
| MIG-SRC-CORE | Provider-neutral migration lifecycle and security | Pre-launch. Keep one job/status/cancel/ACK/erasure/publication path, preserve Algolia route compatibility, separate source vendor from destination cloud provider, and settle self-hosted endpoint SSRF/TLS/redirect/DNS-rebinding policy. **STATE 2026-07-30 (M1G-F `jul28_9pm_4`, merged):** fjcloud owns the exact closed union across enum/parser (`infra/api/src/models/algolia_import_job/provider.rs`), the durable SQL CHECK (`infra/migrations/069_widen_algolia_import_jobs_source_provider_check.sql`), public job JSON/OpenAPI, route admission, and the pinned engine discriminator fixture. The pre-M1G-F one-variant narrative is superseded and archived. **RESOLVED 2026-07-31 — the shared M2F create-flow credential defect is closed:** first-connect discovery publishes the connected and live API-key fingerprint atomically for unchanged inputs, later edits still invalidate the catalog, and mocked-Chromium journeys for all three providers pass with an exact provider-specific multipart mutation proof; backed on `main` for the credential-snapshot fix and the three-provider mocked journey (`web/tests/e2e-ui/mocked/migration_console_flow.spec.ts`, no skip anchors). **ADAPTER + CREATE-CONTRACT UPDATE 2026-08-02:** `has_adapter()` now returns `true` for all three identities, so the `source_provider_unsupported` refusal for Meilisearch and Typesense is stale; and `aug02_5am_2` (`7ea080304`) proved the neutral create path per provider — `create_import_job` takes `body: Bytes`, validates the provider first, then `deserialize_create_import_job_request` selects exactly one of `Create{Algolia,Meilisearch,Typesense}ImportJobRequest`; hosted identity and hosted `sourceIndex` normalize into the shared `source_connection_id` / `source_name`, admission downstream of that seam is not forked per provider, and all four DTOs redact through hand-written `Debug` and `Serialize`. Spellings were read against flapjack `origin/main` `d4c68443a` and match `MigrateFrom{Meilisearch,Typesense}Request` exactly. **Carried gap from that lane's force-merge — do not describe it as ordinary completion:** `preview_source_migration` (`infra/api/src/routes/migration/preview.rs:160`) still accepts opaque `Bytes` without first enforcing a JSON `Content-Type`; unrun work is a failing preview-route media-type contract, the shared boundary applied before proxying, and a proof the JSON path still reaches the engine. Falsifiable exit for the landed half, with a local PostgreSQL up: `(cd infra && cargo test -p api --test platform migration_routes_test) && (cd infra && cargo test -p api --test platform openapi_spec_test)`. Row stays open for end-to-end migration/security acceptance; admission and create-shape are not a served-traffic proof. | M0C: `chats/icg/jul26_am_11_source_migration_architecture_security.md`. M1G-E merged in flapjack at `f70b4d29b`. Activation owner `chats/icg/aug01_11am_1_source_provider_adapter_flag_activation.md` is discharged as of the 2026-08-02 adapter measurement above. **MIGRATION-GUARD AUDIT 2026-08-03, from `chatting/aug02_5am_roadmap_corrections_fc3.md` (merged FC-3 lane, unapplied until now).** Audited denominator: **108 guards** (99 async, 9 sync) across the registered `migration_routes_test` owners, superseding the stale 93 baseline — **86 REAL, 22 MOCK-CONSISTENT, 0 MOCK-CONTRADICTED**. The one contradicted specimen, `preview.rs::typesense_preview_asserts_current_engine_refusal`, was repaired to hit the real engine seam. Breakdown: [migration guard audit](implemented/2026-08-03_aug02_11am_batch_reconciliation.md#migration-guard-audit). **One gap survives — UNCOVERED-PATH: 1, and its owner is live:** the `{host, apiKey}` hosted-source discovery path is uncovered because `migration_routes_test.rs:778-795` hardcodes every request to `/migration/algolia/list-indexes`. FC-5 owns it plus the pinned `ListAlgoliaIndexesRequest` assertion at `openapi_spec_test.rs:435-443`. |
| MIG-MEILI | Meilisearch migration | Pre-launch, gated on MIG-SRC-CORE plus a pinned local exact-value source KAT covering records, primary key, settings, synonyms, tasks/quiescence, permissions, drift refusal, cleanup, and warnings.  **M3a LANDED 2026-07-29 (`6d34a013c`) — the local harness half is done.** `docker-compose.yml` now carries profile-gated, digest-pinned Meilisearch and Typesense services with `LOCAL_MEILISEARCH_PORT`/`LOCAL_TYPESENSE_PORT` overrides, opt-in startup in `scripts/local-dev-up.sh`, profile-aware teardown in `scripts/local-dev-down.sh`, seeding through `scripts/lib/local_source_providers.sh` + `scripts/lib/local_source_provider_evidence.py`, pinned fixtures under `scripts/tests/fixtures/source-migration/{meilisearch,typesense}/`, and a 1,472-line `scripts/tests/local_source_providers_test.sh` registered in `scripts/lib/test_reachability_manifest.sh`; **Stage 3 SSOT correction 2026-07-31:** `grep -c local_source_providers_test scripts/tests/serial_only_tests.txt` returns `0`, so the fast-lane serial scheduler still owns the exit that every suite mutating Docker or shared local-stack state appears in the serial registry. **Merged `--merge-incomplete` after only Stage 2 completed — Stages 1, 3, and 4 did NOT run:** the lane's RED/import proof, serial-registry and residue-free-teardown proof/handoff, `docs/LOCAL_DEV.md` handoff section, `docs/env-vars.md` registration of the new port variables, and planned in-place ROADMAP handoff are incomplete and are this row's surviving debt. M3a's Algolia arm has no local source and never will (Algolia is SaaS-only); the three-provider parity claim in M3b must say two-local-plus-one-cloud or be rescoped. **PROVIDER-CORE UPDATE 2026-08-02 (supersedes the 2026-07-30 refusal claim):** `meilisearch` is recognized by the durable/public closed union and the pinned engine discriminator, and `SourceImportProvider::has_adapter()` now returns `true` for it, so the HTTP 400 `source_provider_unsupported` refusal recorded here is stale. The engine translates ranking, typo tolerance, searchable attributes, faceting and synonyms (`engine/flapjack-http/src/handlers/migration/meilisearch_settings.rs`) and `supports_preview()` includes Meilisearch. Admission and translation are not a completed migration: this row's surviving debt is M3a Stages 1, 3 and 4 — the RED/import proof, the serial-registry and residue-free-teardown proof, the `docs/LOCAL_DEV.md` handoff, and `docs/env-vars.md` registration of the new port variables. | Flapjack M0A: `chats/icg/jul26_am_12_meilisearch_migration_contract_kat.md` |
| MIG-TYPESENSE | Typesense migration | Pre-launch, gated on MIG-SRC-CORE plus a pinned local exact-value source KAT covering typed schema, IDs, exports, aliases, synonyms/curations, permissions, drift refusal, cleanup, and warnings.  **M3a's Typesense container, fixtures and seeding landed 2026-07-29 (`6d34a013c`)** — see the MIG-MEILI row for the shared harness inventory and its incomplete Stages 1, 3, and 4. **PROVIDER-CORE UPDATE 2026-08-02 (supersedes the 2026-07-30 refusal claim):** `typesense` is recognized by the closed union and `has_adapter()` returns `true`, so the HTTP 400 refusal recorded here is stale. **But Typesense is the weakest of the three and this row must say so:** `supports_preview()` in `engine/flapjack-http/src/handlers/migration/mod.rs:268` matches only `Algolia | Meilisearch`, so a Typesense customer cannot use the pre-commit preview at all — the product's stated differentiator is absent for one of three advertised sources. There is also no `typesense_settings.rs` beside the existing `meilisearch_settings.rs`, so settings translation is unproven for this provider. Owner: flapjack `chats/icg/aug02_5am_5_typesense_preview_and_settings_translation.md` (authored, never dispatched). **RE-VERIFIED 2026-08-03 against flapjack `origin/main` `a61c7760e`, and still true:** `supports_preview()` is `matches!(self, Self::Algolia | Self::Meilisearch)`. fjcloud mirrors it honestly — `infra/api/src/routes/migration/capabilities.rs::engine_supported_migration_capabilities` maps Typesense to `preview: false` under an exhaustive match, so a new provider cannot silently inherit a permissive default, and `migration_capabilities` intersects it with route-mounted support. **Newly recorded here from `chatting/aug02_5am_roadmap_corrections_fc3.md`, which no reconciliation had applied until 2026-08-03:** the engine's published surface disagrees with its own handler — `engine/docs2/openapi.json` advertises `/1/migrations/typesense/preview` while the handler refuses it. A route-presence check against that OpenAPI therefore cannot establish Typesense preview support, and any lane tempted to use one must read the handler instead. Repairing the disagreement is flapjack's, not fjcloud's. Legacy text follows: refused with HTTP 400 `source_provider_unsupported` before source discovery, durable job writes, or engine requests. This is a contract-identity change only and does not claim a completed Typesense migration. | Flapjack M0B: `chats/icg/jul26_am_13_typesense_migration_contract_kat.md` |

| Priority | Feature                                                   | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------- | --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **P0**   | **Staging mirror CI deploy-gate blackout recovery** | **STAGING HALF CLOSED 2026-08-06; PROD HALF STILL OPEN — do not read this row as closed.** Staging-mirror run `31080535059` completed all nine required jobs successfully; `deploy-staging` and `e2e-deployed` ran, and live staging serves `dev_sha=b1fcfb3c3a80c0600ccbc44f6821eaf4cde375fb`, ancestral to `main`. **Prod was never in the same failure and is not fixed by the same repair, so it did not close with staging.** Measured live 2026-08-06 ~13:00Z: `bash scripts/canary/deploy_currency_check.sh` reports `env=prod status=breach deployed=154b121a8572 mirror_head=24f5efc2c4e1 ahead_by=2 oldest_undelivered_age=126h` (re-measured 2026-08-07: 2,032 dev commits behind)``; `curl -fsS https://api.flapjack.foo/version` still serves `dev_sha 99262771479dc6b8c5aeb2c20403300854883b86` with `build_time 2026-07-30T13:31:29Z`; and the prod mirror's newest CI push run is `30688550477` (2026-08-01, `failure`), last success `30546844879` (2026-07-30). So the prod mirror does not yet carry the `rust-test` repair that unblocked staging, and no `debbie sync prod` has been attempted since 2026-08-01 — prod's blackout is an unattempted sync, not a red gate. **Owner: FR-8** (`chats/icg/aug05_2am_8_prod_deploy_and_dataplane_proof.md`), Wave 4 of the paid-beta batch, gated behind Wave 3; it is not free-standing work and must not be dispatched ahead of that gate. **Prod exit:** one prod-mirror push run completes with `deploy-prod` succeeding, live `api.flapjack.foo/version` returns a `dev_sha` ancestral to `main` and not `992627714`, and `deploy_currency_check.sh` reports `env=prod status=current`. Historical diagnosis follows. One flow reaches customers (`docs/runbooks/deploy_surfaces.md`): dev `main` -> `debbie sync staging` -> staging-mirror CI -> `deploy-staging`. Its last green push run before the outage was `35719978d` (2026-07-30T16:40Z); the three since (all 2026-08-01) each failed `rust-test` with exit 124 — `timeout 900 cargo test --workspace` killed on a hang — skipping all three deploy jobs. Consequence measured 2026-08-05: staging served `a384a42e6` (1,580 behind `main`), prod `992627714` (1,626 behind); only two migrations pending on each (`070_admin_users.sql`, `071_admin_sessions.sql`), so the gap was code currency, not schema risk. **The hang was named and then FIXED AT ITS OWNER, not quarantined — the "UNDETERMINED" verdict this row used to carry is settled.** CI's `List full test inventory` diagnostic named the two hung `auth_admin` tests (`admin_vms_test::decommission_endpoint_{reports_blockers_when_reference_publication_wins,wins_concurrent_reference_publication_after_inventory_lock}`), 368 of 371 having reported, and the cause was neither a handler defect nor the one-connection route pool: `sqlx::migrate!` takes a **database-wide** advisory lock, so parallel tests that each own a fresh schema still convoyed on `connect_and_migrate`. FR-1 (`aug05_2am_1`, merge `7937b1d823`) fixed it in `infra/api/tests/common/support/pg_schema_harness.rs` with `migrator.set_locking(false)` behind a new `isolated_schema_migrator()` owner — sound precisely because each test owns its own schema and `_sqlx_migrations` via `search_path` — plus a bounded `SCHEMA_CLEANUP_DROP_WAIT` on `DbHarness::drop` so a timed-out test's leftover locks can no longer mask the original bounded failure. Mutation-checked; both tests run unignored today. **Why nobody noticed:** the dev repo keeps Actions disabled by policy, `local-ci.sh --full` runs the same suite on macOS where it passes, and no repo-owned probe read the mirror's CI conclusion. **Exit:** the two named tests are green or their defect is fixed at its real owner, one staging-mirror push run completes with `deploy-staging` succeeding, live `GET https://api.staging.flapjack.foo/version` returns a `dev_sha` that is an ancestor of `main`, and a repo-owned probe fails when the newest staging-mirror push run did not conclude `success`. |
| **P0** | **The deploy-currency alarm has never executed once in its life, on either mirror** | **CLOSED 2026-08-08 by live measurement; historical title retained.** `.github/workflows/deploy_currency.yml` is the hourly canary comparing each deployed `/version` against its mirror `main`, paging Discord when the oldest undelivered commit exceeds `DRIFT_MAX_AGE_HOURS` (default `24`, `docs/env-vars.md:208`). Its rule owner `scripts/canary/deploy_currency_check.sh` fails closed — every indeterminate arm (`version_probe_failed`, `github_probe_failed`, missing `ahead_by`, missing oldest-commit date) emits `status=breach` rather than defaulting to healthy. **It was unarmed on 2026-08-05 and is armed now.** Measured 2026-08-08: `Deploy Currency Drift` runs hourly on the staging mirror (last 23:52Z), checks BOTH envs in one job, and pages Discord (`HTTP 204`) — `env=prod status=breach reason=oldest_undelivered_too_old ahead_by=2 oldest_undelivered_age=161h`, `env=staging status=current`. The prod-mirror `skipped` is deliberate `if:` scoping; one alarm covers both envs. `docs/runbooks/alerting.md:137` makes disable/enable the deploy-freeze mechanism and requires the window be recorded; no such record exists, so this was a forgotten silence, not a deliberate one. **Remediated operationally on 2026-08-05, NOT yet in-repo:** the staging workflow was re-enabled and manually dispatched (run `31017763619`), which delivered both live breaches to Discord (`[OK] discord: HTTP 204`, twice) and exited `1` on `env=prod status=breach reason=oldest_undelivered_too_old oldest_undelivered_age=104h` and `env=staging ... 106h`. The re-enable was sufficient and took ~2h to take effect; an intermediate diagnosis that the cron needed a default-branch push is **withdrawn** — the push never happened. **The durable constraint that survives, and it is the one the probe must be built against: GitHub throttles scheduled runs on this mirror far below their crons.** Measured 2026-08-05, `outside_aws_health.yml` asks for `*/5 * * * *` and its actual consecutive gaps were 91, 132, 98, 119, 159, 167 and 200 minutes. **Any liveness window derived from a cron string would false-alarm constantly** — it must come from measured intervals, taking the ~200-minute observed worst case as its floor. **Liveness re-verified 2026-08-06 ~13:00Z, and the operational half now holds:** `gh api repos/gridl-infra-staging/fjcloud/actions/workflows` reports workflow `315412074` `state=active`, and its run history has `total_count=10` where it was `0` — the workflow has genuinely started executing rather than merely being toggled. Its three runs today (`31079787240` 07:08Z, `31091726421` 10:04Z, `31101150729` 12:22Z) each concluded `failure`, which is the alarm correctly reporting the live prod breach recorded in the row above, not a broken canary; the paired prod-mirror runs conclude `skipped` by the guarded `if:`, which is the expected arm and must not be flagged. **In-repo half LANDED 2026-08-06 (`9ef92`, merge `443e6d685`):** `scripts/probe_scheduled_alarm_liveness.sh`, a 593-line contract test, a six-row denominator (`scripts/tests/scheduled_alarm_workflows.txt`, windows measured not cron-derived), and `schedule scheduled-alarm-liveness-contract` in the routine gate set; indeterminate evidence is never shadowed. **OPEN on one load-bearing thing:** `gate_scheduled_alarm_liveness_contract` (`scripts/local-ci.sh:962-991`) is warning-only for every real liveness verdict - `schedule_run_missing`, `stale`, `not_completed`, `unexpected_skip`, `red_unacknowledged` each emit `SHADOW_WARN` and `return 0` - so it cannot fail on the condition that caused this outage, and its named promotion owner FG-3 was superseded without being dispatched, so nothing owns the removal. **Exit:** delete the shadow after two routine-local runs record `summary checked=6 passed=6 failed=0` with no `SHADOW_WARN`, plus a contract case proving non-zero exit on a `schedule_run_missing` fixture. |
| **P1** | **`6b492`'s source-migration parity proof certifies against a Flapjack commit that is not on flapjack `main`** | **PIN HALF RESOLVED 2026-08-06 on `main` (`0373d6118`, `58a751424`, `0809515ef`, `34610da9f`); lineage half still OPEN.** The pin defect was never fixable by bumping. `FJCLOUD_FLAPJACK_VERSION` has two consumers needing incompatible semantics: `ci.yml` and `scripts/devbox/fetch_flapjack_release.sh` download the EXACT tag `v${pin}`, while both runtime classifiers compared it to the engine's version by string equality. flapjack bumps `main` when work lands and cuts the release later, so in that window no value satisfies both — an unpublished version 404s `local-dev-up-smoke` (in both deploy `needs[]`), and lagging rejects every local source build. **Landed:** the pin is now a FLOOR, implemented in `scripts/lib/local_stack_contract.sh` and `infra/api/src/services/flapjack_proxy/mod.rs` against one shared spec (`scripts/tests/fixtures/flapjack_version_floor_cases.json`) both suites read; numeric three-component compare, prereleases refused not suffix-stripped, new `version_unparseable` fails closed; pin at `1.0.11`, verified first against the published musl manifest (`version 1.0.11`, `revisionKnown true`, `artifact.sha256` = GitHub's asset digest); `flapjack_identity_rejection_message` now names observed version, floor, binary, provenance and remedy, and rules out repointing at an older checkout; `FJCLOUD_FLAPJACK_VERSION_OVERRIDE` retired, since under a floor it could only admit an OLDER engine. Old clause (b)'s literal list was wrong both ways: the four files it named are hermetic and needed no change; `local_stack_contract_test.sh`, `local_dev_up_test.sh` and `local_source_providers_test.sh` were unnamed and broke. **DELIBERATELY NOT BUILT — do not rebuild it: old clause (c)'s `engine/Cargo.toml`-versus-pin currency check.** Under a floor, forward drift is a non-event, so it has no recurring case; and its mandated fail-closed branch for a checkout with no `engine/Cargo.toml` breaks CI unconditionally, because `local-dev-up-smoke` points `FLAPJACK_DEV_DIR` at an extracted release tarball with no manifest and no git repo, then runs `local_demo.sh` -> `local-dev-up.sh`. This repo already shipped that exact class of break once (`scripts/lib/local_stack_contract.sh:5-11`). **STILL OPEN, and what actually gates `6b492`:** its parity receipt (`docs/runbooks/evidence/source-migration-parity/2026-08-05-stage3/receipt.md:13`, on that branch only — the directory does not exist on `main`) certifies against Flapjack `b767c6dc5`, and `git merge-base --is-ancestor b767c6dc5 origin/main` exits `1`: it lives only on unmerged `codex/aug05_fjcloud_stage2_v1010_current_count`, whose `DocumentRevision` Typesense code is absent from flapjack `main`, while its Meilisearch half IS on `main` at `9df4cc76a`. That browser green came from an engine nobody can reproduce. **Exit:** `6b492` re-runs parity against a `main`-built engine and records the engine commit in its receipt, with the ancestry assertion at receipt-validation time — it belongs with `6b492` because the evidence directory does not exist on `main`, and it must NOT be a launcher-time refusal, which would break legitimate co-development against a flapjack branch. No longer blocked on the pin. |
| **P0** | **Security hardening program** | **OPEN - new 2026-07-30.** Findings are deliberately NOT in this row. `ROADMAP.md` is in the `.debbie.toml` sync whitelist and publishes to two PUBLIC mirrors, so this row stays a pointer; security measurements belong in the unsynced `docs/security/` tree. Owners there: `control_baseline.md` (measured state of 67 controls, each with a verification command whose match count is recorded), `hardening_spec.md` (eight waves, red-test-first acceptance, done-conditions), `README.md` (claim vocabulary, evidence discipline, corrections log). Three P0-shaped and several P1 gaps are recorded there, all local-build work; live activation stays parked under `PROJECT_OVERVIEW.md`. Before writing security detail into any file, check whether its tree syncs - `infra/`, `web/`, `ops/`, `scripts/`, `tests/`, `docs/runbooks/`, `docs/getting-started/`, and `docs/screen_specs/` all publish. Exit: each wave's done-condition as stated in the spec. **WAVE 0/1 STATUS 2026-07-31 — five lanes merged, Wave 2 half-run, Wave 3 not started.** Merged: check consolidation (one surviving `security_checks.sh`, dependency audits fail closed and scheduled), the rendered-cloud-init Caddy ordering Rust test, database TLS enforcement on non-local `DATABASE_URL`s, the ASVS L2 extraction that created `docs/security/requirements.md` as the normative owner beside the descriptive `control_baseline.md`, and a read-only live-row re-probe writing dated bundles under `docs/live-state/`. In flight: `jul30_10pm_6` (baseline integrity probe) on an unmerged branch. **Blocked: `jul30_10pm_7` (operator identity) dispatched 2026-07-31 06:15 and stopped 24 seconds later at 0/5 stages carrying only its `baseline: pre-session scrai/tooling sync` commit.** Both Wave 3 lanes (`jul30_10pm_8` admin-session revocation, `jul30_10pm_9` transactional audit) gate on it, so `ADMIN_SENTINEL_ACTOR_ID` and the `ADMIN_KEY`-signed session are untouched and the Wave 1 done-condition in the spec is unmet. Re-dispatching lane 7 is the single highest-leverage action in this program. **WAVE 3 STATUS 2026-08-01 — the gating lane cleared and two of three Wave-3 lanes are resolved; one is stranded.** `jul30_10pm_7` (operator identity) re-dispatched and MERGED (`76ecfe063d`, 7/7 stages), which unblocked the wave. `jul30_10pm_8` (admin session revocation) MERGED (`4500055da`) — durable API-owned admin sessions, see the admin-tooling row in the Platform Ledger. **WAVE 3 STATUS 2026-08-02 — transactional audit is MERGED; the program stays OPEN under its per-wave done-conditions.** `jul30_10pm_9` merged at `2e8fa6c25` and its branch is `0` commits ahead of `origin/main`; it landed the transactional audit-writer seam, fail-closed impersonation audit, transactional customer suspension, and atomic hard-erasure audit. The terminal closeout lane `jul30_10pm_10` is IN FLIGHT (worker `02919`, 2/5 stages at 2026-08-02T10:00Z) and owns re-deriving every `docs/security/control_baseline.md` row the nine merged lanes touched; until it closes, no combined security claim for this batch is measured. Exit unchanged: each wave's done-condition in `docs/security/hardening_spec.md`. **Wave 5 web-surface status 2026-08-03 (`aug02_11am_1`, merge `ac6483e82`) — enforced, not report-only:** `web/svelte.config.js::createDocumentCspDirectives` installs the CSP through `kit.csp.directives` at `kit.csp.mode = 'auto'` with no surviving report-only policy, and `web/src/lib/server/security_headers.ts::applyDocumentSecurityHeaders` owns HSTS, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy` and `X-Robots-Tag`. Password policy (`aug02_11am_2`, merge `84655a059`) landed the same wave; see the signup-abuse ledger row. **Deliberately deferred from this program by the `aug02_11am` orchestration, recorded so the reason is inherited rather than re-derived:** native passkeys / WebAuthn / TOTP — see the customer-auth row below. |
| **P0** | **No stage owns the merged-union gate, so `main` can sit red across a whole batch** | **OPEN; the exact historical title is retained.** Ownership is installed at `scripts/close_batch_union_gate.sh`, backed by `scripts/tests/close_batch_union_gate_test.sh`; both objects exist on current `origin/main`. The batch closeout contains exactly one `## Merged-union gate` section, but it records delegated `bash scripts/local-ci.sh --fast` exit `1` and contains no pre/post SHAs. It therefore cannot close this row for current `origin/main` `caa42abf3`. Exit: the wrapper records one current merged-main receipt with the real delegated exit `0`; never run `local-ci --fast` beside the wrapper or invent a second receipt owner. **DEMONSTRATED 2026-08-04, and the demonstration is the argument for this row.** The FC-5 merge `3fd4098e7` passed every one of the six `.batman.toml` merge gates and reddened `main` in three separate places, none of which those six gates cover: `web-test` (the OpenAPI drift guard — a real frontend/backend contract break, see the source-index picker row), `test-reachability-contract` (`provider_migration_guides_test.sh`, whose hardcoded boundary markers pinned pre-FC-5 copy), and `handover-consumption` (FC-5's own handover, unconsumed the instant it landed). A merged-union run on that tree reported `Totals: pass=39 fail=4`. Two of the four remaining failures were `algolia_migration_parity_live_probe_test.sh` and `apply_ses_log_read_policy_test.sh`, both **timed out** rather than asserting anything, on a host running a concurrent batch — so the union number itself is contention-sensitive and a red union needs a quiet-host re-run before any lane is blamed for it. All three real breaks are repaired; the point that survives is that the merge gate set cannot see the gates that actually broke. **Two follow-on facts about the timeouts, measured rather than assumed.** The reachability watchdog is `REACHABILITY_SUITE_TIMEOUT_DEFAULT_SECONDS=900` (`scripts/local-ci.sh:193`), so both suites ran fifteen minutes at concurrency 8 without finishing. Run individually they were still emitting `PASS` lines at 300s with no `FAIL`, so they are slow rather than broken — but whether they finish inside 900s on a QUIET host is still unmeasured, and re-running them under the same contention could not answer it. Second: the per-suite timing receipt `test_reachability_slow_suites.tsv` is written into a `mktemp -d` and kept only at `${TMPDIR}/local-ci-last-logs`, which **the next `--gate` invocation of any kind overwrites** — so the evidence for why a suite timed out is destroyed by the next thing you run, which is what happened here. Exit for that half: retain the timing receipt per run rather than per last-run, so a timeout can be diagnosed after the fact instead of only while it is happening. **A second blind spot, measured 2026-08-07, same class as this row.** `ops/terraform/` holds **30** `tests_*.sh`; `scripts/local-ci.sh` invokes **four** (`tests_iac_validation_static.sh`, `tests_publish_scripts_buildx_static.sh`, both `tests_flapjack_ami_pointer_*`). The other **26 are invoked by nothing**, and `scripts/probe_test_reachability.sh` cannot notice: its corpus is `scripts/tests/` only (`:11`), so the gate meant to catch unreachable tests is structurally blind to this directory. Live instances: `aug06_10pm_1` put its P0 count assertion in `tests_stage1_static.sh:35`, and `aug07_3am_3` proposes six more in `tests_stage4_static.sh` — none execute. **Exit:** extend the reachability corpus to `ops/terraform/tests_*.sh`, then wire or retire each of the 26 **Triaged 2026-08-07 by running the 25 runnable: 18 GREEN guard nothing (1,379 assertions) — wiring is free, starting with `tests_stage1_static.sh` (46/46), which holds the P0 count assertion. 5 RED are unseen drift (`tests_stage{4,5,6}_static.sh`, `tests_stage7_preflight_unit.sh`, `tests_support_email_canary_static.sh`). 2 are sourced fragments.** |
| P1 | Lane authoring outruns lane dispatch — 17 reviewed lanes have never started | **OPEN — new 2026-07-31, and it is the same finding the 2026-07-30 header recorded at ten lanes, now measured at seventeen.** Authored, reviewed, zero branches: `jul30_pm_{4,5,6,7,8,10}`, `jul28_9pm_{7,8,9}`, `jul30_10pm_{8,9,10}`, `jul28_11am_5`, `jul29_11am_9`, `jul28_6am_10`, `jul30_2am_4`; plus `jul30_10pm_7`, which dispatched and stopped 24 seconds later at 0/5 stages. Re-derive with `for l in <lane-names>; do printf '%s %s\n' "$l" "$(git branch -a --list "*$l*" | wc -l)"; done`. The ceiling is neither authoring nor review — both keep up — it is concurrent-lane capacity against a host-exclusive ~21-minute `--fast` gate that every lane must pass to merge. Compounding effect: because closeout lanes are always last, undispatched work and undocumented closed work grow together. Exit: the authored-with-no-branch count, measured on two consecutive days, is strictly decreasing. **CORRECTED 2026-08-01 — the original count was a measurement artifact.** `batman merge_worktree` prunes a merged lane's branch, so a bare ref check cannot tell merged-and-pruned from never-dispatched; the sound probe is the anchored merge-subject grep `git log --all --format='%s' | grep -cE "^batman merge_worktree: merge batman/<lane>(_[^ ]*)? into "`, because lane ids share prefixes. Under it the genuine 2026-07-31 set was eight, not seventeen. [Correction detail](implemented/2026-08-03_aug02_11am_batch_reconciliation.md#dispatch-backlog-composition). **REMEASURED 2026-08-02 — a critical-path problem, not a counting one.** Of the `aug02_5am` batch's six lanes, three merged and three had no branch: FC-3, FC-5, FC-6. FC-6's non-dispatch is why the three `chatting/aug02_5am_roadmap_corrections_fc{1,2,4}.md` handovers sat unconsumed and this file was reconciled by the operator instead of its declared writer. **The shape is stable across batches and worth naming: the terminal reconcile/close lane is always authored last and dispatched last, so whatever stops the batch stops the lane that would have recorded what the batch did.** Exit is unchanged and still not met. **REMEASURED 2026-08-03, and a first pass at 30 was withdrawn as a method artifact — it counted lanes that had already been dispositioned and lanes that merged without a `batman merge_worktree` subject.** Disposition-aware count: **17**, which is not an increase over the 17 in this row's title; earlier figures in this row were taken by three different methods, so no trend claim between them is supported. The durable finding is composition, not count: **6 of the 17 are batch-close/reconcile lanes** (`aug02_5am_6`, `jul25_pm_6`, `jul26_pm_12`, `jul28_9pm_9`, `jul30_2am_4`, `jul30_pm_10`), which is the same starvation the closeout-debt row owns. **The retirement mechanism already exists and works** — a lane retires by carrying a `Disposition: <stem> superseded|abandoned|carried|merged` line in its first lines, which `closeout_artifact_check.py --roster` reads; the eleven `jul29_8pm_{3..13}` shard lanes and `aug02_11am_5` all carry one and are correctly out of the backlog. **Method, and use this one:** exclude `*_orchestration.md` and `_followups.md`, exclude any file whose head carries `Disposition:`, then require both a branch probe and an anchored merge-subject probe to be empty. Full list: [dispatch backlog composition](implemented/2026-08-03_aug02_11am_batch_reconciliation.md#dispatch-backlog-composition). **REMEASURED 2026-08-07: the class changed from undispatched to unlanded.** Fifteen branches carry unmerged work; `git merge-tree --write-tree main <branch>` reports thirteen conflict-free, including `aug07_3am_3` (4/4 stages, 10 ahead) and `aug07_10am_0` (41 ahead). Exit for this class: a lane reaching its terminal stage merges or records a disposition within 24h **2026-08-07:** `batman status` shows the in-flight stage, so a stopped lane over-reports by one; trust the lane file annotation |
| **P1** | **Three-provider migration browser proof — the last open half of the P1 differentiator, and no longer blocked** | **OPEN — new 2026-08-02, replacing the refugee-conversion-depth row whose three exits all closed (archived in [implemented/2026-08-02_roadmap_header_and_refugee_depth_archive.md](implemented/2026-08-02_roadmap_header_and_refugee_depth_archive.md)).** Every local layer a customer touches when migrating from Algolia, Meilisearch or Typesense is now on `main` — adapter admission, per-provider create contract, backend preview owner, console preview step, untruncated warnings, three guides. What has never run is the unmocked browser journey across all three providers. **The blocker is a single undelivered cross-repo contract, and the dependency chain is four deep:** flapjack MIG-12 (`chats/icg/aug02_1am_0_mig12_provider_neutral_source_migration_core.md`, worker `3db8b`, IN FLIGHT at 3/4 stages) → flapjack FJ-4 (`chats/icg/aug02_5am_4_neutral_source_discovery_contract.md`, authored, **never dispatched**) → fjcloud FC-5 (`chats/icg/aug02_5am_5_neutral_source_discovery_consumer.md`, authored, **never dispatched**) → the browser proofs themselves (`chats/icg/jul30_pm_8`, and `chats/icg/jul28_9pm_7_m3b_three_provider_parity_browser_proof.md`, stopped at 3/4 with +1,930 lines of fixtures and a deliberately-RED spec on its unmerged branch). Until FC-5 lands, `/migration/{source_provider}/list-indexes` deserializes `ListAlgoliaIndexesRequest` for every provider and a Meilisearch `{host, apiKey}` request fails HTTP 422 at the Axum extractor. **Falsifiable dispatch gate for FC-5, both probes must pass:** `git -C <flapjack_dev> grep -q "migrations/typesense/list-indexes" origin/main -- engine/docs2/openapi.json` and `git -C <flapjack_dev> cat-file -e "origin/main:engine/docs2/4_EVIDENCE/2026_08_02_aug02_5am_4_neutral_source_discovery_receipt.md"`. Both measured RED 2026-08-02T10:15Z. **REMEASURED 2026-08-03: BOTH PROBES ARE NOW GREEN.** Flapjack merged FJ-4 (`batman/aug02_5am_4_neutral_source_discovery_contract` at `3d0d2a7a4`/`11ae7984f`); at flapjack `origin/main` `a61c7760e` the receipt file exists and `engine/docs2/openapi.json` publishes `/1/migrations/{algolia,meilisearch,typesense}/list-indexes`. **FC-5 MERGED 2026-08-04 at `3fd4098e7`** (`chats/icg/aug02_5am_5_neutral_source_discovery_consumer.md`, worker `87364`, 4/4 stages, 99/104 tasks) after sitting complete-but-unmerged for the whole `aug03_11am` batch; `/migration/{source_provider}/list-indexes` now deserializes per provider, so a Meilisearch `{endpoint, apiKey}` request no longer fails HTTP 422 at the Axum extractor. **The chain is now zero deep and the unmocked browser journey is the only remaining work — it is the single highest-leverage dispatchable item in this repository.** `jul28_9pm_7`'s branch carries +1,930 lines of provider-parity fixtures and a deliberately-RED spec, but it CONFLICTS with `main` (`git merge-tree --write-tree`), so it must be rebased onto current `main` and re-derived, not lifecycle-merged. **FC-5's handover (`chatting/aug02_5am_roadmap_corrections_fc5.md`) is applied here.** Its Platform-Ledger correction was already carried above (`model-boundary refusal is gone`). Its two live claims: `git merge-base --is-ancestor bc1eb8868 origin/main` exited `0`, so **M3b (`jul28_9pm_7`) now needs nothing from any other lane** — only a rebase; and **B4 (`jul30_pm_6`) inherits FC-5's `host` → `endpoint`/`node` request-spelling correction** in `migration_create_client.ts` and `types_algolia_migration.ts`, the hosted-discovery response typing, and the self-hosting copy fix in `MigrationHostedSearchConnection.svelte` — one more reason `jul30_pm_6` is a per-file salvage, not a merge. **Exit:** a green unmocked three-provider browser run, receipt under `docs/audits/migration-preview/<UTC>/`. |
| **P1** | **Cutover verification ships for 1 of 3 providers and has no vendor round-trip proof** | **OPEN — FQ reconciliation 2026-08-04.** API and console ownership from the 2026-08-03 row still stands: `POST /migration/:source_provider/verify` is registered, the retained-job console is reachable, and `capabilities.verify` drives controls. FQ-4 narrows gap (b) again: `verify_seeded_local_source_red_proof` seeds a local Flapjack source through `FJCLOUD_ALGOLIA_SOURCE_BASE_URL` and asserts full-body equality against `verify_support.rs::expected_parity_report()`, while `verify_maps_unreachable_source_to_labelled_backend_error` covers the real transport-failure arm; current-tree probes find those owners in `infra/api/tests/integration/migration_routes_test/verify*.rs`. This proves source comparison against a real local search engine, not Algolia's vendor service. Gap (a) is unchanged: capability publication and route admission remain Algolia-only, and Meilisearch/Typesense verify still returns `source_provider_unsupported` before source I/O. Detail and handover/tree classifications: [aug03 8pm FQ reconciliation](implemented/2026_08_04_aug03_8pm_handover_reconciliation.md). **Exit, in order:** one Algolia cutover verified end to end against a real Algolia application; then either add source-query capability for the other providers with one proof each, or record an explicit beta decision that cutover verification is Algolia-only and make the console say so. |
| **P1** | **The source-index picker reads the wrong shape for Meilisearch and Typesense** | **OPEN — new 2026-08-04, introduced by the FC-5 merge `3fd4098e7` and caught by the `openapi-drift` guard going red on `main`.** FC-5 made `POST /migration/:source_provider/list-indexes` provider-neutral by returning `ListSourceIndexesResponseBody`, an **`#[serde(untagged)]`** union of two structurally incompatible shapes (`infra/api/src/routes/migration/source.rs:236-241`): Algolia answers `{items, nextCursor}` via `list_algolia_indexes`, and Meilisearch/Typesense answer `{indexes, limit, offset, total}` via `list_hosted_source_indexes` (`source.rs:384-404`). Untagged means **there is no discriminator on the wire**. The frontend was not widened with it: `web/src/lib/api/migration_client.ts:135-141` types the result `Promise<AlgoliaSourceListResponse>` for every provider, and `web/src/lib/components/migration/MigrationCreateFlow.svelte:619,633` reads `page.items` and `page.nextCursor` unconditionally. **It is reachable, not latent** — `MigrationCreateFlow.svelte:309-313` switches `sourceProvider` from the console and `MigrationHostedSearchConnection.svelte` already renders the Meilisearch/Typesense connection form, so a customer who picks a hosted provider gets `undefined` where the index list should be. This is the two providers fjcloud can actually run locally. **FC-5 knew:** `chatting/aug02_5am_roadmap_corrections_fc5.md` hands "hosted discovery response typing" to B4 (`jul30_pm_6`), whose branch is unmerged and CONFLICTs with `main` — so the owed half never landed and nothing failed until the drift guard fired. **What is already done:** `web/src/tests/openapi-drift.test.ts` now pins the union's exact arms through the existing `oneOfSchemaDescriptors` owner rather than asserting a single ref, which is strictly stronger — both a narrowed and a widened union go red, mutation-checked. That restores `main`, it does not fix the client. **NARROWED 2026-08-06 by `aug05_1pm_1` (merge `60dcd3159`) — it does NOT close.** The discrimination decision is made, recorded at its owner, and shipped: the client-side arm was taken, not the wire-tagging arm, because the provider is not on the wire and a provider check would therefore be wrong. `web/src/lib/api/client_normalizers.ts` is the single owner — `isHostedSourceListResponse`, `hostedNextCursor` and `normalizeMigrationSourceListResponse` detect the hosted arm by the presence of an `indexes` array and project it into the one `AlgoliaSourceListResponse` shape every downstream caller already reads. Both response arms and the pagination invariants carry known-answer tests asserting exact index names and document counts, mutation-checked red when the normalizer hunk alone is reverted. **What remains open is exactly one clause and it is not re-homed:** the browser proof that selects Meilisearch in the console and asserts real index rows render. It stays with `chats/icg/aug04_9pm_0_m3b_shared_vm_admission_and_parity_green.md` (`6b492`), whose branch already carries `web/tests/e2e-ui/full/source_migration_provider_parity.spec.ts` and its fixtures, and it is blocked behind the pin-bump chain on the row above — not behind any further client work. **Exit, and do not close it with a type change alone:** decide the discrimination — either tag the wire at the Rust seam or normalize both arms behind one client-side owner — then prove it with a known-answer test per arm plus one browser proof that selects Meilisearch in the console and asserts real index rows render. A passing `svelte-check` is not the exit; the current code type-checks clean and is still wrong at runtime. |
| **P1** | **The local browser-suite union remains open; scoped FQ slices are green** | **NARROWED 2026-08-04 by FQ-1/FQ-2, not closed.** FQ-1's six-file console surface reported 38 total / 34 passed / 0 failed / 4 skipped and mutation-checked daily-chart overlap, support disclosure, create-detail redirect, and dashboard load assertions; FQ-2's eight-spec customer/billing slice reported 32 total / 29 passed / 0 failed / 3 hosted-Stripe skips. Current-tree probes find the FQ-1 SVG text-box owner; the FQ-1/FQ-2 merge subjects are on `HEAD`. FQ-3 is not accepted as landed evidence: its handover is on `origin/main`, but `git log --all --format=%s | grep -Fc "batman merge_worktree: merge batman/aug03_8pm_3_browser_suite_admin_and_staging_verify into main"` returned `0`. The corrected union boundary from the previous row remains load-bearing: first define a local denominator that can go green, repair the canonical clone admin setup, and guard or exclude staging/live-Stripe-dependent specs; then run merged-main `cd web && npm run test:e2e -- --reporter=line --max-failures=0`, record the stated denominator with `0 failed`, and name any residual spec owners. Detail: [aug03 8pm FQ reconciliation](implemented/2026_08_04_aug03_8pm_handover_reconciliation.md). |
| **P1** | **The browser suite's last red, plus a devbox that no session owns** | **OPEN — new 2026-08-05.** Best clean measurement, devbox against merged `main`: **334 passed / 5 failed / 15 skipped**. **(a)** `web/tests/e2e-ui/full/unified-search.spec.ts:78` fails only in the full suite and passes 3/3 alone. A 30s budget masked it — `gotoIndexDetailWithRetry` costs up to 45s (15s ladder + 30s post-ladder expect) — so it died mid-wait blaming a timeout. `eaf1fb490` gave it the 180s budget every other caller declares, and the real error surfaced: after 48s the page still has no `getByRole('heading', { name: <index> })`. It seeds via shared `seedIndex` and navigates at once; its three siblings build a fresh customer, wait for readiness, and pass in 2.6-3.8s. **Cause unproven — measure before assuming the sibling pattern is the fix.** **(b) The devbox has no ownership or locking, and concurrent use silently corrupts runs.** Three consecutive full runs scored 201-243 instead of 334, failing on `ENOENT tests/fixtures/.auth/*.json` while all four setup projects reported passed. Cause: `sync_to_devbox.sh` runs `rsync -a --delete` (`:92`), and `.auth/` is gitignored so it is absent from the source and deleted on every sync; a second session was syncing and running throughout. Proof it was rsync and not the suite: `web/tests/fixtures` mtime moved BACKWARD (18:29 -> 17:36), which only `rsync -a` restoring source mtimes can do, and a foreign `vite dev --port 5173` had been up 43 minutes. This is the same class of defect as the host-exclusive `--fast` mutex, one host over. **Consequence: any devbox browser number is untrustworthy unless the box was exclusively held.** **(c)** The 4 Stripe rows still fail closed there; `scripts/devbox/run_browser_suite.sh` (the run step the repo never had) withholds the key by default and gates it behind `--stripe-live`, because `infra/api/src/startup.rs:84-94` switches the whole stack to `LiveStripeService` when `STRIPE_SECRET_KEY` is set. Whether a key helps is **unmeasured** — the run that tried was invalidated by (b). Exit: `:78` passes in a full-suite run with its cause named; the devbox has an ownership mechanism; the Stripe question is re-measured on an exclusively-held box. |
| **P1** | **Managed API-key lifecycle enforcement is partial** | **PARTIALLY ENFORCED 2026-08-04 by FQ-5.** Managed-key expiry and index allowlists remain owned by `ApiKeyAuth` as previously recorded. FQ-5 adds request-path enforcement for two advisory fields on the sole current `ApiKeyAuth` route consumer (`infra/api/src/routes/discovery.rs::discover`): `source_ip_allowed` rejects peers outside `restrict_sources` using only the `ConnectInfo`-derived trusted socket peer, and `enforce_api_key_hourly_limit` calls `RateLimiter::check_with_limit` for `(api_key_id, client_ip)` buckets, returning 429 with `Retry-After` and body `{"error":"too many requests"}`. Current-tree probes find those owners and exactly one `auth: ApiKeyAuth` route consumer. **Bias remains explicit:** restart resets counters, each API instance grants its own budget, JWT search traffic and engine-issued `flapjack_proxy` keys are outside this boundary, and `max_hits_per_query` is still not enforceable because no `ApiKeyAuth` route accepts a per-query hit count. **Exit:** add a hit-count-bearing request owner or decide the field's fate, and define coverage for engine-issued keys and non-`ApiKeyAuth` traffic before claiming complete lifecycle enforcement. |
| P2 | Manual local dev stack is single-tenant; the Playwright-managed stack is not | **DERIVATION SHIPPED; LIVE ACCEPTANCE REMAINS A GAP (FQ-6, 2026-08-04).** The earlier narrowing still stands for browser lanes: Playwright-managed stacks derive per-worktree web/API/Flapjack ports and compose project names, so they are not the host-wide bottleneck. FQ-6 shipped the manual-stack mirror: `scripts/local-dev-up.sh` now calls `scripts/lib/playwright_port_plan.sh::playwright_apply_manual_stack_port_defaults`, deriving all seven manual-stack host ports from the TypeScript-owned arithmetic while preserving explicit overrides and leaving `assert_no_host_port_collisions` as the guard owner. Stage 3 produced two disjoint seven-port plans, but the live two-stack receipt was `ACCEPTANCE=GAP_SPEC`, not `BOTH_HEALTHY`, because Colima did not expose detached `/private/tmp` bind sources; it also surfaced that separately launched `scripts/api-dev.sh` does not inherit the manual stack child's derived Flapjack, Mailpit, or S3 endpoints. **Exit:** rerun from a Docker-shared detached path or safely mount `/private/tmp`, prove both manual stacks pass Postgres/Flapjack/SeaweedFS/Mailpit/Meilisearch/Typesense health with disjoint ports, and decide or repair the `api-dev.sh` endpoint inheritance gap. |
| **P1** | **The browser stack compiles two Rust binaries inside Playwright's fixed startup window** | **OPEN — new 2026-08-04, and the first diagnosis was wrong in a way worth keeping.** Two browser-census attempts failed at two different points and both were the same defect. **Attempt 1** died at `Error: Timed out waiting 600000ms from config.webServer` (`PLAYWRIGHT_WEB_SERVER_TIMEOUT_MS`, `web/playwright.config.contract.ts:30`); the build log at `.local/flapjack-source-receipts/flapjack-build.*` held exactly one line, `Blocking waiting for file lock on artifact directory` — every fjcloud lane builds the engine into the same `flapjack_dev/engine/target` and Cargo serialises them. That looked like the finding. **Attempt 2, with the engine already built, got past flapjack and died at `[playwright_local_stack] ERROR: API did not become ready at http://localhost:8275/health`** — and `cd infra && cargo build -p api` was measured still *compiling* (`aws-sdk-ec2`, `aws-sdk-ssm`) at 150s, not lock-blocked. **So the class is not the lock.** `scripts/playwright_local_stack.sh` compiles the flapjack engine AND runs the fjcloud API through `cargo run` inside the webServer window; either exceeding it kills the run. A Cargo lock is one way to exceed it, an ordinary recompile after any Rust merge on `main` is another, and **the error names neither** — it always reports a web server timing out. That is why a lane will misdiagnose this, and very likely why `aug02_11am_8` was recorded as port contention. Host context that makes it routine rather than rare: 8 concurrent matt sessions, load average 51/67/88 on 18 cores. **Exit:** pre-build both binaries before Playwright starts, and make a compile that overruns say so instead of expiring a timeout that names the wrong subsystem; optionally give each worktree its own `CARGO_TARGET_DIR`. **Mitigation that works today**, in this order: `(cd infra && cargo build -p api)`, then `(cd "$FLAPJACK_DEV_DIR/engine" && cargo build -p flapjack-server)`, then run Playwright. |
| P3 | Customer auth hardening and OAuth residuals | **DEFERRED-BY-DECISION 2026-08-03, recorded so the reason is inherited rather than re-litigated.** Native passkeys / WebAuthn / TOTP are planned in `docs/security/hardening_spec.md` Wave 6 and were deliberately excluded from the `aug02_11am` batch: `PROJECT_OVERVIEW.md` ranks hardening P3 behind local product quality, and the cheaper parity story was OAuth console sign-in, since `infra/api/src/routes/oauth.rs` already owns start / PKCE / encrypted-state-cookie / code-exchange / identity resolution and `docs/env-vars.md:114-117` already documents all four client credentials. **CORRECTION 2026-08-03 — an earlier draft of this row claimed the OAuth console half never shipped and that the backend had zero consumers. That was wrong, and it repeated the exact error that got the lane retired.** OAuth console sign-in ships end to end on `main`: `web/src/lib/components/OAuthButtons.svelte` renders at `web/src/routes/login/+page.svelte:87` and `web/src/routes/signup/+page.svelte:132`, with `OAuthButtons.test.ts`, the callback route under `web/src/routes/auth/oauth/[provider]/callback/`, and a four-case browser round-trip at `web/tests/e2e-ui/full/oauth_round_trip.spec.ts`. The false claim came from a case-sensitive `grep -rln 'oauth' --include='*.svelte'`; the import is spelled `OAuthButtons`, so only `grep -rlni` finds it. `chats/icg/aug02_11am_5_oauth_console_signin.md` carries `Disposition: … abandoned` and was retired before dispatch on 2026-08-02 for this reason — it is a record, not backlog, and dispatching it would have installed a duplicate `OAuthButtons` owner and regressed the shipped disabled-state behaviour. **The one real residue is documentation:** `docs/screen_specs/login.md` and `docs/screen_specs/signup.md` mention OAuth controls only in viewport prose and carry no state contract for available / unavailable / status-fetch-failure. FS-9 owned that item and stopped. **Exit:** both screen specs carry the three OAuth control states with the shipped behaviour named, and no row or lane asserts the console half is missing. WebAuthn/TOTP stay deferred as above. |
| P1 | `local-ci --fast` is no longer fast — one gate now costs 21 minutes | **OPEN; the exact historical title is retained.** The current source audit `docs/audits/test-wiring/20260730T145719Z_fast_gate_lock_and_acceptance/SUMMARY.md` records one uncontended locked specimen at `elapsed_seconds_exact=1247.040064`, within the 1,800-second budget, but `local_ci_exit_code=1`; it is throughput evidence, not zero-exit acceptance. `bash scripts/tests/local_ci_fast_budget_test.sh` passes 56/56 and preserves that fail-closed distinction. Exit: one wrapper-owned locked run exits 0 within budget with reachability `unaccounted=0`. [History](implemented/2026-06-05_roadmap_v2_reshape_archive.md#local-ci-fast-runtime-history). **A 2026-07-31 remeasurement claiming a self-inflicted pile-up was WITHDRAWN the same day; only its durable lesson is kept.** `--fast` backgrounds every scheduled gate, so N processes sharing one worktree's `bash scripts/local-ci.sh --fast` argv are a single run plus its gate subshells, not N re-invocations — do not read a `ps` count as concurrent runs. **CORRECTED 2026-08-02 — Stage 2 retuned the default to 300s (`scripts/lib/local_ci_fast_lock.sh::FAST_LOCK_DEFAULT_WAIT_SECONDS`) after the caller-session arithmetic proved `1800 + 1247 > 2400`; `scripts/tests/local_ci_fast_lock_test.sh::test_default_wait_budget_fits_one_run_inside_caller_session_budget` now pins that a contended caller can wait the default and still fit one measured whole-suite run inside the 2,400s caller session budget.** **CORRECTED 2026-08-02 from fresh same-locality evidence — record the runtime envelope, not one figure: 1247s on a quiet clone and `real 2385.98` in this same-locality contended run; because this host normally carries 3+ concurrent lanes, 2386s is the applicable rounded-up upper-bound specimen. Stage 2 retuned the default to 300s, which is still strictly correct versus 1800s, but it did not close the caller-session constraint: `300 + 2386 = 2686 > 2400`.** **CLOSED 2026-08-06 by raising the caller timeout — one of the two moves that contract named; shrinking the gate was rejected on the repo's own evidence.** `matt_root/matt/auth_runtime_retry.py::_PROMPT_TIMEOUT_BY_TYPE` now gives `build` and `review_remediation` 4800s instead of the generic 2400s fallback, sized against this repo's gate (mike_dev `6ab567432`). fjcloud's mirror and its test moved with it (`6af478d08`): the test is renamed `test_default_wait_plus_one_run_fits_the_caller_session`, asserts `fits` rather than `exceeds`, and now also requires 600s of working margin beyond the gate so a future growth that merely fits still fails. It gained the wrap-up lead the old arithmetic omitted — matt interrupts a session at 90% of its budget, so the deadline a run must beat was 10% lower than this row claimed. Current margin 1634s; the gate may grow to ~3420s before the test reds. **Shrinking was rejected, not skipped:** dropping suites from `--fast` recreates the defect `jul27_pm_4` closed, the five sequential gates are serialized because they mutate checkout-root `.env.local`/`.local`, and the suite has no CI counterpart because dev Actions are disabled and the mirrors only run CI on a debbie sync. **Two stale mirrors of the old 2400 claim are OWED:** `scripts/local-ci.sh:129` and `docs/env-vars.md:457` still say the constraint is open, and both files are held by the live `aug05_11pm_1` lane; fix them when it merges. **The 2386s specimen is a floor, not a current figure** — taken against 3 concurrent lanes while the host carried 7 fjcloud plus 3 mike_dev lanes on 2026-08-06, and deliberately not re-measured because a whole-suite run holds the clone lock for 20-40 minutes and would have stalled all of them.** **RECONFIRMED 2026-08-07: this stopped two lanes today.** `aug06_10am_5` ($133.31) hit 6/6 then died in its closing `--fast` run at the 36-min session bound; `aug07_10am_0` inherited that proof and stopped at 2/3, integration already conflict-free |
| P1 | `jul27_pm_0` Wave 2's authored spec is stale — re-derive it before dispatching | **CLOSED after live remeasurement; row retained to preserve membership.** Items 1, 3, 4, and 5 were retired with evidence; item 2 was re-homed to `chats/icg/jul28_6am_6_browser_accessibility_closure.md`. Owner: that closure artifact for the surviving accessibility work. Exit (satisfied): every Wave 2 item was either re-verified against live code with a recorded command or struck with its retiring measurement, and no lane was authored from the stale June audit. [History](implemented/2026-06-05_roadmap_v2_reshape_archive.md#wave-2-stale-spec-history). **Stage 4 denominator correction 2026-07-31:** `docs/runbooks/evidence/local-reverification/20260728T161546Z/SUMMARY.md` names `section3_matrix_rows_total=9` but later uses `rows_total=13` for row/owner relationships; the next local-reverification bundle must label both denominator kinds explicitly and must describe `rows_not_local=5` as five of 13 relationships, not five of nine matrix rows. Owner: the next `docs/runbooks/evidence/local-reverification/` bundle author. Exit: its `SUMMARY.md` names both denominators and its relationship totals reconcile to `owner_manifest.tsv`. |
| P1 | Registered orchestrations outlive their work — 2 of 8 fjcloud entries are open | **OPEN; the exact historical title is retained, but the stale count is not reused.** For this batch, `closeout_artifact_check.py --roster` exits `2` because `chatting/jul28_6am_local_quality_and_refugee_readiness_closeout.md` lacks `Ship disposition:`. The artifact and machine-readable ten-lane roster exist, so the surviving debt is final disposition, not a missing taxonomy or replacement closeout. Owner: this batch's closeout lane. Exit: the roster checker exits 0 after the real union result is classified and every lane has a disposition. [History](implemented/2026-06-05_roadmap_v2_reshape_archive.md#orchestration-registry-history). **RECOUNTED 2026-07-31: 7 of the 8 registered fjcloud orchestrations have no closeout artifact.** Only `jul28_6am_0` has one. Missing: `jul26_pm_7`, `jul28_11am_0`, `jul29_11am_0`, `jul29_8pm_0`, `jul30_2am_0`, `jul30_pm_0`, `jul30_10pm_0`. The mechanism is now named rather than guessed: each of those batches authored a terminal batch-close lane and none was ever dispatched — `jul28_11am_5`, `jul29_11am_9`, `jul28_6am_10`, `jul30_2am_4`, `jul30_pm_10`, `jul28_9pm_9` and `jul30_10pm_10` all return zero branches from `git branch -a --list`. A closeout lane gates on every sibling finishing, so it is structurally last in the dispatch queue, and a new batch always opens ahead of it. Exit: every registered orchestration older than 48 hours has a dispatched closeout lane. **RECOUNTED 2026-08-01: 5 of 8 still have no closeout artifact, down from 7.** `chatting/` now carries `jul28_6am_local_quality_and_refugee_readiness_closeout.md`, `jul29_11am_local_honesty_and_debt_discharge_closeout.md`, `jul28_11am_local_proof_and_ssot_truth_closeout.md` and `jul29_8pm_followup_drain_and_b21_receipt_closeout.md`. Still missing: `jul26_pm_7`, `jul30_2am_0`, `jul30_pm_0`, `jul30_10pm_0`, and `jul29_11am_0`'s successor batch. The four undispatched closeout lanes (`jul30_pm_10`, `jul28_9pm_9`, `jul30_10pm_10`, `jul30_2am_4`) are the whole remaining gap. |
| P1       | Launch evidence freshness refresh                         | **OPEN.** The current probe reports six sections with one fresh and five stale; the four malformed names and fourteen rejected bundles are accepted, closed historical baselines rather than freshness failures. Dispatch procedure: [`docs/launch/deployed_proof_refresh_spec.md`](docs/launch/deployed_proof_refresh_spec.md) — authored 2026-07-30, it names per stale section the deployed proof to rerun, the canonical command in dry-run/no-op form first, the credential source, the evidence parent, the dispatch ordering and mutual-exclusion rules, and a falsifiable done-condition. It dispatches only: the five stale sections have not been rerun and this row stays OPEN until they are. Exit: `bash scripts/probe_launch_evidence_freshness.sh` prints `stale=0 missing=0 missing_section=0 unparseable=0` and exits 0 without deleting audit history. [History](implemented/2026-06-05_roadmap_v2_reshape_archive.md#launch-evidence-freshness-history). |
| **P0** | **The console never reads server region config — its region list is a hardcoded literal that 401s its way past the server's own filter** | **OPEN — new 2026-08-07, corrected three times; this revision is the one that survived independent review.** **(a) The server side is correct.** `effective_region_config`/`filter_to_providers` (`provisioner/mod.rs:136-145`, `region_map.rs:135-144`, applied `main.rs:496`) drop regions whose provider is unregistered, so the four Hetzner regions are properly hidden. **Do not re-file this as a defect:** `docs/design/provisioning_multicloud_contract.md:96` already retracted it on 2026-08-06 (`7e0b462f4`). **The stale claim survives only as a frozen copy** inside `chats/icg/aug06_10am_5_multicloud_provisioning_defects.md:31`, in a `matt:preserve` block written four hours BEFORE the retraction and never updated — that copy, not the doc, is what mis-led a later reader. Note the retracted sentence was *literally* true (`filter_to_providers` filters on `entry.provider` and never reads `available`, which nothing outside tests ever sets `false`); what was wrong was the conclusion, because map-key filtering removes the region without consulting the flag. **(b) The console bypasses all of it.** `/internal/regions` sits behind `internal_auth_middleware` and the web layer holds no internal key, so `GET https://api.staging.flapjack.foo/internal/regions` returns **401** and `web/src/routes/console/indexes/+page.server.ts:35` `.catch(() => DEFAULT_INTERNAL_REGIONS)` silently substitutes a six-region literal (`web/src/lib/format.ts:174-222`). The fallback is shape-identical to success, so nothing downstream can tell them apart. **A passing test pins this as intended: `web/src/routes/console/indexes/indexes.server.test.ts:115` asserts the 401 fallback**, so fixing the bug reddens a test that certifies it. **(c) Today's default is `us-east-1` and is safe only by accident** — the literal is hand-ordered, while `available_regions()` sorts by id (`region_map.rs:112`). **Repairing the auth without also fixing `+page.svelte:26`'s positional `regions[0]` silently moves every new index to `eu-central-1`, a Hetzner region with a different rate-card multiplier.** Four Hetzner options fail `provider 'hetzner' not configured`; `eu-west-1` holds zero EC2 instances in any state. **(d) The same `eu-west-1` advertisement is server-side and real, and is why `probe_fleet_dataplane.sh` emits `environment_attribution_ambiguous`, which stopped `aug07_3am_2`.** **Exit:** a customer-authenticated regions endpoint, a fetch failure that surfaces instead of substituting, an explicit default id, and advertisement gated on same-region runtime_params pointers — each with a test that reds on the live behaviour, and delete the test that pins the fallback. |
| **P0**   | **Public engine port + unauthenticated engine dashboard on prod AND staging** | **OPEN.** Per-VM TLS and the control-plane transport switch/design contract are merged, but the switch remains default-off and public tcp/7700 ingress still exists; the dashboard/docs shutdown proof covers only reachable current engines. Owners: `chats/icg/jul26_12am_0_local_migration_and_scale_closure_orchestration.md` for the surviving TLS flip/removal work and `scripts/security/probe_engine_exposure.sh` for the live verdict. Dispatch section: [`docs/launch/deployed_proof_refresh_spec.md` § "Public engine port + unauthenticated engine dashboard on prod AND staging"](docs/launch/deployed_proof_refresh_spec.md#public-engine-port--unauthenticated-engine-dashboard-on-prod-and-staging) collates this row's precondition, read-only probe command, evidence path, and customer-observable done-condition; it changes no owner and closes nothing. Exit: staging-fleet proof enables TLS customer endpoints, public `0.0.0.0/0` and `::/0` tcp/7700 ingress is removed with a failing Terraform contract against reintroduction, replacement-path dashboard/docs return 404, and the probe returns non-EXPOSED. [History](implemented/2026-06-05_roadmap_v2_reshape_archive.md#engine-exposure-history). **CORRECTION 2026-08-05, applied here as the owner of FR-3 `f9a2e`'s `ROADMAP CORRECTION REQUIRED`:** this row's title overstates one half and understates the other. There is no unauthenticated engine dashboard - measured live, only `/health` answers `200` and every other engine path answers `403`. The real exposure is **plaintext customer data-plane traffic on public tcp/7700**, because fjcloud hands customers the engine URL directly as their search endpoint. Removing 7700 before 443 serves would therefore delete the only customer data plane, which is precisely why FR-5 must prove per-VM HTTPS before each retirement. **Progress 2026-08-05 (FR-3, merge `9f0e8edfe9`):** the Caddy-bearing engine AMI is baked, one disposable staging VM was proven serving an authenticated customer search over public HTTPS with no `-k`, it was torn down to zero residue across EC2/DNS/SSM/database, and the staging AMI pointer advanced to `ami-07bff4ef03ac9ad00` (evidence `docs/runbooks/evidence/engine-tls/20260805T111633Z_first_tls_vm/SUMMARY.md`). Public `0.0.0.0/0` tcp/7700 is still present on `sg-047734af5235c69af` (staging) and `sg-0ab78cabd1b997099` (prod), re-measured 2026-08-05T14:4xZ. **MEASURED 2026-08-07 — the Terraform half of this exit is DONE and the live half is blocked on a fifteen-day-old probe defect.** `aug06_10pm_1` (`5f86f452f`) removed `flapjack_public_data_plane` from `ops/terraform/networking/main.tf` and added four rejecting fixtures. **Only some of that guard is live, and the difference was not previously recorded:** the four fixture rejections are in `ops/terraform/tests_iac_validation_static.sh`, which `gate_engine_exposure_probe_contract` runs (`scripts/local-ci.sh:923`, scheduled `:1591`) — measured PASS in 33s on 2026-08-07. The count assertion at `ops/terraform/tests_stage1_static.sh:35` is inert: nothing invokes that suite. See the merged-union-gate row. **The live SG rule is untouched and applying this Terraform now would delete the only customer data plane**, so both apply lanes (`aug06_10pm_2`, `aug06_10pm_4`) are correctly held. **Why 443 still does not serve:** `aug07_3am_2` (`b6735b7da`) was dispatched to find out and STOPPED at Stage 1 without provisioning anything, because `scripts/probe_fleet_dataplane.sh` returns `ACTION_REQUIRED reason=environment_attribution_ambiguous` (`docs/live-state/20260807T105109Z/`). **Root cause identified 2026-08-07, previously unowned: an advertised region with no same-region runtime_params pointers. The region row above owns it in full**, including the 2026-07-23 precedent left open in that bundle's `ROOT_CAUSE.md` |
| **P0**   | **Deactivate credentials exposed by the public-mirror evidence** | Stage 3 evidence `docs/runbooks/evidence/mirror-leak-scan/20260716T233251Z_credential_scan/` records `LIVE_CREDENTIALS_PRESENT` with `CI-unexaminable sites: 31`. The dispatch-ready owners are `docs/runbooks/evidence/secret-rotation/20260717T000002Z_mirror_leak_stage4/GAP_SPEC_rds_password_rotation.md` and `docs/runbooks/evidence/secret-rotation/20260717T000002Z_mirror_leak_stage4/GAP_SPEC_api_ssh_key_rotation.md`. **DOWNGRADED 2026-07-21 — issuing-layer deactivation is DONE and verified; only a residual remains.** `docs/runbooks/evidence/secret-rotation/20260719T182500Z_mirror_leak_rotation_verified/VERDICT.md` records **VERIFIED ROTATED**: SSM prod and staging `db_password` advanced to Version 2 (2026-07-17) and EC2 key pairs `fjcloud-api-prod` / `fjcloud-api-staging` were recreated 2026-07-17, discharging both Stage-4 GAP_SPECs at the issuing layer. **Residual (the only part still open):** the superseded SSH public key may persist in `authorized_keys` on instances launched before 2026-07-17T20:29Z, so EC2 authorization-path *absence* is not yet evidenced; that closure rides with the fleet rebuild. Original text retained as history: *"Issuing-system deactivation remains open under the authorized Terraform/fleet environment-mutation owner; the inventory remains explicitly live until dated RDS rejection and EC2/instance authorization-path absence evidence returns for staging and prod."* Dispatch section: [`docs/launch/deployed_proof_refresh_spec.md` § "Deactivate credentials exposed by the public-mirror evidence"](docs/launch/deployed_proof_refresh_spec.md#deactivate-credentials-exposed-by-the-public-mirror-evidence) collates the residual's precondition, acceptance shape, and evidence paths without re-opening the VERIFIED issuing-layer rotation, and records under its MISSING CAPABILITY heading that no repo-owned command performs the per-instance `authorized_keys`-absence check today; that probe is not created by this link. This row does not own L9 public-mirror recovery design. |
| **P0**   | **The production Flapjack data plane has never served a request** | **NARROWED; the exact historical title is retained.** Production has completed a non-vacuous signup → provision → ingest → exact-search → zero-residue-cleanup lifecycle once. The remaining work is recurring release-candidate freshness, not first-use proof, and the public tcp/7700 contradiction belongs to the engine-exposure row. Owners: `scripts/validate_full_vm_lifecycle_prod.sh` and the next verdict anchor `chats/icg/jul23_11am_8_rc_verdict_launch_anchor.md`. Exit: a fresh non-`--staging-only` RC runs the production data-plane step, proves the exact document hit and zero customer/tenant residue, and records the result against current merged `origin/main`. [History](implemented/2026-06-05_roadmap_v2_reshape_archive.md#production-data-plane-history). |
| **P0**   | **Launch-matrix §1 regressed to 4/6 red on two harness defects** | **OPEN; the exact historical title is retained.** The original harness defects are resolved, and the metering repair, freshness guard, producer metric, provisioning changes and alarm definition are merged. **UPDATED 2026-08-02 (`aug02_5am_4`, `d48f9fb75`) — the local half is proven and the deployed half is not.** A manually driven local run of the real aggregation job produced the exact `usage_daily` row from concrete Flapjack `/metrics` counter deltas with `rows_affected=1`, and its fail-capable wrappers rejected both negative specimens (uncleared seeded row → `reason=not_cleared`; no traffic → `reason=absent`): `docs/runbooks/evidence/aggregation-selfproduction/20260802T064250Z/receipt.md`. That proves local production when the job is manually driven. It does **not** prove staging timer enablement or firing, deployed-host database reachability, or fresh staging rows; alarm application, a staging rehearsal without `rollup_stale`, a terminal `S1_BLOCKER=discharged` receipt and §1 closure remain deploy-gated, and are therefore parked by the local-only operator direction in `PROJECT_OVERVIEW.md`. Owners: `infra/aggregation-job`, the staging deploy/rehearsal owners, and `docs/launch_verification_matrix.md` §1. **Local Falsifiable exit:** `bash scripts/local_real_pipeline_probe.sh` exits `0` emitting `LOCAL_REAL_PIPELINE_STATUS: PASS reason=verified` with concrete `PRE`/`POST`/`delta` counters and `rows_affected >= 1`. **Deploy Falsifiable exit — no local command can satisfy it:** a dated staging-host artifact carrying `systemctl is-enabled fjcloud-aggregation-job.timer`, `systemctl list-timers --all fjcloud-aggregation-job.timer` and `journalctl -u fjcloud-aggregation-job.timer -u fjcloud-aggregation-job.service`, and a staging database query proving an aggregation-produced `usage_daily` row after the timer fired; then apply the alarm, complete the rehearsal without `rollup_stale`, emit the discharge receipt, and rerun §1 to `all_green=1`. [History](implemented/2026-06-05_roadmap_v2_reshape_archive.md#launch-matrix-section-1-history). **Added 2026-08-07 (`aug07_3am_1`, `8e4c4e52e`):** the scheme-change hazard that would have silently zeroed this section is closed before it could fire. `infra/metering-agent/src/tenant_map.rs` no longer selects tenants by exact string equality on `flapjack_url`, and `scripts/tests/flapjack_url_producer_parity_test.sh` (registered in `scripts/lib/test_reachability_manifest.sh`) pins all four producers to one spelling, so enabling engine TLS cannot filter every tenant out of `usage_records`. This removes a blocker from the TLS path; it does not advance §1's own deploy-gated exit |
| P1       | Async admin-broadcast job with `202 Accepted` and status readback | Future backlog only; do not treat this as a second dispatcher or queue for the current synchronous request path. Stage 2 keeps `broadcast_email` synchronous and bounded at five in-flight recipients. Revisit a durable job when `scripts/customer_broadcast.sh --dry-run` reports roughly 125+ non-deleted recipients or the bounded-concurrency capacity KAT loses its 55-recipient/20 ms headroom under the 500 ms guardrail. That trigger is conservative: Stage 1 measured 55 recipients and the first bounce/complaint probe timed out at roughly 60.5 seconds, while five-way bounded fan-out should push the same latency shape well below the ALB default. The denominator alone did not prove the 504 cause; this row exists for future status/idempotency ownership if synchronous request headroom shrinks. |
| P1       | Public-release completion (active orchestration)          | Owner: `LAUNCH.md ## STATUS` for verdict, `docs/launch_verification_matrix.md` for launch-sentence evidence, and `chats/icg/jul16_10am_8_rc_verdict_and_launch_anchor.md` (L8 of the `jul16_10am_0` prod-truth batch) for the next RC/LAUNCH anchor. **Owner moved — do not dispatch the old one:** `chats/icg/jul13_12pm_3_s1_rc_launch_verdict.md` was dispatched 2026-07-15, budget-stopped at 2/3 with its verdict unrun, and was then formally **ABANDONED** by operator decision 2026-07-16 (`LAUNCH.md ## STATUS`). The "(authored, NOT yet dispatched)" description this row previously carried was wrong on both counts and contradicted the correction later in this same row. The jul12_pm anchor ran 2026-07-13: RC bundle `docs/runbooks/evidence/invite-ready-rc/20260713T145624Z_jul12_pm_rc_verdict_launch_anchor/` = `NOT-READY-real-defects`, sole real defect `staging_billing_rehearsal=invoice_email_ses_not_ready` (§2); §§1/3/4/5/6 green per its `verdict.json` `section_impact`. That defect's root cause (staging instance-role CloudWatch Logs read denial) is fixed and live-proven by the IAM row below, and the §1 runner + §1-aware RC classifier tooling is merged (jul13_12pm Wave 2). **CORRECTED 2026-07-16 — Wave 3 was dispatched and did NOT complete.** Worker `0b5da` ran `chats/icg/jul13_12pm_3_s1_rc_launch_verdict.md` on 2026-07-15, finished Stage 1 (frozen-SHA staging currency) and Stage 2 (the six-row §1 bundle), then terminated `budget_exceeded` at 2/3 stages ($161.99, 7h11m, `summary.json:status`) with Stage 3 — **the RC verdict and the LAUNCH anchor, i.e. the lane's entire reason to exist** — never run. Its partial work merged anyway (`f5abff380`). Consequences: no RC newer than the 2026-07-13 `jul12_pm` anchor exists; `chatting/jul13_iam_s1_rc_launch_closeout.json` does not exist, so `closeout_artifact_check.py` fails and the jul13_12pm batch is NOT closed; and the Algolia batch's merge freeze — whose release condition is a `jul13_12pm` verdict in `LAUNCH.md ## STATUS` — remains HELD on a lane that is dead. The §1 bundle it did produce came back **red** (see the §1 P0 row above). Authoring lesson, already an owned rule this lane violated: `pp/create_checklists.md:23` requires that "terminal verdict or closeout-anchor work that gates later dispatch gets its own small serial lane"; bundling the §1 run and the terminal verdict in one lane is precisely what let a budget stop strand the deliverable. Remaining launch work is re-carved accordingly in `chats/icg/jul16_10am_0_prod_truth_launch_orchestration.md`, which re-dispatches the RC path to a verdict (the first arm the freeze table itself authorizes) rather than abandoning the exam. Do not announce until that RC reaches an allowed launch shape. Exit unchanged: `chatting/jul13_iam_s1_rc_launch_closeout.json` exists on `origin/main` and `python3 scripts/launch/validate_launch_closeout.py --closeout=chatting/jul13_iam_s1_rc_launch_closeout.json --launch=LAUNCH.md --roadmap=ROADMAP.md --matrix=docs/launch_verification_matrix.md ...` passes per the Wave 3 gate in `chats/icg/jul13_12pm_0_iam_s1_rc_orchestration.md`.                                                                                                                                                                                                                 |
| P1       | In-VPC dunning evidence IAM read policy                   | Newly surfaced 2026-07-13 by `docs/runbooks/evidence/ses-coverage-a1/20260713T004742Z_jul12_pm_in_vpc_rerun/GAP_SPEC.md`. The scoped read policy now lives in `ops/iam/fjcloud-instance-role.tf` and was applied to staging against the actual `fjcloud-instance-role`; `docs/runbooks/evidence/ses-iam-read/20260713T202728Z_staging_apply/` records `terraform_apply`, all four CloudWatch Logs API probes `ok`, `stream_denominator=5`, and `state_reconciliation=not_needed`. Keep this row open until Wave 3 reruns the canonical §1 six-row in-VPC bundle and proves `all_green.txt=1`; do not treat the IAM apply proof alone as the dunning/inbox launch RC. Update 2026-07-14: the Wave 2 execution tooling for that rerun is merged — `scripts/launch/run_ses_coverage_a1_in_vpc.sh` (six-row runner, canonical `run_manifest.json`), `scripts/lib/ses_coverage_a1_integrity.py`, and the `--section1-manifest`-aware RC classifier in `scripts/launch/invoke_rc_with_env.sh` — so the only remaining work is dispatching `chats/icg/jul13_12pm_3_s1_rc_launch_verdict.md`. Exit: fresh §1 bundle under `docs/runbooks/evidence/ses-coverage-a1/` with `all_green.txt=1` at a frozen merged SHA.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| P1       | Algolia migration replacement program (active orchestration) | **OPEN.** The local Algolia lifecycle, real-source correctness receipt, terminal UI, multi-node refusal boundary, privacy scrub, and exact erasure acceptance are merged; staging and production remain default-closed. The provider-neutral successor chain is dependency-ready and authored but undispatched. Owner: `chats/icg/jul30_2am_0_three_provider_migration_and_gate_throughput_orchestration.md`. Exit: the declared migration chain reaches V5 green, `chatting/jul13_9pm_algolia_migration_reliability_closeout.md` exists, and the rostered closeout checker passes before any served activation. [History](implemented/2026-06-05_roadmap_v2_reshape_archive.md#algolia-migration-program-history). |
| P1       | Execute staging billing rehearsal rerun                   | CLOSED 2026-07-12 by `docs/runbooks/evidence/staging-billing-rehearsal/20260712T010555Z_current_main/`, reconciled in `chatting/20260712T050500Z_jul11_pm_launch_unblock_closeout.md`. Staging was deploy-current for `dev_sha=ae06b99a96bc9d02f02e5fe4c930379f54270cd1` (`deployable_drift=false`, `doc_only_ahead=false`), `summary.json` is `passed` / `rehearsal_completed`, and `invoice_email.json` records CloudWatch Logs SES send-events evidence with `emails_with_messages=1` and `missing_invoice_ids=[]`. The reset-first owner resets exactly one already-allowlisted tenant, and repeat-pass reuse is constrained to that confirmed tenant before the live billing/email path runs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| P1       | Web-plane deploy automation                               | Landed for the branch-aware staging deploy and served-surface proof. `origin/main:.github/workflows/ci.yml` contains both `--branch=staging` and `--branch=main` Pages deploy anchors, and the closeout live probes showed `cloud.staging.flapjack.foo/pricing` plus `_app/version.json` current while prod also served the signup CTA. The prior branch-alias staleness is closed. Remaining follow-up only: split the prod `--branch=main` deploy into the prod mirror with its own scoped Cloudflare token; that two-token hardening is deferred and non-gating.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| P1       | AWS live-infra E2E remaining guardrails                   | Repo-owned prep and validation contracts are implemented. Budget action auto-enforcement remains a future option; alert-only coverage is live.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| P1       | Live RDS restore proof (2026-04-23 captured)              | Current-main snapshot restore succeeded and isolated target cleanup evidence was captured at `docs/runbooks/evidence/database-recovery/20260612T002802Z_current_main/`; reruns keep the same contract and evidence-bundle shape. **Added 2026-07-28 (`jul28_11am_3` Stage 3, measured not assumed): the `docs/launch_verification_matrix.md` §4 contract-coverage row cites four evidence logs that were never produced** — `docs/runbooks/evidence/database-recovery/20260525T231835Z_a4_coverage/{drill_static.log,drill_unit.log,evidence_static.log,evidence_unit.log}` are all absent, so that row is now marked `UNSUPPORTED` rather than reading as proof. Owner producers exist and are runnable: `ops/terraform/tests_rds_restore_drill_static.sh`, `ops/terraform/tests_rds_restore_drill_unit.sh`, `ops/terraform/tests_rds_restore_evidence_static.sh`, `ops/terraform/tests_rds_restore_evidence_unit.sh`. Falsifiable produce/replace/remove exit: either (a) produce all four receipts by running the owner scripts, tee stdout to the four named paths, and confirm `bash scripts/check_status_doc_consistency.sh` plus a dead-link scan report zero missing targets under that bundle; (b) replace the four citations with a real evidence bundle that proves the same coverage; or (c) remove the four citations from §4 and restate the row's coverage claim to what the surviving bundle files actually prove. Closing this row requires one of those states — leaving the citations present and the files absent is the failure condition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| P2       | `panics_total` monitoring alarm wiring                    | **Applied + live-confirmed (2026-07-11).** `docs/runbooks/evidence/panics-alarm/20260710T210400Z/` records staging and prod PASS, and a fresh live `aws cloudwatch describe-alarms --alarm-name-prefix fjcloud --region us-east-1 --query "MetricAlarms[?MetricName=='PanicsPerPeriod'].[AlarmName,StateValue]"` (creds explicitly sourced from `.secret/.env.secret`, ambient unset) returned `fjcloud-staging-api-panics-high OK` and `fjcloud-prod-api-panics-high OK` in namespace `fjcloud/api`. The app counter is `panics_total`; Terraform alarms watch `PanicsPerPeriod`. The closeout's earlier `InvalidClientTokenId` reread was a stale-ambient-cred artifact, now superseded by this successful live readback. This P2 row is effectively closed (both envs live).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| P2       | SES deliverability proof                                  | Narrowed 2026-07-13 by the terminal jul12_pm in-VPC bundle. `verify_email_clickthrough`, `password_reset_clickthrough`, `ses_bounce`, and `ses_complaint` are green in `docs/runbooks/evidence/ses-coverage-a1/20260713T004742Z_jul12_pm_in_vpc_rerun/`; clean-env support/canary proof remains green at `docs/runbooks/evidence/ses-inbox-canary-clean-env/20260711T014655Z/SUMMARY.md`. Launch-matrix §1 remains partial because `staging_dunning_delivery` and dependent `dunning_email_inbox` are blocked by the instance-role CloudWatch Logs read-policy gap above, not by email-send or clickthrough product logic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| P2       | Repo GC completion — worktree and remote-branch manifest  | Narrowed 2026-07-15 by `docs/runbooks/evidence/repo-gc/20260715T043720Z/`: four clean, exact-tip-merged jul12 worktrees/branches (L4, stopped L7, L7b, and the L8 producer) were removed from the committed manifest; dirty L1/L3 worktrees were excluded. Post-cleanup `git branch --no-merged main` has nine rows: preserved `backup/pre_jul12_stage3_main_d5d673d33`, unclassified `batman/jul12_pm_8_batch_closeout_reconcile`, and seven active jul13/jul14/codex/plan refs (`batman/jul13_12pm_3_s1_rc_launch_verdict`, `batman/jul13_9pm_{2,3,4}_*`, `batman/jul14_pm_1_wave3_unshipped_launch_owners`, `codex/jul14-algolia-final-review`, `plan/jul14-algolia-amendments`). `git branch -r --no-merged origin/main` has one active row, `origin/plan/jul14-algolia-amendments`; no remote deletion is approved. Remaining jul12 cleanup is limited to preserving or resolving the two dirty merged worktrees and classifying the original unmerged L8 branch; exit remains zero rows from both exact branch commands after active batches terminate. |
| P1       | Shared-VM teardown leaks DNS records and node credentials | **SHIPPED 2026-07-22 (`jul21_am_1`, merge `29a8649b6`, 3/3 stages) — the leak is closed at the code level; two residuals remain.** One idempotent teardown seam (`routes/admin/vms.rs::teardown_retired_vm_resources:368`) now owns EC2 instance + Cloudflare A record + SSM node key + inventory row and is called from `decommission_vm` (`:507`), and a read-only detector at `GET /admin/vms/orphans` (`services/vm_orphan_reconcile.rs`) proved `orphans_found == 0` against the live fleet with every source `status: scanned` and no `enumeration_incomplete`. **Residual 1 — the reconciler is detect-only:** it reports orphans, it does not delete them, and nothing schedules it; a human still has to read the endpoint. **Residual 2 — instance-only orphan enumeration is explicitly out of scope** and was deferred to a follow-up lane that does not exist yet, so an EC2 instance with no inventory row is still invisible. The load-bearing false-green guard is the state-agnostic instance oracle unit test (stopped/pending/previous-AMI VMs must not classify as orphans), because today's 5-VM fleet does not exercise previous-AMI residue. Original text retained as history: *Authored, reviewed, never dispatched — the leaking code is unchanged, so this recurs on every VM retirement.* The admin `decommission_vm` path (`infra/api/src/routes/admin/vms.rs:434`) deletes only the `vm_inventory` DB row; it does not delete the VM's public Cloudflare A record or its SSM node key. The 2026-07-20/21 prod fleet rebuild consequently stranded **28 dangling public DNS records + 28 live `fj_live_` SSM credentials** (a subdomain-takeover and orphaned-credential surface). Those were swept by hand on 2026-07-21 and re-verified at 0 dangling with all 5 live VMs intact — but the hand sweep fixed the *instances*, not the *code*. Owner: `chats/icg/jul21_am_1_shared_vm_teardown_standardization.md` (parked/unvetted; single lane, independent of the console/engine waves, so it can dispatch any time). Its reviewed shape: promote the existing cleanup function into one idempotent teardown seam owning EC2 instance + Cloudflare record + SSM key + inventory row, add an orphan reconciler, and surface retire/clean in the admin console. Systemic write-up: `chats/suggestions/jul20_9pm_fjcloud_dev__vm_teardown_leaks_dns_and_node_credentials.md`. Exit: a single teardown owner all retirement paths call, plus a reconciler proving 0 orphaned DNS records and 0 orphaned SSM keys against the live fleet. **NEW CONSEQUENCE MEASURED 2026-08-07, customer-facing now.** `https://cloud.flapjack.foo/infrastructure` renders **"Availability 6.667% — Degraded"**; prod `GET /public/infrastructure` reports `us-east-1 vm_count=15 healthy=1`, staging `vm_count=7 healthy=0 health=outage`. **Those denominators are phantom.** Read-only `aws ec2 describe-instances --region us-east-1` (running) returns **one** engine in prod's subnet `subnet-0f20d0fd83a362024` (`i-072d4333322ffd0eb`) and five in staging's `subnet-03e3357684f12dcc8`. So 14 of prod's 15 counted VMs do not exist; which row owns the single `healthy` is not observable from outside the DB, but the denominator is. Mechanism: `compute_public_infrastructure` counts `vm_inventory.list_active` rows with a tenant on a running deployment; `region_health` treats zero-healthy as `Outage` and `Unknown` as not-healthy; `health_status` defaults `'unknown'` (`infra/migrations/009_deployment_extensions.sql:8`), written only by `services/health_monitor.rs:374,410`. **This is Residual 1 (detect-only reconciler, nothing scheduled) with a public blast radius, inverted from Residual 2** — a row with no instance. **Staging's 0/7 is NOT phantom-only: five staging engines run and none reports healthy**, a separate live failure (never-checked vs failing undistinguished). **Exit added:** the public page must not count inventory rows with no live instance, and reconciliation must be scheduled |
| P2       | Live-state probe suite has no data-plane or fleet probe   | **SHIPPED AND HONEST 2026-07-22 (`jul21_10pm_2`, merge `84096fe8a`, 5/5 stages) — narrowed, not closed.** `scripts/probe_fleet_dataplane.sh` (read-only AWS only) plus a `fleet_dataplane` row wired into `scripts/probe_live_state.sh` through its existing `register_raw`/`add_row` owners, registered as a six-site hermetic contract gate in `local-ci --fast`. It satisfies the exit condition's hard part: `PROBE_ERROR` on absent/empty/unknown tokens and `ACTION_REQUIRED` on every un-probed fleet — it cannot render a silent pass. **Why this row stays open:** the probe's first real run (`docs/live-state/20260723T075504Z/`) returns `status: ACTION_REQUIRED, reason=environment_attribution_ambiguous` — it cannot yet attribute VMs to an environment (staging discovery returns an `eu-west-1` pointer with `outcome: missing` alongside the real `us-east-1` one), so it has never actually asserted a live search response. `flapjack_build_identity` still returns `SKIP_NO_CREDS` in both environments. So the guard is honest but not yet *informative*, and the search-response half of the exit condition is unproven. Exit (unchanged, now partially met): a fleet row that resolves environment attribution and asserts a real search response from live VMs. Original framing retained as history: *A guard that cannot fail.* `scripts/probe_live_state.sh` is the project live-state inventory, but its newest snapshot (`docs/live-state/20260722T232028Z/SUMMARY.md`, 17 probe rows) covers only Stripe, SNS, SSM, Cloudflare, RDS, privacy.com and API `/health` — there is **no VM/fleet probe and no search-data-plane probe at all**. The one engine-oriented row, `flapjack_build_identity`, returns `SKIP_NO_CREDS` with `reason=missing_expected_identity` in **both** staging and prod, i.e. it reports a non-failure when it has nothing to compare against. This is the same shape as the failure that let a dead prod search plane certify green for three months (the P0 above), and it is exactly what `CLAUDE.md`'s "a guard that cannot fail is not a guard" rule forbids — probes must not default to healthy on missing or indeterminate state. Exit: a fleet/data-plane probe row that queries live VMs for engine reachability and a real search response, that reports `ACTION_REQUIRED` (never `SKIP`) when identity or reachability is indeterminate. |
| P1       | Lane merges are being lost at `batman lifecycle` | **OPEN and remeasured 2026-08-02 — the debt changed class.** `bash scripts/probe_lane_branch_publication.sh; echo "exit=$?"` still fails closed, now with `branches=55 PUSHED=19 LOCAL_AHEAD=2 DIVERGED=1 NO_REMOTE_BRANCH=0 MERGED=33 REMOTE_ONLY_AHEAD=0` and `exit=1` (was `branches=47 PUSHED=19 LOCAL_AHEAD=1 DIVERGED=1 NO_REMOTE_BRANCH=3 MERGED=23` at 2026-07-31). `NO_REMOTE_BRANCH` went 3 → 0, so nothing is stranded locally-only any more; but `PUSHED` is flat at 19 while `MERGED` rose 23 → 33, which makes the remainder **disposition debt, not publication debt** — branches that are published, unmerged, and mostly carry a written disposition no sweep has executed. Verified 2026-08-02: `jul29_11am_7` superseded (per `chats/icg/jul29_11am_9`), `jul29_8pm_2` carried by `chats/icg/jul30_pm_2_followup_ledger_rollup.md`, `jul16_10am_5` superseded (per `chatting/jul16_prod_truth_launch_closeout.md`), `jul30_pm_6` superseded for its component layer by `aug02_9am_2` and must NOT be merged, and `jul26_12am_2` / `jul25_am_3` / `jul25_am_2` / `jul24_pm_6` / `jul13_12pm_3` / `jul24_pm_a3` already PRUNE per the corrections block above. The one published branch holding unmerged work with no blocking disposition is `jul28_9pm_7_m3b_three_provider_parity_browser_proof` (+1,930 lines of provider-parity fixtures and a deliberately-RED unmocked spec); it is correctly held, because landing a red `full`-project spec would redden the suite, and its unblock is the cross-repo neutral-discovery contract. **REMEASURED 2026-08-04 with a second probe answering a different question.** `bash scripts/probe_lane_branch_content.sh` merges each branch into `main` in memory (`git merge-tree --write-tree`) and compares the result tree to `main`'s, so it answers "does this branch still carry work?" rather than "is it pushed?". Census: `unmerged=22 NO_OP=5 ADDS=3 CONFLICT=14`, exit `1`. **Five branches are provably deletable** — merging them changes `main`'s tree by nothing: `aug02_5am_2`, `aug02_5am_6` (FC-6 itself), `jul13_9pm_22`, `jul20_9pm_4`, `jul24_am_8`. Deletion is `batman gc`'s, not this row's; recording the disposition is. **Of the 3 ADDS, 2 add only their own checklist annotation**; the third (`jul21_pm_1`) also carries an engine-exposure `GAP_SPEC.md` worth salvaging. **Two 2026-08-02 dispositions are corrected:** `jul28_9pm_7` is no longer held by the cross-repo contract (FC-5 merged `3fd4098e7`) and must be rebased rather than lifecycle-merged; `jul26_12am_2` carries `scripts/lib/playwright_port_plan.sh`, absent from `main` and the named exit of the manual-stack row. **This probe is deliberately NOT in `local-ci --fast`:** it is red today, and a red gate in the pre-push path refuses every push — the exact 2026-08-03 failure. Wire it in only once `NO_OP=0`, and treat that as this row's exit. Why a probe at all: `git branch --list` stops being valid after GC, `git log main..<branch>` counts merge-main-into-branch as work, and `git diff main <branch>` reports main's own commits as deletions — two ad-hoc counts in one session on 2026-08-04 disagreed about whether a branch existed. Durable evidence owner: `docs/audits/branch-disposition/DISPOSITIONS.md`; recurrence owner: the two probes. |
| P1       | Console browser-coverage residuals (batch close in flight) | **Added 2026-07-27 and CORRECTED the same day — read the correction first.** An earlier revision of this row claimed the batch's gate "closed red at 18/19" and was "arithmetically unreachable." **That claim was WRONG and is withdrawn.** The target at `docs/audits/ui_test_coverage_p0_ledger.md:77` is 19 semantically valid **tags**, not 19 test declarations: `grep -roh '@p0_coverage' web/tests/ \| wc -l` returns **19** across 18 declarations, because `web/tests/e2e-ui/full/console_replica_lifecycle.spec.ts:36` legitimately closes rows 16 and 17 in a single declaration. `chats/icg/jul27_pm_1_console_customer_quality_closeout.md` carry-forward 3 states this explicitly and forbids splitting that declaration to inflate the count — **do not split it.** The count gate is MET; semantic validity is being assessed by worker `cc118` running that closeout lane. **Three real residuals survive and need named owners at batch close:** (1) row 8 (`web/tests/e2e-ui/full/billing_portal_payment_method_update.spec.ts:210`) self-skips whenever `stripeCustomerId` starts with `cus_local_`, so it executes only under `API_DEV_ALLOW_LIVE_STRIPE=1` and does not run under the default local Stripe mock — a conditional-execution gap, not a proof; (2) "zero skips" is **not locally satisfiable and must never be written as a gate**: `web/tests/e2e-ui/full/auth-end-effects.spec.ts:66` skips on every local run because `web/playwright.config.contract.ts:625-633` unconditionally forces `ENVIRONMENT=local` + `SKIP_EMAIL_VERIFICATION=1` + `API_DEV_ALLOW_SKIP_EMAIL_VERIFICATION=1` onto the locally spawned stack, and `web/tests/e2e-ui/full/console.spec.ts:526` self-skips for want of a payment-method-free shared-plan customer; (3) `auth-end-effects.spec.ts:17,107` fail whenever Mailpit is not running — a stack-ownership gap that the closeout lane still works around with a manual "start Mailpit first" instruction rather than an owned automated step. **Stage 3 screen-spec reconciliation note, 2026-07-28:** the focused Vitest claim proofs pass, but the focused Playwright proof for analytics/events/overview remains incomplete because the corrected local-stack run timed out on blank-page analytics cases before reaching the full selection; do not cite those Playwright files as refreshed Stage 3 behavior proof until this row's browser-stack owner reruns the same focused selection green. Exit: `chatting/jul26_pm_11_console_customer_quality_closeout.md` exists, `closeout_artifact_check.py --roster` exits 0, each of the three residuals carries a named owner row here, and the focused analytics/events/overview Playwright selection has a recorded green rerun before those files are cited as refreshed Stage 3 behavior proof. **CLOSED ON COUNT AND ON SPECIMENS, re-measured 2026-07-29 at `fbd27347a`.** `grep -roh '@p0_coverage' web/tests/ | wc -l` returns **19** across 18 declarations, and all six rows the reconciled ledger left `REAL` now have named specimens on `main`: row 6 `web/tests/e2e-ui/full/console_daily_usage_chart.spec.ts:75`, row 8 `billing_portal_payment_method_update.spec.ts:210`, row 11 `onboarding.spec.ts:330`, row 14 `indexes.spec.ts:872`, rows 16+17 `console_replica_lifecycle.spec.ts:35`. What is NOT closed is execution honesty for one of them — see the skip-integrity row: row 8's specimen is B2, which still always-skips locally. |
| P1       | Routine-local skip integrity audit follow-up | **Added 2026-07-27 from `docs/audits/test-integrity/20260727T160044Z_skip_classification/SUMMARY.md`.** Six catalog rows always skipped under the routine local baseline: B1/B2/B7/B10/B21/B36; the non-empty P0 subset was B2 `web/tests/e2e-ui/full/billing_portal_payment_method_update.spec.ts:222`, B7 `web/tests/e2e-ui/full/upgrade_to_shared_unmocked.spec.ts:22`, and B21 `web/tests/e2e-ui/full/auth-end-effects.spec.ts:91`, so counted `@p0_coverage` overstated proven routine-local coverage by 3 rows. **UPDATED 2026-07-29 — the P0 subset closes by receipt-backed execution, not by static skip-anchor deletion.** Exit: `bash scripts/tests/p0_coverage_execution_receipt_test.sh; echo "exit=$?"` prints `rows_checked=3`, `PASS: B2/B7/B21 have fresh runner-supported executed_pass receipts`, and `exit=0`. That proves fresh runner-supported `executed_pass` receipts for B2, B7, and B21 through the existing receipt owner and local runner; it does not prove every historical `test.skip(` anchor was deleted, and it does not prove unrelated non-P0 skip-classification rows execute locally. B21 is no longer treated here as structurally unrunnable, the sole surviving P0 always-skip, or red-by-construction when the receipt guard passes; future regressions must fail through the receipt owner rather than a static skip-anchor scan. |
| P1       | Local migration ledger can record a migration as applied without running it | **Added 2026-07-27 from `chats/icg/jul27_am_4_console_payment_method_end_effect_completion.md`, which found `_schema_migrations` claiming `051_customer_subscription_cycle_anchor.sql` applied while `public.customers` lacked the column.** A hand-audit reporting coverage of "all 30 `ADD COLUMN` statements" found exactly three drifted columns — `billing_mode` (`014_billing_mode.sql`), `quota_warnings_sent` (`050_quota_warnings_sent_jsonb.sql`), and `subscription_cycle_anchor_at` (`051`), all on `customers`. **That audit was incomplete:** re-measured 2026-07-27, `infra/migrations/` holds **51** `ADD COLUMN` occurrences across 24 files, and the pass checked no `CREATE TABLE`, `CREATE INDEX`, or constraint DDL at all — so treat those three columns as a sample, not an inventory. They were re-applied idempotently to the local database, which fixed the symptom and not the cause. **Mechanism (verified 2026-07-27):** `scripts/lib/migrate.sh:59-76` treats any database with user tables but an empty `_schema_migrations` as a "pre-tracking volume" and seeds **every** migration filename as applied **without executing any of them**, swallowing all errors with `>/dev/null 2>&1 \|\| true` at `:73`. The script's own comment at `:54` concedes the consequence: "Adding a new migration to a legacy volume requires --clean." There is no checksum or post-apply verification, so a filename recorded as applied is never re-checked. **Scope is local only — production and staging are NOT affected:** `ops/scripts/migrate.sh:43` and CI both use `sqlx migrate run`, which keeps its own `_sqlx_migrations` table with checksum validation. The exposure is `scripts/local-dev-up.sh:191`, `scripts/local-dev-migrate.sh:46,63`, `scripts/integration-up.sh:116`, and `scripts/playwright_local_stack.sh:274` — i.e. the local dev database and every local integration and browser-test stack. That matters here specifically because the automated suite is the only QA process: a drifted local schema makes tests fail for the wrong reason or pass while proving nothing. **Ruled out:** post-application edits to those migration files (`git log` shows exactly one commit each, never modified). A second instance of the same class sits alongside it: `scripts/tests/migrate_test.sh` exists but is invoked by **no** gate or runner, so the migration helper's only test can never fail. Owner: `chats/icg/jul27_pm_3_local_schema_drift_detector.md`. Exit: `bash scripts/tests/probe_local_schema_drift_test.sh` passes hermetically, the `local-schema-drift-contract` gate is registered in `scripts/local-ci.sh` and runs `migrate_test.sh` too, `bash scripts/probe_local_schema_drift.sh` exits 0 against a live local database, and a deliberately dropped column makes that probe exit non-zero. |
| P1       | Five merged fjcloud batches have no closeout artifact     | **OPEN; the exact historical title is retained, but the old count is not asserted.** This batch's declared closeout now has the exact ship disposition, one canonical union section, and roster-complete dispositions; its roster checker exits 0 while the continuation remains `carried` pending `batman land`. Its accounting stays red at `entries_classified=238 of 999` and `feed_open_window=999` versus expected `797` (`feed_out_of_window_open=0`), and its canonical union receipt stays red at exit `1`. This reconciles only this batch; the other historical closeout debt remains open. Exit: every declared closeout has roster-complete lane dispositions plus a ship disposition, and each orchestration's roster checker exits 0. [History](implemented/2026-06-05_roadmap_v2_reshape_archive.md#batch-closeout-debt-history). **SIXTH BATCH ADDED 2026-08-03 — `aug02_11am`, and this instance shows the mechanism cleanly.** Six lanes merged and all six wrote `chatting/aug02_11am_roadmap_handover_fs{1,2,3,4,6,7}.md` on time; the consuming lane `aug02_11am_9` stopped at 2/3, so none reached this file until the 2026-08-03 reconciliation applied them by hand, and the declared closeout artifact does not exist. Identical shape to `aug02_5am` four days earlier (`..._fc{1,2,4}.md` unconsumed, `aug02_5am_6` never dispatched, per the 2026-08-02 header above). **Two consecutive batches, same failure: the producing side works and the consuming side is the single point of failure.** The fix is not another reconcile lane — either an unconsumed handover becomes detectable (a `chatting/*_roadmap_handover_*.md` with no applied claim here is a red state a gate can assert), or reconcile stops being the step a budget stop drops. Inventory: [aug02_11am lane inventory](implemented/2026-08-03_aug02_11am_batch_reconciliation.md#aug02-11am-lane-inventory). **RECOUNTED 2026-08-07 and the mechanism has now fully taken over.** Thirteen orchestrations were authored since 2026-08-01; `ls chatting/ | grep -iE 'aug.*closeout'` returns seven, and **every batch since `aug05_12pm` has none** — `aug05_11pm`, `aug05_1pm`, `aug05_2am`, `aug06_3pm`, `aug06_10am`, `aug06_10pm` and `aug07_3am` are all open. The direct, observable consequence is this file: before the 2026-08-07 header above, `grep -c` for each of the six lanes merged in the previous twelve hours returned `0` in both `ROADMAP.md` and `LAUNCH.md`, and `ROADMAP.md`'s newest commit was `4f3cf26ea` from 2026-08-06T21:17. **A contributing cause is now measured and is not dispatch order: this ledger is structurally full.** It sits at 199 of its 200-line cap and twelve rows are within 60 bytes of the 4,000-byte row cap, so a reconcile lane that wants to add a row must first retire one — which is unbudgeted work no checklist assigns. Exit unchanged; add to it that the shape gate must have headroom a reconcile lane can actually use |
| P2       | Staging aggregation job is not producing `usage_daily` rollups | **UPDATE 2026-07-24 — repaired at the code level, proven fresh once by hand, not yet durable; the §1 P0 row above carries the full receipt.** The running-fleet root cause was found and hand-fixed: staging metering agents were pointed at PROD `api.flapjack.foo`, Flapjack `/metrics` returned 403, and the synthetic seeder was selecting prod-VPC nodes; manual convergence of 6 nodes plus a hand-run aggregation job yielded one fresh row (2026-07-24T09:19:25Z). The durable code fixes (env-aware metering-URL derivation + admin-key persistence in `ops/user-data/bootstrap.sh` / `cloud_init.rs`) are merged but apply only to **newly provisioned** VMs; the systemd timer was never proven to self-produce, the staleness alarm is unmerged, and the latest live-state still reads `ACTION_REQUIRED`. Split out 2026-07-23 from the §1 P0 above so the pipeline defect has its own owner rather than living inside a launch-harness row. Evidence: `docs/runbooks/evidence/ses-coverage-a1/20260722T204849Z_jul16_10am_s1_rerun/staging_dunning_delivery.log` — `classification=rollup_stale`, `usage_daily has no recent rollups (0 rows within 48h)`. Everything upstream of it is green: the rehearsal preflight passes all eleven configuration checks and staging deploy currency is `deployable_drift=false` at `9d521e1c9`. Candidate owners in order of cheapness to check: the `infra/aggregation-job` systemd timer state on the staging API host, then whether metering rows are arriving at all (a dead `metering-agent` would starve the aggregator rather than break it), then the job's own error path. **Do not diagnose this from the §1 runner** — the runner is correctly reporting someone else's outage. Note the shape: this is the same class of failure as the dead prod data plane — a background job silently stopped producing and nothing alerted, which is also what the fleet/data-plane probe row above exists to prevent. Exit: `usage_daily` rows within 48h on staging, a rerun `staging_billing_rehearsal` reaching `rehearsal_completed`, and a probe or alarm that fails loudly on rollup staleness. **UPDATE 2026-08-02 — proven to produce when manually driven locally; staging timer production still not proven durable.** `aug02_5am_4` (`d48f9fb75`) recorded the real job producing an exact `usage_daily` row locally with both negative specimens rejected (`docs/runbooks/evidence/aggregation-selfproduction/20260802T064250Z/receipt.md`). That local proof does not show the staging timer fired or that fresh staging rows exist; timer enablement, firing and deployed database reachability remain unproven, the alarm remains unapplied, and the latest recorded live-state remains `ACTION_REQUIRED`. Do not infer a launch verdict, staging freshness or blocker discharge from the local receipt. **Local Falsifiable exit:** `bash scripts/local_real_pipeline_probe.sh` exits `0` emitting `LOCAL_REAL_PIPELINE_STATUS: PASS reason=verified` with concrete `PRE`/`POST`/`delta` counters and `rows_affected >= 1`. **Deploy Falsifiable exit — no local command can satisfy it:** a dated staging-host artifact carrying `systemctl is-enabled fjcloud-aggregation-job.timer`, `systemctl list-timers --all fjcloud-aggregation-job.timer`, `journalctl -u fjcloud-aggregation-job.timer -u fjcloud-aggregation-job.service`, and a staging database freshness query proving an aggregation-produced `usage_daily` row after that timer fired; then `staging_billing_rehearsal` reaches `rehearsal_completed` and the staleness probe or applied alarm fails loudly on stale rollups. |
| P1       | `jul23_11am` batch — undispatched launch/security successors + one built-but-unmerged UI lane | **Added 2026-07-24 to give the batch's stranded tail an explicit owner (its L9 reconcile never ran).** Six lanes were authored and **never dispatched**, and their dispatch is the shortest path to a launch verdict: **L7** (`jul23_11am_7`, §1 bundle rerun — gated on the non-existent `S1_BLOCKER=discharged` receipt), **L8** (`jul23_11am_8`, RC verdict + `LAUNCH.md` anchor — the announce-gate owner, still un-run so no verdict exists since 2026-07-22), **L9** (`jul23_11am_9`, batch closeout + ROADMAP reconcile), **L10** (`jul23_11am_10`, engine TLS endpoint switch — see engine-exposure P0), **L10V** (`jul24_am_3`, endpoint-switch live proof), **L12** (`jul23_11am_12`, enabled autorepair replacement proof). Separately, **L11** (`jul23_11am_11`, replacement-timeline admin UI) is **built 6/6 on `batman/jul23_11am_11` (+22 ahead of `main`) and NOT merged** — real frontend (`962e7d4d4 Render admin VM lifecycle timeline`, browser tests) that renders L3's `vm_lifecycle_events`; `main`'s `web/src/routes/admin/fleet/[id]/+page.svelte` has no `lifecycle` reference. Disposition needed: rebase L11 onto current `main` and merge (the backend it consumes is already on `main`), or explicitly abandon. **RESOLVED 2026-07-26 — MERGED to `main` via recovery lane `jul26_am_12_replacement_timeline_ui_recovery` (worker `b0aea`, 3/3, 57/57), whose branch carried the stranded UI at merge `245ed9dc7`; landed here as `3d6b8a5f0` with 26 files, +1753/-368, and `local-ci --fast` green. The diagnosis below is retained because the *mechanism* that stranded it is still open. Prior status, now historical — "still unmerged, still conflict-free, three days stale; needs no rebase":** `git merge-tree --write-tree origin/main batman/jul23_11am_11_replacement_timeline_admin_ui` returns a tree with zero conflicts against current `main`, and `git cherry -v origin/main <branch>` shows all 21 commits still unmerged, including `962e7d4d4` (render), `108a3f68b` (load), `fec204cc7` (browser coverage). Its 2026-07-24 root cause is now diagnosed and is *not* a rebase problem: the lane had no `[merge]` stage — see the new "Lane merges are being lost at `batman lifecycle`" row. **Its re-homing made this worse, not better:** `chats/icg/jul26_12am_0_local_migration_and_scale_closure_orchestration.md` re-homes it as L18 in Wave 3, gated on L11-real-pipeline (unauthored) which is itself gated on L2 (`jul26_12am_2`, an 11h/$116 runaway), because both would touch `fleet.spec.ts`/fixtures. A conflict-free, 6/6-complete, +1450-line merged-backend consumer is therefore queued behind two lanes that do not exist yet. Recommended disposition: merge it now as its own short lane ahead of Wave 2B and make the later fixture lanes consume the merged result, rather than the reverse. Exit: L8 lands a fresh RC verdict in an allowed shape with a matching `LAUNCH.md ## STATUS` entry; L11 is merged or stamped abandoned; the orchestration is deregistered from `mike_dev/pp/supervise.md`. |
| P3       | Reintroduce Linux-Playwright Firefox/WebKit               | CI is Chromium-only after the 2026-05-02 cutover; reintroduce if customer signal shows Firefox/non-Safari-WebKit gaps.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| P3       | Console unification program (post-launch)                 | One Svelte console for the managed cloud and the standalone OSS binary; retire the React `engine/dashboard` after a parity gate. Program SSOT: `docs/design/console_unification_revised_plan.md` (v7, 2026-07-18) — all four operator decisions are settled there, including the 2026-07-18 source-home decision: the shared console will live in **flapjack_dev** and fjcloud will consume a pinned flapjack revision (ADR: flapjack_dev `engine/docs2/3_IMPLEMENTATION/decisions/active/0006_console_source_home.md`). Not on the launch path. Start gates per the plan's sequencing section: Algolia fjcloud web-touching waves complete before P1/P2; E7 merged on flapjack `origin/main` before P4. Immediate residue (**RE-VERIFIED 2026-07-22 — the prior "4 missing analytics paths" wording is STALE**): all 9 analytics `#[utoipa::path]` handlers ARE already registered in `infra/api/src/openapi.rs:102-110` (9 defined == 9 registered). The genuinely-unregistered handlers are **8 Algolia migration-job + shared-upgrade routes** (`{create,get,list,cancel,resume}_algolia_import_job`, `check_algolia_destination_eligibility`, `get_upgrade_status`, `upgrade_to_shared`), which are entangled with the **active Algolia migration program** (P1, F9 in flight) and its deliberately-gated availability + deferred-resume-is-a-defect rule — so their OpenAPI registration is routed to that program's owner (`chats/icg/jul13_9pm_0_algolia_migration_reliability_orchestration.md`), NOT a standalone console-prep task. Regenerate with `UPDATE_OPENAPI_ARTIFACT=1` only when that owner registers them. **Console-prep batch status (`jul20_9pm`, merged 2026-07-20) — 3 of 4 design deliverables shipped:** `docs/design/console_transport_mapping_catalog.md` (476 lines, per-surface transport derivation web→proxy→engine), `docs/design/console_core_extraction_map.md` (228 lines, neutral-vs-billing-coupled `$lib` split for P3), and `docs/design/console_bff_proxy_contract.md` (293 lines, `/console-api/*` treated as a credential-injecting SSRF boundary). **The fourth did NOT ship:** lane B3 (`jul20_9pm_4_ops_surface_screen_specs`) merged its own checklist file and nothing else — 0/2 stages, 1/27 items, $8.45 — so `docs/screen_specs/{system,cluster,snapshots}.md` **do not exist**. Its own closeout stamp reads "deferred — completeness FAIL (deliverable ABSENT) — merged per operator no-cherry-pick directive — NOT shipped; requires a continuation lane." **RESOLVED 2026-07-22 — the continuation ran.** `chats/icg/jul20_9pm_4b_ops_surface_screen_specs.md` (2/2 stages, `needs_review`) landed all three specs at `4838a70f3`: `docs/screen_specs/system.md` (102 lines), `cluster.md` (95), `snapshots.md` (105), all present on `origin/main` and indexed in `docs/screen_specs/coverage.md`. **R5 (System/Cluster/Snapshots parity build) is no longer doc-blocked.** Its remaining gate is the program's own sequencing rule — Algolia fjcloud web-touching waves complete before P1/P2 — which is currently held by F10. |
| P2 | `cold_storage_endpoint_reads_env` is order-dependent and fails about half of `--test platform storage` runs | **OPEN — new 2026-07-31.** The test mutates process-global `COLD_STORAGE_ENDPOINT` (`infra/api/tests/integration/integration_cold_tier_test.rs:490-506`) under `ENV_LOCK`, but sibling tests in the same binary read that variable without taking the lock, so parallel threads race. Measured on an unchanged tree: two consecutive `cargo test -p api --test platform storage` runs gave `133 passed; 0 failed` then `132 passed; 1 failed`; the test passes deterministically when run alone. This is a false red, so it trains readers to re-run rather than diagnose. Re-derive: `cd infra && for i in 1 2; do cargo test -p api --test platform storage 2>&1 | grep '^test result'; done`. Exit: ten consecutive filtered runs pass, with the ambient-env dependency removed rather than hidden behind a wider lock or a retry. |

- **DictionariesTab size gate returned to the default owner.** Owner: `scripts/check-sizes.sh`; the captured roadmap-v2 title `DictionariesTab size override remains.` is now historical, not the current state. The 2026-07-07 EditorDialog split removed both EditorDialog `PER_FILE_OVERRIDES` caps, and the later size-gate cleanup also removed the stale `DictionariesTab.svelte|800` override. `web/src/lib/components/DictionariesTab.svelte` is now governed by the normal `.svelte` fallback limit; the remaining temporary Svelte override is the index-detail shell extraction cap in `PER_FILE_OVERRIDES`. **ROADMAP's own shape debt is STILL OPEN and is homed nowhere else, so it is recorded on this size-gate bullet (2026-07-30, `jul29_11am_8`).** Its enforcement owner is `scripts/check_roadmap_v2_shape.sh`, NOT `scripts/check-sizes.sh`, which still does not track `ROADMAP.md` at all. That gate now enforces a `MAX_ROW_BYTES=4000` per-row invariant alongside its 200-line cap, and the byte half is satisfied: the census at `1426d5983` moved from 198 lines / longest row 11256 bytes / 11 rows over bound to 198 lines / longest row 3434 bytes / 0 rows over bound, with `rows_restructured=10 of 10` and every moved revision preserved under `implemented/2026-06-05_roadmap_v2_reshape_archive.md` (proof: `python3 docs/audits/roadmap-restructure/20260730_stage3_preservation/prove_roadmap_content_preserved.py` exits 0 with zero missing SHAs, paths, or sentences). **The line half is not satisfied, and that is why this stays OPEN:** narrowing a row's cell text moves bytes into the archive but deletes no physical line, so headroom against the 200-line cap is still only 2 lines and any new row or bullet can turn the gate red. Exit: `awk 'END{print NR}' ROADMAP.md` leaves headroom against the 200-line cap at >= 5 lines, or the line count materially drops.
- **jul07_3pm release-batch parked lanes are narrowed.** Owner: `chats/icg/jul07_3pm_0_dev_velocity_refugee_delight_orchestration.md`; parked lanes, updated 2026-07-13: Playwright `workers > 1` remains deferred behind evidence of benefit over the now-green `workers: 1` baseline, lane 15 (staging billing rehearsal rerun) is closed by `docs/runbooks/evidence/staging-billing-rehearsal/20260712T010555Z_current_main/`, and lane 17 (`e2e-deployed` failure paging) landed through the deployable-currency SSOT below. Lane 12 (settings sub-tabs part 2: Language & Text, Facets & Filters, Display) is already merged into the jul09 release-batch state; do not treat it as a built-but-unmerged terminus lane.
- **SES bounce/complaint live probe rerun remains open.** (Captured roadmap-v2 title — historical, kept for shape invariants; the probe is GREEN as of 2026-07-11.) Owner: `scripts/probe_ses_bounce_complaint_e2e.sh`; current clean-env bundle: `docs/runbooks/evidence/ses-inbox-canary-clean-env/20260711T014655Z/SUMMARY.md`. The fresh rerun hydrated the staging DB URL through SSM, RAN bounce and complaint against live staging, and exercised `poll_sns_side_effects`, second-send suppression, audit rows, and cleanup with exit `0` for both owners.
- **Account-retention automation implemented.** Owners: `docs/runbooks/account_data_policy.md` and the `infra/retention-job` crate (deployed via `ops/systemd/fjcloud-retention-job.timer`); it runs the automated retention-duration cleanup loop for `list_deleted_before_cutoff` candidates and delegates erasure to `POST /admin/customers/:id/hard-erase`.
- **AWS live-infra E2E current-main wrapper evidence passed.** Owner: `scripts/launch/live_e2e_evidence.sh`; evidence: `docs/runbooks/evidence/live-e2e-guardrails/20260612T033222Z_current_main/fjcloud_live_e2e_evidence_20260612T033222Z_79205/summary.json` (`overall_verdict=pass`, runtime-smoke and billing rehearsal checks pass, no external blockers). Credentialed destructive billing/webhook capture remains a future option via the wrapper's existing live-mutation path. Operational detail owned by `docs/runbooks/aws_live_e2e_guardrails.md`.
- **Staging billing rehearsal current-main rerun remains open.** (Captured roadmap-v2 title — historical; RESOLVED 2026-07-12 by the current-main live rerun.) Owner: `scripts/staging_billing_rehearsal.sh`. Canonical fresh proof is `docs/runbooks/evidence/staging-billing-rehearsal/20260712T010555Z_current_main/`: `deploy_status_before.json` records staging current for `dev_sha=ae06b99a96bc9d02f02e5fe4c930379f54270cd1` with `deployable_drift=false`, `summary.json` is `passed` / `rehearsal_completed`, and `invoice_email.json` proves invoice-email dispatch on the reset-first current-month path with `emails_with_messages=1` and `missing_invoice_ids=[]`. The deployable-currency SSOT fast-follow remains landed at `docs/runbooks/evidence/deployable-currency/20260710T213440Z/`, and the reset-first rehearsal preflight half is now closed by this live rerun.
- **Unmerged lane disposition (re-verified 2026-07-10 — every branch below re-checked with `git merge-base --is-ancestor` and `git cherry -v main`).** ABANDON, all confirmed unmerged and carrying no salvageable product code: `batman/jul08_am_1_rc_shape_drift_investigate_and_rerun` (stopped 0/5 stages; its one product diff re-adds an `onclick` handler to `PaymentMethodSetupForm.svelte` that main **deliberately rejected** — main carries an explicit "Do NOT reintroduce onclick / capture-phase click+pointerdown listeners here" comment, so merging would regress main; its `ops/terraform/dns/main.tf` change is byte-identical to main's and the branch is otherwise _behind_ main), `batman/jul08_pm_1_rc_rerun_produce_clean_bundle` (4 unique commits, 214 files, essentially all `DIRMAP.md` noise), `batman/jul09_am_6_billing_rehearsal_real_run` (evidence/DIRMAP/checklist only), and the `polished-beta-staging-verify` rerun4/rerun5 branches. **`batman/jul07_3pm_14_playwright_parallel_workers` is now ABANDON, not SALVAGE (correction).** Its fixture/spec-ownership refactor **already reached main independently** via `5256ad368`, `159053c14`, `a74928abe` (`web/tests/fixtures/fixtures.ts`, 4660 lines, full disposable-fixture machinery). Note the salvage lane `jul09_pm_4` did **not** carry that refactor — it landed only evidence, DIRMAP, config, and `scripts/lib/stripe_request.sh`; the refactor was re-authored on main under a different spec-naming scheme (main: `account-delete.spec.ts` / `auth-end-effects.spec.ts`; branch 14: `public-` prefixed). Branch 14's sole remaining unique delta is the `workers: 4` flip, which failed its own Stage-5 3-green-runs gate. Nothing left to salvage. **Full-sweep extension (2026-07-11):** every remaining unmerged branch beyond the five above — 25 branches plus the stray `stage3-pages-parity-publish` worktree — was classified via `git cherry -v main` and three-dot product diffs: 13 carry content that already landed on main (main's re-authored version is newer where it matters), 12 are evidence/checklist-only, **zero orphaned product code — nothing to merge**. The only unique un-landed content anywhere is planning prose on `prep/post-5d631-queued-edits` (a deploy-latency profiling idea and a shared-login verify-harness idea; its third row, the search-preview pagination undercount, already landed via `bfe5e7223`/`3cca4b672`/`c9eedd2dc`). Every listed branch/worktree is safe to delete under the same ABANDON basis. **Remote extension (2026-07-12):** the 16 unmerged `origin/*` branches and the 3 local `backup/stage1_*` refs were audited the same way — zero unlanded product code; details and the deletion-manifest follow-up live in the "Repo GC completion" Planned row above. The pruned `prep/post-5d631-queued-edits` tip's two open-work rows are now recovered as Planned rows above (note: the pagination undercount commits cited earlier in this bullet are the frontend workaround; the backend root fix is the recovered open row).
- **Playwright parallelism deferred behind a green serial baseline.** `web/playwright.config.ts` remains `workers: 1`. Latest owner evidence is `docs/runbooks/evidence/playwright-serial-green/20260710T220025Z/{SUMMARY.md,triage.md}`: two consecutive full serial runs passed with `301 passed`, `19 skipped`, `0 failed`, plus `lint:e2e` and `local-ci --fast` green. Future `workers > 1` work is deferred until evidence shows a real benefit over this now-green serial baseline.
- **Playwright CI local Stripe-mode regression remains open.** Owner: `.github/workflows/ci.yml`; `STRIPE_LOCAL_MODE: "1"` is already owned by the Playwright job environment, so future work should preserve that CI contract instead of adding a parallel local-mode path.
- **June 3 customer-release rerun follow-ups remain open (narrowed).** Metrics product work is closed: `docs/runbooks/evidence/metrics-tab-reconcile/20260711T005607Z/finding.md` classifies the old miss as stale-staging drift and local proof is green/non-vacuous. The former jul11 metrics probe residual is now closed by `docs/runbooks/evidence/metrics-probe-ns-timestamp/20260712T190558Z_devalue_fix/classification.md`, which captures a green authenticated staging rerun after the probe learned SvelteKit's devalue-indexed `__data.json` payload shape (`exit_code == 0`, `metrics_tab_data_ok == true`). The historical `verify_email` S3 inbox item stays tracked by `web/tests/e2e-ui/smoke/customer_release_surfaces.spec.ts`; clean-env support/canary proof is green at `docs/runbooks/evidence/ses-inbox-canary-clean-env/20260711T014655Z/SUMMARY.md`.
- **`e2e-deployed` deploy-currency gate remains open.** Owners: `.github/workflows/ci.yml::e2e-deployed`, `scripts/launch/wait_for_pages_parity.sh`, `scripts/lib/deployable_currency.sh`, and `scripts/deploy_status.sh --json`. The served-marker parity guard and branch-aware staging deploy are landed, and the deployable-currency SSOT fast-follow is landed; keep this row open only for future deployed-lane evidence that consumes those owners rather than raw commit-behind drift.
- **Public launch is NOT announce-ready.** Current verdict is owned by `LAUNCH.md ## STATUS`; the top entry is `2026-07-13 jul12_pm RC verdict launch anchor — NOT announce-ready` (`NOT-READY-real-defects`, sole real defect `staging_billing_rehearsal=invoice_email_ses_not_ready`). That defect's IAM root cause is fixed and live-proven (see the In-VPC dunning IAM row above), **Owner corrected 2026-07-23:** `chats/icg/jul13_12pm_3_s1_rc_launch_verdict.md` was ABANDONED by operator decision 2026-07-16 and must not be dispatched; the RC-verdict owner is `chats/icg/jul16_10am_8_rc_verdict_and_launch_anchor.md` (L8), which has never been dispatched and is currently parked behind Algolia F10. No roadmap row may clear the announce gate until L8 lands a fresh RC verdict in an allowed launch shape and a matching `LAUNCH.md ## STATUS` entry. The newest `LAUNCH.md ## STATUS` entry is the 2026-07-22 `jul16_10am_7` data-plane SHIP/PARK record, which explicitly asserts no launch-shape verdict.

## Archive

Historical implementation details and done-only roadmap prose live in [`implemented/`](implemented/). Do not duplicate that audit trail here.
