# Stage 4 GAP SPEC: no-prod-mutation closure

## Purpose
Record why Stage 4 selected Path C (no mutation) for the canary admin-key incident and define objective criteria that would justify a follow-on mutating stage.

## Observed evidence
- Stage 1 lane bundle: `docs/live-state/20260601T014651Z`
- Stage 1 verdicts:
  - `REPRO_RESULT=no_repro` (`docs/live-state/20260601T014651Z/stage1_repro_verdict.env`)
  - `DRIFT_VERDICT=ssm_eq_ec2` (`docs/live-state/20260601T014651Z/stage1_drift_verdict.env`)
- Stage 4 live re-check evidence bundle: `docs/live-state/20260601T030318Z`
  - `stage4_customer_loop_admin_cleanup_live_contract.txt`: PASS (HTTP 404 accepted)
  - `stage4_lambda_invoke_meta.json`: `StatusCode=200`, no `FunctionError`
  - `stage4_lambda_tail.log`: no `step admin_cleanup failed`, includes `customer loop canary completed successfully`

## Why mutation is unjustified
- There is no current same-invoke reproduction of `admin_cleanup` 401.
- Current drift verdict source indicates no SSM-vs-EC2 admin key mismatch.
- Restarting prod API or mutating canary/runtime code without active reproduction would be speculative and could introduce avoidable production risk.

## Unblock criteria for a follow-on mutating stage
Proceed with mutating Path A/B only when fresh evidence includes both:
1. `REPRO_RESULT=repro_401` from same-invoke canary + CloudWatch request correlation.
2. Branch-specific proof:
- Path A: refreshed `DRIFT_VERDICT=ssm_ne_ec2`.
- Path B: refreshed `DRIFT_VERDICT=ssm_eq_ec2` plus deterministic canary/runtime seam defect reproduced in targeted tests.

Absent those criteria, retain Path C (diagnostic-only) behavior.
