<!-- [scrai:start] -->
## console

| File | Summary |
| --- | --- |
| +layout.server.ts | Stub summary for +layout.server.ts. |
| +page.server.ts | Stub summary for +page.server.ts. |

| Directory | Summary |
| --- | --- |
| account | The account directory contains SvelteKit page server-side logic, likely handling account-related HTTP operations and data fetching for the web frontend. |
| api-keys | Handles the API keys management page for the console dashboard, providing a server-side load function to fetch API keys and indexes for the authenticated user, and two form actions to create new API keys with configurable limits and scopes or revoke existing keys. |
| billing | The billing directory contains a SvelteKit server endpoint for the billing page interface. |
| indexes | The indexes directory is a SvelteKit console route that manages search indexes, with a list view and a dynamic detail page for individual indexes. |
| migrate | The migrate directory contains a SvelteKit page server component for the console's migration interface. |
| onboarding | Onboarding page server that loads dashboard context and provides actions for creating search indexes (with region selection) and generating user credentials, with transient retry logic and session-aware error handling. |
| resend-verification | The resend-verification directory contains a SvelteKit server endpoint that handles resending verification messages, likely for email confirmation or account verification flows. |
| settings | — |
<!-- [scrai:end] -->
