// Vitest is configured to include src/**/*.test.ts. Keep the actual fixture
// owner test colocated with the fixture and import it here as the test entry.
import '../../tests/fixtures/shared_auth_call_counter.test';
