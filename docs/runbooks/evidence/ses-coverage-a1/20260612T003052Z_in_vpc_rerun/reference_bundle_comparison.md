# Reference Bundle Comparison

Current bundle: `docs/runbooks/evidence/ses-coverage-a1/20260612T003052Z_in_vpc_rerun`
Reference bundle: `docs/runbooks/evidence/ses-coverage-a1/20260609T174733Z_in_vpc_rerun`

## Artifact Shape

- Current bundle reuses the copied `stage4_integrity.py` parser from the reference bundle and validates the same six probe IDs.
- `EXPECTED`, `DETECT_KIND`, `REQUIRED_FILES`, and `detect_from_log()` are preserved by direct copy; sidecars and TSV rows are generated by importing the copied parser.
- `probe_results.tsv` keeps the canonical columns: `probe_id`, `rc`, `pass`, `log_path`.
- `probe_results.json`, `probe_run_order.txt`, preflight outputs, AWS credential probes, and secret-scan outputs remain supporting Stage 2 scratch evidence only; current status is owned by `probe_results.tsv`, the six sidecars, `all_green.txt`, and `failure_classifications.json`.

## Probe Outcome Comparison

- Reference non-green rows: `{'verify_email_clickthrough': {'rc': '1', 'pass': '0'}, 'password_reset_clickthrough': {'rc': '1', 'pass': '0'}}`.
- Current non-green rows: `{'verify_email_clickthrough': {'rc': '1', 'pass': '0'}, 'password_reset_clickthrough': {'rc': '1', 'pass': '0'}, 'dunning_email_inbox': {'rc': '1', 'pass': '0'}, 'staging_dunning_delivery': {'rc': '1', 'pass': '0'}}`.
- Current non-green classifications: `{'verify_email_clickthrough': 'verify_email_not_marked_verified', 'password_reset_clickthrough': 'password_reset_token_not_cleared', 'dunning_email_inbox': 'dunning_owner_rehearsal_reset_failed', 'staging_dunning_delivery': 'tenant_allowlist_shell_quoting_backslash_uuid'}`.
- Current parser-passing rows: `['ses_bounce', 'ses_complaint']`.
- New classification shape versus reference: dunning wrapper failure plus the `%q`/backslash `FJCLOUD_TEST_TENANT_IDS` reset blocker.

## Disposition

- The current bundle is not all-green because `verify_email_clickthrough`, `password_reset_clickthrough`, `dunning_email_inbox`, and `staging_dunning_delivery` are non-green.
- `ses_bounce` and `ses_complaint` remain green with `rc=0` and parser sidecar `pass=true`.
- This current bundle supersedes older status prose for the `20260612T003052Z` rerun lane; older bundles are reference shape inputs only.
