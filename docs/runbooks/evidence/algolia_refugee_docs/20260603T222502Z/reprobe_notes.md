# Re-probe Notes

- Re-ran Stage 4 validation at source HEAD `237b40653eeec8c6a12e41a3740583f1b2659f2b` after finding the prior 20260603T221604Z bundle identified pre-remediation source HEAD 29a5e2297.
- The first staging retry used process substitution for the SSM hydrator and inherited a stale local admin key, producing HTTP 401 only at admin cleanup after all migration cases had already succeeded.
- Re-probed SSM hydration directly; it exported ADMIN_KEY and SES_FROM_ADDRESS. The successful staging command used `eval "$(bash scripts/launch/hydrate_seeder_env_from_ssm.sh staging)"` so SSM values replaced local env values before validation.
- Prod contract-only output proves documented HTTP verbs, not OPTIONS-only path reachability.
- Hydrated staging output includes validator-owned `migration case succeeded: <marker>` lines for every executable migration marker and completed teardown/admin cleanup.
- Provenance refresh (s112): re-ran the reproducible commands (`bash scripts/check-sizes.sh`, `bash scripts/tests/validate_customer_quickstart_test.sh` => 90 passed, the local-links python probe, the corrected external URL probe, and `bash scripts/validate_customer_quickstart.sh prod --contract-only`) at source HEAD `7e774e20693852b6ae7e23e2aca30a77a1098a63`. Validator/test source bytes are unchanged from `237b40653eeec8c6a12e41a3740583f1b2659f2b` (commit `7e774e20` only added the evidence bundle), so the hydrated staging output captured under that parent SHA remains the authoritative staging proof for the current source revision. Updated `head_sha.txt` and `commands.md` to record the HEAD this bundle is now validated against.
- Provenance refresh (s113): re-ran the same reproducible commands (`bash scripts/check-sizes.sh`, `bash scripts/tests/validate_customer_quickstart_test.sh` => 90 passed, the local-links python probe, the corrected external URL probe, and `bash scripts/validate_customer_quickstart.sh prod --contract-only`) at source HEAD `aaa42231a38ce21dd62268f133c35ad56246d661`. Commit `aaa42231a` only updated evidence-bundle files (no validator/test source changes), so the hydrated staging output captured under parent SHA `237b40653` remains the authoritative staging proof for the current source revision. Updated `head_sha.txt` and `commands.md` to record the HEAD this bundle is now validated against.
- Provenance refresh (s114): re-ran the same reproducible commands (`bash scripts/check-sizes.sh`, `bash scripts/tests/validate_customer_quickstart_test.sh` => 90 passed, the local-links python probe, the corrected external URL probe, and `bash scripts/validate_customer_quickstart.sh prod --contract-only`) at source HEAD `aeec3ef440332158fb18f186f4852f908b1efb1b`. `git diff aaa42231a aeec3ef44 -- ':(exclude)docs/runbooks/evidence/'` is empty (commit `aeec3ef44` only updated evidence-bundle files), so the hydrated staging output captured under parent SHA `237b40653` remains the authoritative staging proof for the current source revision. Structural note: the bundle commit cannot contain its own SHA, so each in-place refresh records the immediate parent of the bundle-introducing commit; this is the source HEAD whose bytes the captured outputs correspond to. Updated `head_sha.txt` and `commands.md` to record the HEAD this bundle is now validated against.
- Provenance refresh (s116): re-ran the same reproducible commands (`bash scripts/check-sizes.sh`, `bash scripts/tests/validate_customer_quickstart_test.sh` => 90 passed, the local-links python probe, the corrected external URL probe, and `bash scripts/validate_customer_quickstart.sh prod --contract-only`) at source HEAD `a9503f65c76501586140a57fafbeda43250dda18`. `git diff 237b40653 a9503f65c -- ':(exclude)docs/runbooks/evidence/'` is empty (every commit since `237b40653` has been an evidence-only refresh — `7e774e20`, `aaa42231a`, `aeec3ef44`, `a9503f65c`), so the hydrated staging output captured under parent SHA `237b40653` remains the authoritative staging proof for the current source revision. Per the s114 structural note, this in-place refresh records the immediate parent of the bundle-introducing commit; this is the source HEAD whose bytes the captured outputs correspond to. Updated `head_sha.txt` and `commands.md` to record the HEAD this bundle is now validated against.
- Provenance refresh (s138): re-ran uncached reproducible probes at source HEAD 3305ea8f7fb8fff1629a7388d8f3f5a5290a4d83 after the supplied open bug reported stale Stage 4 evidence. Local links, corrected external URLs, non-mutating prod verb contract, and hydrated staging validation all passed. The current-HEAD validation cache supplied clean-tree passes for bash scripts/check-sizes.sh and bash scripts/tests/validate_customer_quickstart_test.sh (90 passed, 0 failed). Unlike the prior evidence-only refresh chain, current HEAD includes a source comment change in scripts/validate_customer_quickstart.sh, so this refresh updates head_sha.txt, commands.md, command outputs, and utc_timestamp.txt to identify the actual validated Stage 4 revision. Open questions: none
